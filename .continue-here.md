---
phase: refactor-programa-tejido-duplicados
task: phase2-regenerar-lineas
status: in_progress
last_updated: 2026-04-04T22:00:00.000Z
---

<current_state>
Phase 1 COMPLETADA y commiteada: `suppressObservers` / `restoreObservers` en ReqProgramaTejido.
Commit: c0ce756f

Phase 2 en análisis: consolidar el patrón `new ReqProgramaTejidoObserver(); foreach (...) { $observer->saved($r); }`
Se analizaron las variantes antes de diseñar la API. Aún no se escribió código.
</current_state>

<completed_work>

## Phase 1 — suppressObservers / restoreObservers ✅

- Agregados a `app/Models/Planeacion/ReqProgramaTejido.php` (líneas 265 y 277)
- Refactorizados 6 archivos: DuplicarTejido, DividirTejido, DragAndDropTejido, BalancearTejido, VincularTejido, EliminarTejido
- Commit: `c0ce756f`

</completed_work>

<remaining_work>

## Phase 2 — regenerarLineas (pendiente)

### Método a agregar en ReqProgramaTejido

```php
public static function regenerarLineas(iterable $registros): void
{
    $observer = new \App\Observers\ReqProgramaTejidoObserver();
    foreach ($registros as $registro) {
        if ($registro instanceof static && $registro->Id) {
            $observer->saved($registro);
        }
    }
}
```

### Variantes identificadas (usages a reemplazar)

| Archivo | Líneas | Patrón |
|---------|--------|--------|
| DuplicarTejido | 596-598 | `$observer = new ...; $observer->saved($registro)` — 1 registro |
| DividirTejido | 750-769 | `$observer = new ...; foreach ($registrosParaObserver) { $observer->saved($r); }` |
| DividirTejido | 1314-1320 | mismo patrón |
| BalancearTejido | 275-291 | loop con try/catch interno — DEJAR como está (tiene manejo de error específico por iteración) |
| BalancearTejido | 598-599 | `$observer->saved($registro->fresh())` — 1 registro con fresh() |
| DragAndDropTejido | 177-189 | loop con `$m->refresh()` antes de saved — DEJAR como está (refresh es intencional) |
| EliminarTejido | 102-104 | `::find($id)` + `$observer->saved($r)` en loop |
| EliminarTejido | 201-204 | loop estándar |
| EliminarTejido | 296-307 | loop estándar |
| EliminarTejido | 399-401 | `::find($id)` + conditional |
| EliminarTejido | 515-543 | loop estándar |
| EliminarTejido | 609-612 | loop estándar |
| UpdateTejido | 589-590 | `$observer->saved($registro)` — 1 registro |
| VincularTejido | 182-187 | loop estándar |
| VincularTejido | 360-363 | loop estándar |

### Decisión de diseño
- Los casos simples (loop estándar + single record) → `ReqProgramaTejido::regenerarLineas($registros)`
- BalancearTejido 275-291: DEJAR (tiene try/catch por iteración con Log específico)
- DragAndDrop 177-189: DEJAR (tiene `$m->refresh()` + query con filtros adicionales)
- Los `::find($id)` en EliminarTejido: pueden cargar el modelo primero y pasar colección

## Phase 3 — eliminar wrappers privados calcularFormulasEficiencia (pendiente)

- `DividirTejido::calcularFormulasEficiencia()` privado (línea ~853) — solo delega a TejidoHelpers
- `UpdateTejido::calcularFormulasEficiencia()` privado (línea ~830) — solo delega a TejidoHelpers
- Reemplazar `self::calcularFormulasEficiencia(...)` → `TejidoHelpers::calcularFormulasEficiencia(...)` directo
- Eliminar ambos métodos privados

</remaining_work>

<decisions_made>
- Phase 1 usó sub-agentes en paralelo (1 agente = 1 archivo)
- suppressObservers() devuelve ?object (el dispatcher), restoreObservers() lo acepta y re-registra el observer
- BalancearTejido y DragAndDropTejido mantienen el split getEventDispatcher/unsetEventDispatcher porque hay lógica entre ambas llamadas
</decisions_made>

<next_action>
1. Agregar `regenerarLineas(iterable $registros)` a ReqProgramaTejido
2. Reemplazar los usos simples con sub-agentes en paralelo (uno por archivo)
3. Dejar BalancearTejido 275-291 y DragAndDrop 177-189 sin tocar
4. Luego Phase 3: eliminar wrappers privados
</next_action>
