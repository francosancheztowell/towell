---
phase: 17-ux
plan: "02"
wave: 3
depends_on: [16-01, 15-01, 15-02, 12-01, 18-01, 20-02]
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09, UX-11, UX-12, UX-13, UX-14, UX-15, UX-16, UX-17, UX-18]
excluded: [UX-10]  # contraseñas: fuera por decisión del owner (2026-09-25)
files_modified:
  - resources/views/layouts/app.blade.php
  - resources/views/components/layout-head.blade.php
  - resources/views/components/layout-styles.blade.php
  - resources/views/components/layout/global-loader.blade.php
  - resources/views/components/navbar/navbar.blade.php
  - resources/views/produccionProceso.blade.php
  - resources/views/errors/{layout,403,404,419,500,503}.blade.php
  - resources/views/login.blade.php, resources/views/components/auth/login-form.blade.php (solo textos/acentos)
  - lang/es/{auth,validation,passwords,pagination}.php, lang/es.json
  - config/app.php (locale/fallback/faker por defecto)
  - resources/js/utils/{notifications,http,sesion,acciones-tactiles}.ts
  - resources/js/componentes/{index,conexion,dias-liberar}.ts
  - resources/js/app-core.js (initNavigation → window.loader)
  - resources/js/types/global.d.ts
  - resources/css/app.css (foco visible, user-select de datos)
  - app/Http/Controllers/ModulosController.php (UX-16, solo strings)
  - tests/Feature/Ux/**, tests/Js/utils-*.test.mjs
must_haves:
  truths:
    - "Un redirect()->with('error', …) al home se ve (flash) aunque la vista no lo pinte."
    - "Toda página del layout tiene <title> propio derivado del título de página, del módulo o del default."
    - "El layout nunca emite dos <h1> anidados (navbar + x-layout.page-title)."
    - "Se puede hacer pinch-zoom salvo en las pantallas andón listadas."
    - "Folios y datos se pueden seleccionar y copiar; el chrome (navbar, botones) no."
    - "Validaciones de Laravel en español; locale es."
    - "403/404/419/500/503 cargan CSS, tienen el mismo 'Volver' y 500 muestra el código del evento de SU excepción."
    - "419 y 401 dan el mismo aviso y la misma acción en http, Livewire y formularios."
    - "Sin conexión aparece un banner; al volver desaparece."
    - "Toasts con una sola duración y debajo del navbar."
    - "Botones de ícono globales con aria-label; foco visible por teclado en toda la app."
  artifacts:
    - "utils/acciones-tactiles.ts: long-press + botón '⋮' que dispara el mismo menú que el clic derecho."
    - "17-02-CHECKLIST.md (UX-18) por pantalla, para las 19-xx."
    - "17-02-SUMMARY.md, HANDOFF.md."
---

<objective>
Cerrar los hallazgos globales de UX (UX-01..09, 11..18) en layout, componentes globales, utils y páginas
de error, sin tocar vistas/JS de módulos (lo que cada módulo debe adoptar va a HANDOFF + checklist).
Mismo diseño salvo lo que corrige el hallazgo.
</objective>

<propiedad>
Dueño (SESIONES-OLA-3.md): layouts/**, layout-head/scripts/styles, global-loader, navbar.blade.php,
errors/**, produccionProceso, lang/**, config/app.php (locale), resources/js/{utils,componentes,types}/**,
app.css, fontawesome-display.css, textos de login y components/auth/**, tests/Js/utils-*, tests/Feature/Ux/**.
Autorizados por el prompt de apertura fuera de esa lista: ModulosController (UX-16, solo strings),
app-core.js (HANDOFF 16 A3). Los parciales `components/navbar/sections/**`, `components/ui/**`,
`components/tabla*.blade.php`, `components/layout/page-*` (DS, sin sesión activa en la Ola 3) solo se
tocan para aria-label / tamaño de texto / foco (UX-07, UX-14, UX-15), sin cambiar su API.
Prohibido: vistas y JS de módulos, AuthController, reglas de contraseña, PT, package.json, vite.config.js,
bootstrap/app.php (sus textos sin acento van a HANDOFF).
</propiedad>

<tasks>

<task type="auto">
<name>1. UX-01 flash en layout y home</name>
<action>El layout ya monta x-ui.flash (DS-09). Verificar con test que el home (produccionProceso) muestra
session('error') ("No tienes acceso a este módulo"). Home: estado vacío con x-empty.empty-state si
$modulos está vacío.</action>
<verify>tests/Feature/Ux/FlashLayoutTest.php</verify>
</task>

<task type="auto">
<name>2. UX-02 title, UX-03 un solo h1, HANDOFF 18-01 #1</name>
<action>layout-head: `<title>` = @yield('title') › texto plano de @yield('page-title') › moduleNameForRoute() ›
"Producción Towell", siempre con sufijo "· Towell". navbar: el contenedor del título es `<h1>` solo si el
contenido de page-title no trae ya un `<h1>` (x-layout.page-title); si lo trae, `<div>`.
Meta towell-ruta: getName() ?? uri().</action>
<verify>tests/Feature/Ux/LayoutHeadTest.php (title derivado, un solo h1, towell-ruta con uri()).</verify>
</task>

<task type="auto">
<name>3. UX-04 zoom, UX-05 user-select, UX-07 12 px, UX-15 foco</name>
<action>Viewport sin `maximum-scale=1, user-scalable=no`, salvo andón: lista de rutas en layout-head
(crudo.index y las que se confirmen) + `@section('viewport-fijo')` para que un módulo se sume.
Body: quitar `user-select:none` y `-webkit-touch-callout:none` globales; aplicarlos solo al chrome
(nav, botones, [role=button], .select-none) en app.css; tablas, inputs y datos seleccionables.
Texto < 12 px en layout/navbar/componentes globales → `text-caption`/`text-xs`.
app.css: `:focus-visible` con anillo de 2 px en toda la app (sin tocar el foco de ratón).</action>
<verify>LayoutHeadTest (viewport); captura; grep de text-[9-11px] en los archivos tocados = 0.</verify>
</task>

<task type="auto">
<name>4. UX-06 acciones sin clic derecho (helper global)</name>
<action>resources/js/utils/acciones-tactiles.ts: `accionesTactiles(root, selector, abrir, {demoraMs})`:
long-press (pointerdown ≥ 500 ms sin mover > 10 px) dispara `abrir(el, {x,y})` y suprime el click
siguiente; `botonAcciones(el, abrir)` inserta un botón "⋮" (aria-label "Más acciones", 44 px) para
tablet. Expuesto como `window.accionesTactiles`. No se aplica a ningún módulo aquí.</action>
<verify>tests/Js/utils-acciones-tactiles.test.mjs (fake DOM + timers).</verify>
</task>

<task type="auto">
<name>5. UX-08 idioma</name>
<action>lang/es/{validation,auth,passwords,pagination}.php + lang/es.json (textos comunes de framework);
config/app.php locale/fallback 'es', faker 'es_MX' (la .env de producción debe decir APP_LOCALE=es:
SUMMARY + HANDOFF de .env.example). Acentos en textos de layout, errores, login y login-form
(sin tocar name/pattern/inputmode/oninput).</action>
<verify>tests/Feature/Ux/IdiomaTest.php (mensaje required en español).</verify>
</task>

<task type="auto">
<name>6. UX-09 páginas de error + HANDOFF 20-02 #5</name>
<action>errors/layout.blade.php propio con @vite('resources/css/app.css'), title, un h1 y un solo
"Volver al inicio" (route produccion.index si hay sesión, login si no) + "Regresar" (history.back sin
onclick inline: script mínimo o <a>). 403/404/419/500/503 lo extienden conservando imagen/colores.
500: código de referencia buscando $exception y getPrevious() en `monitoreo.eventos_por_excepcion`,
con eventoId solo como respaldo si no hay excepción. 404: mensaje "Página no encontrada".</action>
<verify>tests/Feature/Ux/PaginasErrorTest.php (cada código renderiza, carga CSS, 500 muestra el código de la excepción correcta).</verify>
</task>

<task type="auto">
<name>7. UX-11 419/401 unificado, UX-12 banner, UX-13 toasts</name>
<action>utils/sesion.ts: `sesionExpirada()` (un aviso + recarga) usado por http.ts y por un hook de
Livewire (`Livewire.hook('request', … fail 419/401 → preventDefault + sesionExpirada)`) registrado en
componentes/index.ts. Formularios HTML ya redirigen al login con flash (bootstrap/app.php; acentos → HANDOFF).
componentes/conexion.ts: banner fijo bajo el navbar (role=status) con `towell:conexion` + online/offline
del navegador. notifications.ts: TOAST_DURATION única (5 s) y `.towell-toasts` a
`top: calc(var(--pt-navbar-height,64px) + .75rem)`.</action>
<verify>utils-http / utils-notifications / nuevo componentes-conexion test.</verify>
</task>

<task type="auto">
<name>8. UX-14 aria-label, UX-16 mojibake, HANDOFF PT B2 y 16 A3</name>
<action>aria-label en botones de ícono de navbar, avatar, loader, cierre de flash/alert. ModulosController:
revertir doble codificación (Ã³→ó…) sin tocar lógica. mostrarModalDiasLiberar → componentes/dias-liberar.ts
(lee días/base de data-* del nav; mismo Swal y validación; window.mostrarModalDiasLiberar para el onclick
existente de PT). app-core.js initNavigation usa window.loader.show(LOADER_DELAY)/hide().</action>
<verify>typecheck, test JS de dias-liberar (validación), ratchet baja (<script> inline -1).</verify>
</task>

<task type="auto">
<name>9. UX-18 checklist, HANDOFF, evidencia</name>
<action>17-02-CHECKLIST.md (por pantalla: title, h1, flash, zoom, selección, acciones táctiles, 12 px,
aria-label, foco, 419, textos con acento, estados vacío/carga/error). HANDOFF por módulo (h1 propios,
contextmenu en 12 archivos, text-[9-11px] en 33 vistas, duraciones de toast locales, títulos, andón).
Capturas antes/después 1280×800 y 768×1024 (login, home, pantalla por módulo grande, errores) en
.planning/phases/17-ux/evidencia/. Checks completos + code-review. 17-02-SUMMARY.md. Push.</action>
</task>

</tasks>
