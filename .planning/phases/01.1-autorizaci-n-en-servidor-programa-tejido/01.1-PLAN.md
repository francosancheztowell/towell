---
phase: planeacion-restructuring
plan: "01.1"
wave: 1
depends_on: ["01"]
autonomous: true
requirements: [PT-CON-01, BUG-003]
files_modified:
  - routes/modules/planeacion.php
  - app/Http/Controllers/Planeacion/ProgramaTejido/ColumnasProgramaTejidoController.php
  - resources/views/modulos/programa-tejido/liberar-ordenes/index.blade.php
  - tests/Feature/Planeacion/PlaneacionEscrituraAutorizacionTest.php
  - tests/Feature/Planeacion/Concerns/ConPermisosPlaneacion.php
  - tests/fixtures/planeacion/programa-tejido/rutas.json
must_haves:
  truths:
    - "Ninguna ruta de escritura de routes/modules/planeacion.php queda sin module.permission salvo las justificadas como de solo lectura."
    - "Liberar Muestras exige crear del módulo Muestras (idrol 5), decisión del owner."
    - "El permiso de una superficie no autoriza la otra."
  artifacts:
    - "Test de inventario que rompe ante una escritura nueva sin permiso."
    - "Snapshot de rutas regenerado y diff revisado."
  key_links:
    - "tests/fixtures/planeacion/programa-tejido/rutas.json -> inventario de escrituras"
    - "EnsureModulePermission (solo lectura) -> userCan()"
---

<objective>
Cerrar la autorización en servidor de las escrituras de Planeación que PT-01 dejó abiertas, sin tocar EnsureModulePermission ni la lógica de negocio.
</objective>

<context>
Del snapshot de PT-01 (240 rutas) salen 20 rutas POST/PUT/PATCH/DELETE sin `module.permission`:
- 7 son `Route::redirect` 301 (aceptan cualquier método por construcción).
- 8 son lecturas por POST: codificacion.buscar, lmat.matriz-calibre.lote, datos-relacionados ×2, preview-fechas-balanceo ×2, verificar-cambio-telar ×2.
- 2 son preferencias por usuario: columnas ×2. Aceptaban `usuario_id` del request (IDOR).
- 2 escriben un archivo en planta: descargar-programa ×2, el TXT UNC. Hoy no tienen permiso.
- Aparte está el gap de permiso de PT-01: `muestras.liberar-ordenes.procesar` exige `crear,2`.
</context>

<tasks>

<task type="auto">
<name>01.1.1 Permisos en rutas</name>
<files>routes/modules/planeacion.php</files>
<action>
- Liberar Muestras: `crear,2` pasa a `crear,5`.
- descargar-programa lleva `registrar,2` en Programa y `registrar,5` en Muestras. Es el mismo permiso con el que la UI muestra el botón (`x-navbar.button-report`).
</action>
<verify>Test por ruta: sin permiso → 403; con el permiso de la otra superficie → 403; con el correcto → pasa el router.</verify>
<done>El snapshot muestra solo esos tres cambios de middleware.</done>
</task>

<task type="auto">
<name>01.1.2 IDOR de columnas</name>
<files>app/Http/Controllers/Planeacion/ProgramaTejido/ColumnasProgramaTejidoController.php</files>
<action>Un `usuario_id` distinto de `Auth::id()` responde 403, y siempre se usa `Auth::id()`. El front nunca manda `usuario_id` (ni `window.App.user` ni `window.authUserId` existen), así que no se rompe nada.</action>
<verify>Test: un `usuario_id` ajeno → 403; el propio → 200.</verify>
<done>Ningún usuario lee ni pisa preferencias de otro.</done>
</task>

<task type="auto">
<name>01.1.3 UI de Liberar alineada al servidor</name>
<files>resources/views/modulos/programa-tejido/liberar-ordenes/index.blade.php</files>
<action>En Muestras el botón Liberar evalúa `crear` del idrol 5 (`moduleId`). Es gating de permiso, no cambio visual.</action>
<verify>Revisión manual; el servidor ya lo cubre con tests.</verify>
<done>No se ofrece una acción que termina en 403.</done>
</task>

<task type="auto">
<name>01.1.4 Inventario y snapshot</name>
<files>tests/Feature/Planeacion/PlaneacionEscrituraAutorizacionTest.php, tests/fixtures/planeacion/programa-tejido/rutas.json</files>
<action>
- Allowlist documentada de escrituras sin permiso de módulo.
- Regenerar el snapshot con `PT_ROUTES_SNAPSHOT=update` y revisar el diff.
- El gap de liberar sale de `DIFERENCIAS` y la caracterización pasa a `[]`.
</action>
<verify>`php artisan test tests/Feature/Planeacion`.</verify>
<done>Una ruta de escritura nueva sin permiso rompe el test.</done>
</task>

</tasks>

<verification>
- Suite completa en verde.
- `git diff tests/fixtures/...rutas.json` contiene solo los cambios esperados.
</verification>

<rollback>
Revertir el commit: son middlewares y un guard de controller, sin cambios de datos.
</rollback>
