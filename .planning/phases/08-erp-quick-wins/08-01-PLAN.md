---
phase: 08-erp-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Providers/AppServiceProvider.php
  - app/Support/Programas/ProgramaModulo.php
  - app/Livewire/UrdEng/EdicionOrden.php
  - app/Livewire/UrdEng/EdicionOrdenes.php
  - app/Livewire/InventarioTrama/NuevoRequerimiento.php
  - app/Livewire/InventarioTrama/ConsultarRequerimiento.php
  - app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php
  - tests/Feature/LivewirePermisosModuloTest.php
  - tests/Feature/EditarOrdenPermisosTest.php
  - tests/Feature/EngomadoEdicionOrdenesTest.php
  - tests/Unit/ProduccionUrdidoCicloJuliosTest.php
  - tests/Unit/ProduccionUrdidoOrdenesRealesTest.php
  - tests/Feature/NuevoRequerimientoLivewireTest.php
  - tests/Feature/ConsultarRequerimientoLivewireTest.php
  - tests/Feature/NotificarCortadoRolloOrdenProduccionTest.php
autonomous: true
requirements: [ERP-F0-01, ERP-F0-02, ERP-F0-06]

must_haves:
  truths:
    - "Una acción Livewire enviada a /livewire/update vuelve a pasar por EnsureModulePermission con los parámetros de la ruta donde se montó la página"
    - "Un usuario sin 'acceso' al módulo (por idrol) no puede montar ni hidratar NuevoRequerimiento, ConsultarRequerimiento, EdicionOrden ni EdicionOrdenes (403)"
    - "Un usuario con 'acceso' pero sin 'modificar'/'crear' no puede guardar nada desde esas pantallas (403) y la BD no cambia"
    - "guardarMetrosFila solo escribe filas de producción del folio abierto y no cerradas en AX; un Id de otro folio o un valor no numérico no cambia la BD"
    - "GET /tejedores/cortadoderollo/orden-produccion ya no ejecuta SELECT @@VERSION ni devuelve la clave 'debug'"
  artifacts:
    - path: "app/Providers/AppServiceProvider.php"
      provides: "Registro de EnsureModulePermission como middleware persistente de Livewire"
      contains: "addPersistentMiddleware"
    - path: "app/Support/Programas/ProgramaModulo.php"
      provides: "idrol verificado de Programa Urdido / Programa Engomado"
      contains: "function idrol"
    - path: "tests/Feature/LivewirePermisosModuloTest.php"
      provides: "Tests de acceso/edición por idrol de las 4 pantallas y del middleware persistente"
    - path: "tests/Feature/NotificarCortadoRolloOrdenProduccionTest.php"
      provides: "Test de que getOrdenProduccion no expone debug"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "App\\Http\\Middleware\\EnsureModulePermission"
      via: "Livewire::addPersistentMiddleware"
      pattern: "addPersistentMiddleware\\(\\[EnsureModulePermission::class\\]\\)"
    - from: "app/Livewire/UrdEng/EdicionOrden.php"
      to: "produccionEditable()"
      via: "guardarMetrosFila filtra por folio y AX"
      pattern: "produccionEditable\\(\\$this->orden\\(\\)\\)->whereKey"
    - from: "app/Livewire/UrdEng/EdicionOrden.php"
      to: "userCan('modificar', idrol)"
      via: "booted()/autorizarEdicion()"
      pattern: "userCan\\('modificar'"
---

<objective>
Cerrar los tres huecos de seguridad de la Fase 0 de la auditoría ERP (§2.5, §2.1 fila 14): acciones Livewire sin re-chequeo de permisos, IDOR en `EdicionOrden::guardarMetrosFila` y fuga de `SELECT @@VERSION` en `getOrdenProduccion`.

Purpose: hoy `module.permission` solo protege la carga de la página; cualquier POST a `/livewire/update` se salta el permiso, y 4 pantallas no tienen chequeo propio (`EdicionOrden` fija `puedeEditar = true`). `guardarMetrosFila` acepta cualquier Id de producción que mande el cliente.
Output: middleware persistente registrado, gates por idrol en 4 componentes, IDOR corregido, endpoint sin debug; cada cambio con su test en el mismo commit.

Commits: uno por requisito (CONTEXT, discreción). Las tareas 1-3 son ERP-F0-01 y se commitean juntas al cerrar la Tarea 3; Tarea 4 = ERP-F0-02; Tarea 5 = ERP-F0-06. Mensajes: `fix(08-01): ERP-F0-0N <resumen>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-erp-quick-wins/08-CONTEXT.md
@.planning/auditoria_erp_2026-09.md
@CLAUDE.md
@tests/Feature/EditarOrdenPermisosTest.php
@tests/Concerns/SiembraPermisos.php

<interfaces>
Extraído del código (2026-09-22). No hace falta explorar más.

app/Http/Middleware/EnsureModulePermission.php (alias `module.permission`, registrado en bootstrap/app.php):
  handle(Request $request, Closure $next, string $action, string $module): Response
  — llama userCan($action, $module); si expectsJson() responde JSON 403, si no abort(403).

vendor/livewire (v4.3.3) PersistentMiddleware: `Livewire::addPersistentMiddleware(array)`; en /livewire/update reaplica los middleware de la ruta ORIGINAL cuya clase coincide (compara `Str::before($value, ':')`, así que `module.permission:acceso,31` resuelto a `App\Http\Middleware\EnsureModulePermission:acceso,31` entra). NO se aplica en `Livewire::test()` (solo en la ruta real de Livewire), por eso los componentes necesitan además su chequeo propio. Getter: `app(\Livewire\Mechanisms\PersistentMiddleware\PersistentMiddleware::class)->getPersistentMiddleware()`.

Ciclo de vida Livewire 4 (SupportLifecycleHooks): mount → boot, mount, booted; update → boot, hydrate, booted. `booted()` corre en CADA request con las propiedades ya cargadas (en boot() `$module` aún tiene el valor por defecto).

app/Helpers/permission-helpers.php:
  userCan(string $action, string|int $module): bool — si `is_numeric($module)` busca por idrol; si no, por nombre (SYSRoles tiene 5 nombres duplicados: usar SIEMPRE idrol).
  userPermissions($module) memoiza en el contenedor: `permisos.roles` (colección keyBy nombre en minúsculas → {idrol, modulo}) y `permisos.usuario.{idusuario}` (keyBy idrol → {acceso, crear, modificar, eliminar, registrar}).

tests/Concerns/SiembraPermisos.php: `sembrarPermisos(int $idusuario, array [idrol => 'Nombre'])` siembra los 5 permisos en 1. Para casos "acceso sí, modificar no" sembrar a mano `app()->instance('permisos.usuario.'.$id, collect([idrol => (object)[...]]))` como hace PlaneacionMutationAuthorizationTest.

app/Support/Programas/ProgramaModulo.php (enum Urdido|Engomado):
  resolve(string): self; permissionModule(): string ('Programa Urdido' | 'Programa Engomado'); programModel(); productionModel().

app/Livewire/UrdEng/EdicionOrden.php:
  boot(BomMaterialesService) — abort_unless(Auth::check())
  mount(string $module, int $ordenId, bool $fromReimpresion=false) — línea ~125: `$this->puedeEditar = true;` (comentario "ponytail: gate de puesto/supervisor apagado")
  updatedForm($valor, $campo), guardarMetrosFila(int $registroId, mixed $valor) (:152-180, usa `$modelo::find($registroId)`), confirmarMetros(string), confirmarNoTelas(), updatedJulios($valor, $clave), updatedMetrosFila($valor, string $id), guardarJulio(int $fila)
  private orden(): Model; private produccionEditable(Model $orden): Builder (:622 — where Folio = orden.Folio y AX null o != 1); private avisar(tipo, msg) → notificar() despacha 'program-board-notify'.

app/Livewire/UrdEng/EdicionOrdenes.php: boot() abort_unless(Auth::check()); mount(string $module='engomado'); acciones seleccionar/editar/calificar/ordenar/limpiarFiltros (solo lectura + redirect). `App\Livewire\Engomado\EdicionOrdenes` es alias vacío que extiende a esta clase.

app/Livewire/InventarioTrama/NuevoRequerimiento.php: mount(?string $folio); mutaciones públicas actualizarCantidad(int,int,float), agregarRequerimiento(), eliminarFila(int,int), guardar(). (No borrar irATelar/__construct/guardar aquí: fila 11 de §2.1 está fuera de alcance.)
app/Livewire/InventarioTrama/ConsultarRequerimiento.php: mount(); cambiarStatus(string, RequerimientoStatusService) muta; editar() redirige.

Páginas que montan estos componentes (rutas SIN module.permission hoy):
  tejido.php:170 y :240 (nuevo requerimiento), :89, :173 y :253 (consultar), urdido/engomado `editar-ordenes-programadas` y `reimpresion-*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Resolver idrol de las 4 pantallas y registrar el middleware persistente (ERP-F0-01, parte 1)</name>
  <files>app/Providers/AppServiceProvider.php, tests/Feature/LivewirePermisosModuloTest.php</files>
  <action>
Paso A — idrol (solo lectura, por decisión D de CONTEXT "resolver módulo por idrol, no por nombre"). Con `php artisan tinker --execute` contra la conexión `sqlsrv` (ProdTowel) listar `idrol, modulo, Ruta, Dependencia` de SYSRoles cuyo `Ruta` o `modulo` contenga `requerimiento`, `Programa Urdido`, `Programa Engomado`, `reimpresion` o `editar-ordenes`, y para cada idrol candidato contar en SYSUsuariosRoles cuántos usuarios tienen `acceso=1`, `crear=1`, `modificar=1`. Elegir: (1) Nuevo Requerimiento (Inventario Trama), (2) Consultar Requerimiento, (3) Programa Urdido, (4) Programa Engomado. Regla de mapeo: la fila cuya `Ruta` coincide con la URL del menú (`/tejido/inventario/trama/...`, `/invtrama/...`, `/modulo-...`); si Nuevo y Consultar son una sola fila, usar la misma para ambas. Si algún idrol elegido tiene 0 usuarios con `acceso` o con el permiso de mutación que se le va a exigir (crear para Nuevo, modificar para Consultar y Programa Urd/Eng), el mapeo está mal: detenerse y reportar. Si la conexión a 192.168.2.28 no responde, es un auth gate dinámico: devolver checkpoint pidiendo al usuario ejecutar la misma consulta y pegar el resultado; no inventar números. Anotar la tabla idrol/nombre/conteos en el SUMMARY.

Paso B — en `AppServiceProvider::boot()`, junto a `Livewire::setUpdateRoute(...)`, añadir `Livewire::addPersistentMiddleware([EnsureModulePermission::class]);` con su `use App\Http\Middleware\EnsureModulePermission;`. Una línea de comentario: las acciones Livewire re-ejecutan el `module.permission` de la ruta donde se montó la página (auditoría §2.5).

Paso C — crear `tests/Feature/LivewirePermisosModuloTest.php` (extiende Tests\TestCase) con el primer test: `test_ensure_module_permission_es_middleware_persistente_de_livewire` que afirma que `getPersistentMiddleware()` contiene `EnsureModulePermission::class`. Los tests de las pantallas se añaden en Tareas 2-3.

No commitear todavía (ERP-F0-01 se commitea al final de la Tarea 3).
  </action>
  <verify>
    <automated>php artisan test --filter=LivewirePermisosModuloTest</automated>
  </verify>
  <done>Tabla de 4 idrol verificados (con conteos > 0) registrada para el SUMMARY; `grep -c "addPersistentMiddleware" app/Providers/AppServiceProvider.php` = 1; el test pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Gate por idrol en EdicionOrden y EdicionOrdenes (ERP-F0-01, parte 2)</name>
  <files>app/Support/Programas/ProgramaModulo.php, app/Livewire/UrdEng/EdicionOrden.php, app/Livewire/UrdEng/EdicionOrdenes.php, tests/Feature/LivewirePermisosModuloTest.php, tests/Feature/EditarOrdenPermisosTest.php, tests/Feature/EngomadoEdicionOrdenesTest.php, tests/Unit/ProduccionUrdidoCicloJuliosTest.php, tests/Unit/ProduccionUrdidoOrdenesRealesTest.php</files>
  <behavior>
    - Sin permisos sembrados: Livewire::test(EdicionOrden, module urdido) → 403; Livewire::test(EdicionOrdenes, module urdido) → 403.
    - acceso=1, modificar=0 sobre el idrol de Programa Urdido: EdicionOrden monta, `set('form.NoTelarId','999')` → 403 y la BD conserva '101'.
    - acceso=1, modificar=1: los tests existentes de EditarOrdenPermisosTest siguen pasando (guardado y sincronización a Engomado).
    - EngomadoEdicionOrdenesTest (incluido el GET /engomado/reimpresion-engomado) pasa con permisos sembrados.
  </behavior>
  <action>
Per D de CONTEXT ERP-F0-01 (idrol, no nombre):
1. `ProgramaModulo`: añadir `public function idrol(): int` con `match` Urdido → idrol verificado de Programa Urdido, Engomado → idrol de Programa Engomado, cada uno con comentario `// Programa Urdido` / `// Programa Engomado`. No tocar `permissionModule()` (lo usa ProgramBoard).
2. `EdicionOrdenes`: añadir `public function booted(): void` con `abort_unless(userCan('acceso', ProgramaModulo::resolve($this->module)->idrol()), 403, 'No tienes acceso a este módulo.')`. En boot() no sirve: ahí `$module` aún vale 'engomado' por defecto. El alias `App\Livewire\Engomado\EdicionOrdenes` lo hereda.
3. `EdicionOrden`: añadir `booted()` igual (acceso) usando `$this->moduloEnum()->idrol()`. Reemplazar `$this->puedeEditar = true;` y su comentario ponytail por `$this->puedeEditar = userCan('modificar', $this->moduloEnum()->idrol());` (el gate de puesto/supervisor sigue apagado; esto es el permiso de módulo). Añadir `private function autorizarEdicion(): void` con `abort_unless(userCan('modificar', $this->moduloEnum()->idrol()), 403, 'No tienes permiso para modificar esta orden.')` y llamarlo como primera línea de: updatedForm, guardarMetrosFila, confirmarMetros, confirmarNoTelas, updatedJulios, guardarJulio (updatedMetrosFila pasa por guardarMetrosFila). Se re-consulta userCan en vez de confiar en `puedeEditar` porque el permiso puede revocarse con la pestaña abierta (userPermissions está memoizado por request, cuesta 0 consultas extra).
4. Tests: en EditarOrdenPermisosTest, EngomadoEdicionOrdenesTest, ProduccionUrdidoCicloJuliosTest y ProduccionUrdidoOrdenesRealesTest usar `Tests\Concerns\SiembraPermisos` y sembrar al usuario de prueba (idusuario 10 donde aplique; si el test crea otro usuario, usar su clave) con `[idrol Urdido => 'Programa Urdido', idrol Engomado => 'Programa Engomado']` usando `ProgramaModulo::Urdido->idrol()` / `Engomado->idrol()` para no duplicar números. En LivewirePermisosModuloTest añadir los tests de <behavior> (sin permisos → `assertForbidden()`; acceso sin modificar → `assertForbidden()` al hacer set y valor intacto en BD). Para los fixtures de tablas reutilizar el setUp de EditarOrdenPermisosTest copiándolo (tablas UrdProgramaUrdido, EngProgramaEngomado, UrdProduccionUrdido, EngProduccionEngomado, UrdJuliosOrden, CatUbicaciones, UrdConsumoHilo, URDCatalogoMaquinas en `sqlsrv` sqlite).
  </action>
  <verify>
    <automated>php artisan test --filter="LivewirePermisosModuloTest|EditarOrdenPermisosTest|EngomadoEdicionOrdenesTest|ProduccionUrdidoCicloJuliosTest|ProduccionUrdidoOrdenesRealesTest"</automated>
  </verify>
  <done>Los 5 filtros pasan; `grep -c "puedeEditar = true" app/Livewire/UrdEng/EdicionOrden.php` = 0; `grep -c "function booted" app/Livewire/UrdEng/EdicionOrden.php app/Livewire/UrdEng/EdicionOrdenes.php` = 1 en cada archivo.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Gate por idrol en NuevoRequerimiento y ConsultarRequerimiento + commit ERP-F0-01</name>
  <files>app/Livewire/InventarioTrama/NuevoRequerimiento.php, app/Livewire/InventarioTrama/ConsultarRequerimiento.php, tests/Feature/LivewirePermisosModuloTest.php, tests/Feature/NuevoRequerimientoLivewireTest.php, tests/Feature/ConsultarRequerimientoLivewireTest.php</files>
  <behavior>
    - Sin permisos: Livewire::test(NuevoRequerimiento) y Livewire::test(ConsultarRequerimiento) → 403.
    - NuevoRequerimiento con acceso=1, crear=0: `call('agregarRequerimiento')` y `call('actualizarCantidad', 0, 0, 5)` → 403, sin filas nuevas en TejTramaConsumos.
    - ConsultarRequerimiento con acceso=1, modificar=0: `call('cambiarStatus', 'Cancelado')` → 403 y el Status no cambia.
    - Los tests existentes de ambos componentes pasan con los 5 permisos sembrados.
  </behavior>
  <action>
Per D de CONTEXT ERP-F0-01:
1. NuevoRequerimiento: `private const IDROL = <idrol Nuevo Requerimiento>; // <nombre SYSRoles>`. `public function booted(): void` con `abort_unless(userCan('acceso', self::IDROL), 403)`. Como primera línea de actualizarCantidad, agregarRequerimiento, eliminarFila y guardar: `abort_unless(userCan('crear', self::IDROL), 403, 'No tienes permiso para capturar requerimientos.')` (discreción: la pantalla es de captura, su permiso natural es `crear`; documentarlo en el SUMMARY). No tocar irATelar, __construct ni listaTelares (fuera de alcance, fila 11 de §2.1).
2. ConsultarRequerimiento: `private const IDROL = <idrol Consultar>;` + `booted()` con acceso; primera línea de cambiarStatus: `abort_unless(userCan('modificar', self::IDROL), 403, ...)`.
3. Tests: NuevoRequerimientoLivewireTest y ConsultarRequerimientoLivewireTest usan SiembraPermisos con el idrol real (leerlo de la constante vía `ReflectionClassConstant` o repetir el número con comentario; preferir repetir el número: es un test). Añadir los casos de <behavior> a LivewirePermisosModuloTest o a cada test existente (donde ya estén los fixtures de tablas; preferir el test existente del componente para no duplicar setUp).
4. Correr la suite completa: si otro test monta estas pantallas y ahora da 403, sembrarle permisos (no relajar el gate).
5. Commit único de ERP-F0-01 con los archivos de Tareas 1-3: `fix(08-01): ERP-F0-01 permisos de modulo en acciones Livewire`.
  </action>
  <verify>
    <automated>php artisan test</automated>
  </verify>
  <done>Suite completa en verde; `grep -c "abort_unless(userCan(" app/Livewire/InventarioTrama/NuevoRequerimiento.php` ≥ 5; commit de ERP-F0-01 creado sin incluir archivos ajenos al plan (verificar con `git show --stat HEAD`).</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 4: IDOR de guardarMetrosFila y rechazo de no numéricos (ERP-F0-02)</name>
  <files>app/Livewire/UrdEng/EdicionOrden.php, tests/Feature/EditarOrdenPermisosTest.php</files>
  <behavior>
    - Fila Id 2 con Folio 'ORD-901' (otra orden): `call('guardarMetrosFila', 2, '50')` sobre la orden ORD-900 → despacha 'program-board-notify' type error; Metros1 de la fila 2 sigue null.
    - Fila Id 1 de ORD-900 con AX=1 → error y sin cambio (regla existente, ahora por la consulta).
    - Fila Id 1 de ORD-900: `call('guardarMetrosFila', 1, 'abc')` → error, Metros1 sin cambio (hoy `(float)'abc'` escribe 0).
    - Fila Id 1 de ORD-900: `call('guardarMetrosFila', 1, '12.5')` → Metros1 = 12.5; `''` → null.
  </behavior>
  <action>
Per D de CONTEXT ERP-F0-02: en `guardarMetrosFila` (:152-180) sustituir `$modelo::find($registroId)` por `$this->produccionEditable($this->orden())->whereKey($registroId)->first()` y borrar la variable `$modelo` si queda sin uso. Mensaje del null: 'No se guardó: esa fila no pertenece a esta orden o ya está en AX.'; el chequeo `AX === 1` posterior queda redundante: borrarlo (la consulta ya excluye AX = 1). Antes de escribir: si `$valor` no es null ni '' y `! is_numeric($valor)`, `avisar('error', 'No se guardó: los metros deben ser un número.')` y return. El resto (Metros2/Metros3, round 2) igual.
Tests: añadir a EditarOrdenPermisosTest los 4 casos de <behavior> insertando filas en UrdProduccionUrdido (ya existe la tabla en el setUp). Usar el patrón existente `assertDispatched('program-board-notify', type: 'error')`.
Commit: `fix(08-01): ERP-F0-02 guardarMetrosFila solo acepta filas del folio abierto`.
  </action>
  <verify>
    <automated>php artisan test --filter=EditarOrdenPermisosTest</automated>
  </verify>
  <done>Tests en verde; `grep -c "::find(\$registroId)" app/Livewire/UrdEng/EdicionOrden.php` = 0; `grep -c "produccionEditable(\$this->orden())->whereKey" app/Livewire/UrdEng/EdicionOrden.php` = 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 5: Quitar SELECT @@VERSION y bloque debug de getOrdenProduccion (ERP-F0-06)</name>
  <files>app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php, tests/Feature/NotificarCortadoRolloOrdenProduccionTest.php</files>
  <behavior>
    - Con ReqProgramaTejido (sqlite) conteniendo NoTelarId '201' EnProceso 1: GET route('notificar.cortado.rollo.orden.produccion', ['no_telar' => '201']) → 200, JSON con 'success' y 'orden', sin clave 'debug'.
    - Sin orden activa: 404 con 'error' y sin clave 'debug'.
    - Sin no_telar: 400.
    - Ninguna consulta ejecutada contiene '@@VERSION' (DB::listen sobre todas las conexiones).
  </behavior>
  <action>
Per D de CONTEXT ERP-F0-06, en `getOrdenProduccion` (:219-265): borrar el bloque try/catch "Probar conexión a TOW_PRO" (`SELECT @@VERSION` contra `sqlsrv_ti`) y la variable `$conexionTowPro`; quitar `'debug' => [...]` de la respuesta 404 y de la 200. Conservar método y rutas (`tejedores.php:52`): `/orden-produccion` puede tener clientes externos (excluido por CONTEXT). No tocar `config/database.php` (`sqlsrv_tow_pro`) ni CLAUDE.md.
Test nuevo con `Tests\Concerns\UsesSqlsrvSqlite` (useSqlsrvSqlite + createProgramaTejidoTable; si la tabla no trae `NoProduccion`, añadir la columna en el test con Schema) y un usuario autenticado (`actingAs`), la ruta no tiene module.permission.
Commit: `fix(08-01): ERP-F0-06 getOrdenProduccion sin probe de version ni debug`.
  </action>
  <verify>
    <automated>php artisan test --filter=NotificarCortadoRolloOrdenProduccionTest</automated>
  </verify>
  <done>Test en verde; `grep -c "@@VERSION\|conexion_tow_pro" app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php` = 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → /livewire/update | El cliente decide qué método público y con qué argumentos se llama; el snapshot va firmado pero los argumentos de acción no |
| navegador → GET /tejedores/cortadoderollo/orden-produccion | Endpoint autenticado que respondía información del servidor SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | Acciones Livewire de páginas con module.permission | mitigate | `Livewire::addPersistentMiddleware([EnsureModulePermission::class])` en AppServiceProvider |
| T-08-02 | Elevation of Privilege | NuevoRequerimiento, ConsultarRequerimiento, EdicionOrden, EdicionOrdenes | mitigate | `booted()` con userCan('acceso', idrol) + abort_unless por acción mutante (crear/modificar), idrol verificado contra SYSUsuariosRoles |
| T-08-03 | Tampering | EdicionOrden::guardarMetrosFila (IDOR) | mitigate | Consulta acotada `produccionEditable(orden)->whereKey($id)`; rechazo de no numéricos; test |
| T-08-04 | Information Disclosure | getOrdenProduccion | mitigate | Eliminar `SELECT @@VERSION` y la clave `debug`; test que la ausencia |
| T-08-05 | Denial of Service | Gate con idrol equivocado deja la pantalla muerta para todos | mitigate | Tarea 1 exige conteo > 0 de usuarios con cada permiso antes de fijar el número |
</threat_model>

<verification>
- `php artisan test` en verde.
- `grep -c "addPersistentMiddleware" app/Providers/AppServiceProvider.php` = 1.
- `grep -c "puedeEditar = true" app/Livewire/UrdEng/EdicionOrden.php` = 0.
- `grep -c "@@VERSION" app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php` = 0.
- 3 commits (ERP-F0-01, ERP-F0-02, ERP-F0-06); `git status` no muestra archivos del usuario incluidos en ellos.
</verification>

<success_criteria>
- Acciones Livewire re-ejecutan `module.permission`; las 4 pantallas sin chequeo propio lo tienen por idrol (ERP-F0-01).
- `guardarMetrosFila` rechaza registros de otro folio y valores no numéricos, con test (ERP-F0-02).
- `getOrdenProduccion` no expone versión del servidor ni debug, con test (ERP-F0-06).
</success_criteria>

<output>
Crear `.planning/phases/08-erp-quick-wins/08-01-SUMMARY.md` al terminar (incluir la tabla idrol/nombre/conteos de la Tarea 1).
</output>
