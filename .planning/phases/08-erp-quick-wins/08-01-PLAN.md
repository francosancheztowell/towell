---
phase: 08-erp-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Providers/AppServiceProvider.php
  - app/Http/Middleware/EnsureModulePermission.php
  - app/Support/Programas/ProgramaModulo.php
  - app/Livewire/UrdEng/EdicionOrden.php
  - app/Livewire/UrdEng/EdicionOrdenes.php
  - app/Livewire/InventarioTrama/NuevoRequerimiento.php
  - app/Livewire/InventarioTrama/ConsultarRequerimiento.php
  - tests/Unit/EnsureModulePermissionTest.php
  - tests/Feature/EditarOrdenPermisosTest.php
  - tests/Feature/EngomadoEdicionOrdenesTest.php
  - tests/Unit/ProduccionUrdidoCicloJuliosTest.php
  - tests/Unit/ProduccionUrdidoOrdenesRealesTest.php
  - tests/Feature/NuevoRequerimientoLivewireTest.php
  - tests/Feature/ConsultarRequerimientoLivewireTest.php
autonomous: true
requirements: [ERP-F0-01, ERP-F0-02]

must_haves:
  truths:
    - "Una acción Livewire enviada a /livewire/update vuelve a pasar por EnsureModulePermission con los parámetros de la ruta donde se montó la página, y sin permiso termina en 403 (no en una respuesta JSON que Livewire ignora)"
    - "Un usuario sin 'acceso' al módulo (por idrol) no puede montar ni hidratar NuevoRequerimiento, ConsultarRequerimiento, EdicionOrden ni EdicionOrdenes (403)"
    - "Un usuario con 'acceso' pero sin 'modificar'/'crear' no puede guardar nada desde esas pantallas (403) y la BD no cambia"
    - "Ningún usuario que hoy entra por el menú (reimpresión/editar órdenes/requerimientos) pierde el acceso por el idrol elegido"
    - "guardarMetrosFila solo escribe filas de producción del folio abierto y no cerradas en AX; un Id de otro folio o un valor no numérico no cambia la BD"
  artifacts:
    - path: "app/Providers/AppServiceProvider.php"
      provides: "Registro de EnsureModulePermission como middleware persistente de Livewire"
      contains: "addPersistentMiddleware"
    - path: "app/Http/Middleware/EnsureModulePermission.php"
      provides: "abort(403) cuando la petición trae X-Livewire (aunque pida JSON)"
      contains: "X-Livewire"
    - path: "app/Support/Programas/ProgramaModulo.php"
      provides: "idrol verificado de Programa Urdido / Programa Engomado"
      contains: "function idrol"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "App\\Http\\Middleware\\EnsureModulePermission"
      via: "Livewire::addPersistentMiddleware"
      pattern: "addPersistentMiddleware\\(\\[EnsureModulePermission::class\\]\\)"
    - from: "app/Livewire/UrdEng/EdicionOrden.php"
      to: "produccionEditable()"
      via: "guardarMetrosFila filtra por folio y AX"
      pattern: "produccionEditable\\(\\$this->orden\\(\\)\\)->whereKey"
    - from: "app/Livewire/UrdEng/EdicionOrden.php"
      to: "userCan('modificar', idrol)"
      via: "autorizarEdicion()"
      pattern: "userCan\\('modificar'"
---

<objective>
Cerrar los dos huecos de seguridad Livewire de la Fase 0 (auditoría §2.5): acciones sin re-chequeo de permisos y el IDOR de `EdicionOrden::guardarMetrosFila`. (ERP-F0-06, el probe `@@VERSION`, va en 08-06.)

Purpose: hoy `module.permission` solo protege la carga de la página; un POST a `/livewire/update` se salta el permiso, y 4 pantallas no tienen chequeo propio (`EdicionOrden` fija `puedeEditar = true`). Además, `Livewire\Drawer\Utils::applyMiddleware` (vendor/livewire/livewire/src/Drawer/Utils.php:183-196) solo respeta redirects y aborts: si EnsureModulePermission devuelve un JSON 403 (rama `expectsJson()`), la acción se ejecuta igual. `guardarMetrosFila` acepta cualquier Id de producción que mande el cliente.
Output: middleware persistente registrado y que aborta en peticiones Livewire, gates por idrol en 4 componentes, IDOR corregido; cada cambio con su test en el mismo commit.

Commits: uno por requisito. Tareas 1-3 = ERP-F0-01 (un commit al cerrar la Tarea 3); Tarea 4 = ERP-F0-02. Mensajes `fix(08-01): ERP-F0-0N <resumen>`.
Tests: cada tarea corre solo su `--filter` (la suite completa tarda más de 10 min y no hay paratest). La suite completa corre UNA vez, al final de la Tarea 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-erp-quick-wins/08-CONTEXT.md
@.planning/auditoria_erp_2026-09.md
@CLAUDE.md
@tests/Feature/EditarOrdenPermisosTest.php
@tests/Unit/EnsureModulePermissionTest.php
@tests/Concerns/SiembraPermisos.php

<interfaces>
Extraído del código (2026-09-22). No hace falta explorar más.

app/Http/Middleware/EnsureModulePermission.php (alias `module.permission`):
  handle(Request $request, Closure $next, string $action, string $module): Response
  — si userCan($action, $module) → $next; si no: `if ($request->expectsJson()) return response()->json([...], 403);` y si no `abort(403, $message)`.

Livewire v4.3.3:
  PersistentMiddleware: `Livewire::addPersistentMiddleware(array)`; en /livewire/update arma una petición falsa con la ruta ORIGINAL (clona headers de la petición real, así que conserva `X-Livewire`) y reaplica los middleware cuya clase coincide (`Str::before($value, ':')`). Getter: `app(\Livewire\Mechanisms\PersistentMiddleware\PersistentMiddleware::class)->getPersistentMiddleware()`. NO se aplica en `Livewire::test()`.
  Utils::applyMiddleware: corre el pipeline y solo hace `abort($response)` si la respuesta es RedirectResponse; cualquier otra respuesta (JSON 403 incluido) se descarta → la acción corre. Por eso el middleware debe ABORTAR cuando `$request->hasHeader('X-Livewire')` (el JS de Livewire siempre lo manda; `HandleRequests::isLivewireRequest()` usa el mismo header).
  Ciclo de vida: mount → boot, mount, booted; update → boot, hydrate, booted. `booted()` corre en cada request con las propiedades ya cargadas.

app/Helpers/permission-helpers.php:
  userCan(string $action, string|int $module): bool — numérico = idrol; string = nombre (userPermissions indexa `SYSRoles` por `mb_strtolower(modulo)`: con nombres repetidos gana una fila arbitraria).
  Memoización en contenedor: `permisos.roles` (keyBy nombre minúsculas → {idrol, modulo}), `permisos.usuario.{idusuario}` (keyBy idrol → {acceso, crear, modificar, eliminar, registrar}).

tests/Concerns/SiembraPermisos.php: `sembrarPermisos(int $idusuario, array [idrol => 'Nombre'])` siembra los 5 permisos en 1. Para "acceso sí, modificar no" sembrar a mano `app()->instance('permisos.usuario.'.$id, collect([idrol => (object)[...]]))` y `permisos.roles`.
tests/Unit/EnsureModulePermissionTest.php: patrón `(new EnsureModulePermission)->handle(Request::create(...), fn, 'crear', 'Programa Tejido')` con helper `usuarioConPermiso(nombre, idrol, [permisos])`.

app/Support/Programas/ProgramaModulo.php (enum Urdido|Engomado): resolve(string), permissionModule(): 'Programa Urdido' | 'Programa Engomado' (lo usa ProgramBoard.php:390, :500-502 con userCan por NOMBRE), programModel(), productionModel().

app/Livewire/UrdEng/EdicionOrden.php:
  boot(BomMaterialesService) — abort_unless(Auth::check()); mount(string $module, int $ordenId, bool $fromReimpresion=false) — :125 `$this->puedeEditar = true;` (comentario "ponytail: gate de puesto/supervisor apagado")
  updatedForm, guardarMetrosFila(int $registroId, mixed $valor) (:152-180, `$modelo::find($registroId)`), confirmarMetros(string), confirmarNoTelas(), updatedJulios, updatedMetrosFila, guardarJulio(int)
  private orden(): Model; private produccionEditable(Model $orden): Builder (:622 — Folio = orden.Folio y AX null o != 1); private avisar(tipo, msg) → despacha 'program-board-notify'.
app/Livewire/UrdEng/EdicionOrdenes.php: boot() abort_unless(Auth::check()); mount(string $module='engomado'); acciones de lectura + editar() (redirect) + calificar() (dispatch). `App\Livewire\Engomado\EdicionOrdenes` es alias vacío.
  Se llega a EdicionOrdenes desde las filas de menú de reimpresión (`/urdido/reimpresion-urdido`, `/engomado/reimpresion-engomado`) y a EdicionOrden desde `editar-ordenes-programadas` — NO desde la fila "Programa Urdido/Engomado".
app/Livewire/InventarioTrama/NuevoRequerimiento.php: mutaciones actualizarCantidad(int,int,float), agregarRequerimiento(), eliminarFila(int,int), guardar(). (irATelar/__construct/listaTelares fuera de alcance, §2.1 fila 11.)
app/Livewire/InventarioTrama/ConsultarRequerimiento.php: cambiarStatus(string, RequerimientoStatusService) muta; editar() redirige.

Los 6 tests que montan estos componentes (verificado con grep; son los ÚNICOS a sembrar): EditarOrdenPermisosTest, EngomadoEdicionOrdenesTest (incluye GET /engomado/reimpresion-engomado), tests/Unit/ProduccionUrdidoCicloJuliosTest, tests/Unit/ProduccionUrdidoOrdenesRealesTest, NuevoRequerimientoLivewireTest, ConsultarRequerimientoLivewireTest.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Resolver idrol sin dejar a nadie fuera + middleware persistente que aborta en Livewire (ERP-F0-01, 1/3)</name>
  <files>app/Providers/AppServiceProvider.php, app/Http/Middleware/EnsureModulePermission.php, tests/Unit/EnsureModulePermissionTest.php</files>
  <behavior>
    - Petición con headers `X-Livewire: 1` y `Accept: application/json`, usuario sin el permiso → `handle()` lanza HttpException con status 403 (no devuelve JsonResponse).
    - Petición JSON sin X-Livewire, sin permiso → sigue devolviendo JsonResponse 403 (test existente intacto).
    - `getPersistentMiddleware()` de Livewire contiene `EnsureModulePermission::class`.
  </behavior>
  <action>
Paso A — idrol (solo lectura; decisión de CONTEXT "resolver módulo por idrol, no por nombre"). Con `php artisan tinker --execute` contra `sqlsrv` (ProdTowel):
(a) Listar `idrol, modulo, Ruta, Dependencia` de SYSRoles donde `Ruta` o `modulo` contenga `requerimiento`, `Programa Urdido`, `Programa Engomado`, `reimpresion` o `editar-ordenes`.
(b) Elegir 4 idrol destino: Nuevo Requerimiento, Consultar Requerimiento (la fila cuya Ruta es la URL del menú; si es una sola fila, la misma para ambas), Programa Urdido y Programa Engomado.
(c) Chequeo de nombre: `SELECT idrol FROM SYSRoles WHERE LOWER(modulo) IN ('programa urdido','programa engomado')` debe dar exactamente una fila por nombre y coincidir con el idrol elegido (así `userCan(..., 'Programa Urdido')` de ProgramBoard y el nuevo gate por idrol miran la misma fila).
(d) Chequeo de bloqueo: para cada fila de menú que realmente usan (reimpresión urdido/engomado, editar órdenes urdido/engomado si tienen fila, nuevo/consultar requerimiento), contar usuarios con `acceso=1` en esa fila de menú y sin `acceso=1` en el idrol destino → debe ser 0. Para EdicionOrden además: usuarios con `acceso=1` en la fila de editar órdenes/reimpresión y sin `modificar=1` en Programa Urdido/Engomado → debe ser 0. Para Nuevo Requerimiento: usuarios con acceso y sin `crear`; para Consultar: usuarios con acceso y sin `modificar` → reportar el número.
(e) Si (c) o (d) no dan 0, o algún idrol destino tiene 0 usuarios con `acceso`: DETENERSE y devolver checkpoint con la tabla, sin fijar números. Si 192.168.2.28 no responde, es un auth gate dinámico: devolver checkpoint pidiendo al usuario correr las consultas y pegar el resultado; no inventar números. Anotar todo en el SUMMARY.

Paso B — EnsureModulePermission: la rama JSON solo aplica si la petición NO trae `X-Livewire`: `if ($request->expectsJson() && ! $request->hasHeader('X-Livewire'))` → JSON 403; en cualquier otro caso `abort(403, $message)`. Comentario de una línea: Livewire descarta las respuestas no-redirect de los middleware persistentes (Utils::applyMiddleware), solo un abort detiene la acción.

Paso C — AppServiceProvider::boot(), junto a `Livewire::setUpdateRoute(...)`: `Livewire::addPersistentMiddleware([EnsureModulePermission::class]);` con su `use`.

Paso D — tests en tests/Unit/EnsureModulePermissionTest.php (RED antes de B/C): los 3 casos de <behavior>. No commitear todavía.
  </action>
  <verify>
    <automated>php artisan test --filter=EnsureModulePermissionTest</automated>
  </verify>
  <done>Tabla idrol/nombre/conteos con los chequeos (c) y (d) en 0 registrada para el SUMMARY; `grep -q "addPersistentMiddleware" app/Providers/AppServiceProvider.php`; `grep -q "X-Livewire" app/Http/Middleware/EnsureModulePermission.php`; test en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Gate por idrol en EdicionOrden y EdicionOrdenes (ERP-F0-01, 2/3)</name>
  <files>app/Support/Programas/ProgramaModulo.php, app/Livewire/UrdEng/EdicionOrden.php, app/Livewire/UrdEng/EdicionOrdenes.php, tests/Feature/EditarOrdenPermisosTest.php, tests/Feature/EngomadoEdicionOrdenesTest.php, tests/Unit/ProduccionUrdidoCicloJuliosTest.php, tests/Unit/ProduccionUrdidoOrdenesRealesTest.php</files>
  <behavior>
    - EditarOrdenPermisosTest (casos nuevos en el archivo existente, con su setUp): sin permisos → Livewire::test(EdicionOrden, urdido) `assertForbidden()`; acceso=1 y modificar=0 → monta, `set('form.NoTelarId','999')` → `assertForbidden()` y la BD conserva '101'.
    - EngomadoEdicionOrdenesTest (caso nuevo): sin permisos → Livewire::test(EdicionOrdenes) `assertForbidden()`.
    - Con los 5 permisos sembrados, todos los tests existentes de los 4 archivos siguen pasando.
  </behavior>
  <action>
Per CONTEXT ERP-F0-01 (idrol, no nombre):
1. ProgramaModulo: `public function idrol(): int` con `match` Urdido → idrol de Programa Urdido, Engomado → idrol de Programa Engomado (de la Tarea 1), con comentario del nombre. No tocar `permissionModule()`.
2. EdicionOrdenes: `public function booted(): void` con `abort_unless(userCan('acceso', ProgramaModulo::resolve($this->module)->idrol()), 403, 'No tienes acceso a este módulo.')` (en boot() `$module` aún vale 'engomado').
3. EdicionOrden: `booted()` igual con `$this->moduloEnum()->idrol()`. Reemplazar `$this->puedeEditar = true;` y su comentario ponytail por `$this->puedeEditar = userCan('modificar', $this->moduloEnum()->idrol());` (el gate de puesto sigue apagado; esto es el permiso de módulo). `private function autorizarEdicion(): void` con `abort_unless(userCan('modificar', $this->moduloEnum()->idrol()), 403, 'No tienes permiso para modificar esta orden.')`, llamado como primera línea de updatedForm, guardarMetrosFila, confirmarMetros, confirmarNoTelas, updatedJulios y guardarJulio (se re-consulta en vez de confiar en `puedeEditar`: el permiso puede revocarse con la pestaña abierta; userPermissions está memoizado por request).
4. Tests: en los 4 archivos de esta tarea, `use SiembraPermisos` y sembrar al usuario de prueba (idusuario 10 o la clave del usuario que cree cada test) con `[ProgramaModulo::Urdido->idrol() => 'Programa Urdido', ProgramaModulo::Engomado->idrol() => 'Programa Engomado']`. Los casos negativos van en EditarOrdenPermisosTest y EngomadoEdicionOrdenesTest existentes (reutilizan su setUp; no crear archivo nuevo).
  </action>
  <verify>
    <automated>php artisan test --filter="EditarOrdenPermisosTest|EngomadoEdicionOrdenesTest|ProduccionUrdidoCicloJuliosTest|ProduccionUrdidoOrdenesRealesTest"</automated>
  </verify>
  <done>Los 4 filtros en verde; `! grep -q "puedeEditar = true" app/Livewire/UrdEng/EdicionOrden.php`; `grep -q "function booted" app/Livewire/UrdEng/EdicionOrdenes.php`.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Gate por idrol en NuevoRequerimiento y ConsultarRequerimiento + commit ERP-F0-01 (3/3)</name>
  <files>app/Livewire/InventarioTrama/NuevoRequerimiento.php, app/Livewire/InventarioTrama/ConsultarRequerimiento.php, tests/Feature/NuevoRequerimientoLivewireTest.php, tests/Feature/ConsultarRequerimientoLivewireTest.php</files>
  <behavior>
    - Sin permisos: Livewire::test(NuevoRequerimiento) y Livewire::test(ConsultarRequerimiento) → `assertForbidden()`.
    - NuevoRequerimiento con acceso=1, crear=0: `call('agregarRequerimiento')` → 403 y sin filas nuevas en TejTramaConsumos.
    - ConsultarRequerimiento con acceso=1, modificar=0: `call('cambiarStatus', 'Cancelado')` → 403 y el Status no cambia.
    - Con los 5 permisos sembrados, los tests existentes de ambos componentes pasan.
  </behavior>
  <action>
Per CONTEXT ERP-F0-01:
1. NuevoRequerimiento: `private const IDROL = <idrol>; // <nombre SYSRoles>`, `booted()` con `abort_unless(userCan('acceso', self::IDROL), 403)`, y como primera línea de actualizarCantidad, agregarRequerimiento, eliminarFila y guardar: `abort_unless(userCan('crear', self::IDROL), 403, 'No tienes permiso para capturar requerimientos.')` (discreción: pantalla de captura → `crear`; anotarlo en el SUMMARY). No tocar irATelar, __construct ni listaTelares.
2. ConsultarRequerimiento: `private const IDROL`, `booted()` con acceso, primera línea de cambiarStatus con `modificar`.
3. Tests: SiembraPermisos en los dos archivos existentes (idrol repetido como número con comentario) y los casos negativos de <behavior> en esos mismos archivos.
4. Solo se siembran los 6 fixtures listados en interfaces. Si al final (Tarea 4) otro test falla, se reporta en el SUMMARY; no se le siembran permisos para taparlo.
5. Commit único de ERP-F0-01 con los archivos de las Tareas 1-3: `fix(08-01): ERP-F0-01 permisos de modulo en acciones Livewire`.
  </action>
  <verify>
    <automated>php artisan test --filter="NuevoRequerimientoLivewireTest|ConsultarRequerimientoLivewireTest|EnsureModulePermissionTest|EditarOrdenPermisosTest|EngomadoEdicionOrdenesTest|ProduccionUrdidoCicloJuliosTest|ProduccionUrdidoOrdenesRealesTest"</automated>
  </verify>
  <done>Filtro en verde; `git show --stat HEAD` del commit ERP-F0-01 lista solo archivos de files_modified de las Tareas 1-3.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 4: IDOR de guardarMetrosFila y rechazo de no numéricos (ERP-F0-02) + suite completa</name>
  <files>app/Livewire/UrdEng/EdicionOrden.php, tests/Feature/EditarOrdenPermisosTest.php</files>
  <behavior>
    - Fila Id 2 con Folio 'ORD-901': `call('guardarMetrosFila', 2, '50')` sobre la orden ORD-900 → despacha 'program-board-notify' type error; Metros1 de la fila 2 sigue null.
    - Fila Id 1 de ORD-900 con AX=1 → error y sin cambio.
    - Fila Id 1 de ORD-900: `call('guardarMetrosFila', 1, 'abc')` → error, Metros1 sin cambio (hoy `(float)'abc'` escribe 0).
    - Fila Id 1 de ORD-900: `'12.5'` → Metros1 = 12.5; `''` → null.
  </behavior>
  <action>
Per CONTEXT ERP-F0-02: en guardarMetrosFila (:152-180) sustituir `$modelo::find($registroId)` por `$this->produccionEditable($this->orden())->whereKey($registroId)->first()` (borrar `$modelo` si queda sin uso). Mensaje del null: 'No se guardó: esa fila no pertenece a esta orden o ya está en AX.'; borrar el chequeo `AX === 1` posterior (la consulta ya lo excluye). Antes de escribir: si `$valor` no es null ni '' y `! is_numeric($valor)`, `avisar('error', 'No se guardó: los metros deben ser un número.')` y return. El resto igual.
Tests: los 4 casos en EditarOrdenPermisosTest (filas en UrdProduccionUrdido del setUp existente; `assertDispatched('program-board-notify', type: 'error')`).
Commit `fix(08-01): ERP-F0-02 guardarMetrosFila solo acepta filas del folio abierto`.
Cierre del plan: correr `php artisan test` completo UNA vez. Fallos ajenos a este plan se anotan en el SUMMARY sin tocarlos.
  </action>
  <verify>
    <automated>php artisan test --filter=EditarOrdenPermisosTest && ! grep -q '::find($registroId)' app/Livewire/UrdEng/EdicionOrden.php</automated>
  </verify>
  <done>EditarOrdenPermisosTest en verde; `grep -q 'produccionEditable($this->orden())->whereKey' app/Livewire/UrdEng/EdicionOrden.php`; suite completa ejecutada una vez y su resultado en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → /livewire/update | El cliente decide qué método público y con qué argumentos se llama; el snapshot va firmado pero los argumentos de acción no |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | Acciones Livewire de páginas con module.permission | mitigate | `addPersistentMiddleware([EnsureModulePermission::class])` + abort(403) cuando hay `X-Livewire` (Livewire ignora respuestas JSON de middleware); test unitario |
| T-08-02 | Elevation of Privilege | NuevoRequerimiento, ConsultarRequerimiento, EdicionOrden, EdicionOrdenes | mitigate | `booted()` con userCan('acceso', idrol) + abort_unless por acción mutante (crear/modificar) |
| T-08-03 | Tampering | EdicionOrden::guardarMetrosFila (IDOR) | mitigate | Consulta acotada `produccionEditable(orden)->whereKey($id)`; rechazo de no numéricos; test |
| T-08-05 | Denial of Service | Gate con idrol equivocado deja fuera a usuarios que hoy entran por el menú | mitigate | Tarea 1 (c)/(d): nombre único = idrol elegido y 0 usuarios bloqueados; si no, checkpoint |
</threat_model>

<verification>
- Filtros de cada tarea en verde y la suite completa ejecutada una vez al final (Tarea 4).
- `grep -q "addPersistentMiddleware" app/Providers/AppServiceProvider.php` y `grep -q "X-Livewire" app/Http/Middleware/EnsureModulePermission.php`.
- `! grep -q "puedeEditar = true" app/Livewire/UrdEng/EdicionOrden.php`.
- 2 commits (ERP-F0-01, ERP-F0-02) sin archivos del usuario.
</verification>

<success_criteria>
- Acciones Livewire re-ejecutan `module.permission` y abortan sin permiso; las 4 pantallas sin chequeo propio lo tienen por idrol sin bloquear a usuarios actuales (ERP-F0-01).
- `guardarMetrosFila` rechaza registros de otro folio y valores no numéricos, con test (ERP-F0-02).
</success_criteria>

<output>
Crear `.planning/phases/08-erp-quick-wins/08-01-SUMMARY.md` al terminar (incluir la tabla idrol/nombre/conteos y los chequeos de la Tarea 1).
</output>
