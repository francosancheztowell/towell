---
phase: 08-erp-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/catalogosurdido/catalago-maquinas.blade.php
  - resources/views/components/telares/telar-requerimiento.blade.php
  - resources/views/modulos/catalogos-atadores/actividades/index.blade.php
  - resources/views/modulos/catalogos-atadores/comentarios/index.blade.php
  - resources/views/modulos/catalogos-atadores/maquinas/index.blade.php
  - resources/views/modulos/engomado/BPM-Engomado/index.blade.php
  - resources/views/modulos/engomado/captura-formula/index.blade.php
  - resources/views/modulos/engomado/eng-actividades-bpm/index.blade.php
  - resources/views/modulos/engomado/modulo-produccion-engomado.blade.php
  - resources/views/modulos/engomado/programar-engomado.blade.php
  - resources/views/modulos/engomado/reporte-resumen-engomado.blade.php
  - resources/views/modulos/gestion-modulos/index.blade.php
  - resources/views/modulos/urdido/BPM-Urdido/index.blade.php
  - resources/views/modulos/urdido/produccion/_modal-oficial.blade.php
  - resources/views/modulos/urdido/programar-urdido.blade.php
  - resources/views/modulos/urdido/reportes-resumen-urdido.blade.php
  - resources/views/modulos/usuarios/select.blade.php
  - resources/js/app.js
  - resources/views/modulos/urdido/Urdido-BPM-Line/index.blade.php
  - resources/views/modulos/atadores/programaAtadores/index.blade.php
  - app/Livewire/Mecanicos/VerificaMaquina/Index.php
  - app/Livewire/Concerns/ConTabla.php
  - tests/Unit/VistasTailwindV4Test.php
  - tests/Feature/VerificaMaquinaIndexPaginacionTest.php
autonomous: true
requirements: [ERP-F0-04, ERP-F0-05, ERP-F0-09, ERP-F0-10]

must_haves:
  truths:
    - "Los fondos de modal que usaban bg-opacity-* se ven translúcidos (clase bg-X/NN generada por Tailwind v4), no opacos"
    - "El modal de reporte-resumen-engomado tiene ancho máximo (max-w-md)"
    - "Cada página descarga el CSS de Tailwind una sola vez (solo vía @vite de x-layout-styles)"
    - "Marcar una actividad en Urdido-BPM-Line con la sesión rotada ya no da 419 silencioso: usa http.post y avisa con notify"
    - "Programa Atadores no sondea con la pestaña oculta y sondea cada 15 s"
    - "VerificaMaquina página 2 funciona en SQL Server 2008 R2 (sin OFFSET/FETCH)"
  artifacts:
    - path: "tests/Unit/VistasTailwindV4Test.php"
      provides: "Guarda: 0 bg-opacity-* y 0 max-md en vistas; app.js no importa app.css"
    - path: "tests/Feature/VerificaMaquinaIndexPaginacionTest.php"
      provides: "Página 2 de VerificaMaquina sin OFFSET"
  key_links:
    - from: "resources/views/modulos/urdido/Urdido-BPM-Line/index.blade.php"
      to: "window.http"
      via: "http.post(route('urd-bpm-line.toggle'))"
      pattern: "http\\.post\\(\"\\{\\{ route\\('urd-bpm-line\\.toggle'"
    - from: "app/Livewire/Mecanicos/VerificaMaquina/Index.php"
      to: "App\\Support\\PaginacionCompat"
      via: "PaginacionCompat::paginar"
      pattern: "PaginacionCompat::paginar"
---

<objective>
Bugs de front y perf rápida de la Fase 0 (auditoría §2.4, §2.6, §2.5): fondos de modal opacos por `bg-opacity-*` (Tailwind v3), Tailwind cargado dos veces, 419 en Urdido-BPM-Line, polling de Atadores con pestaña oculta y `paginate()` incompatible con SQL Server 2008 R2 en VerificaMaquina.

Purpose: arreglos de 1-3 líneas con alto impacto visual/rendimiento, sin abstracciones nuevas.
Output: 17 vistas corregidas, app.js sin import doble, BPM-Line Urdido igual que Engomado, polling pausado, paginación compatible; guardas en tests.

Commits: uno por requisito (Tareas 1-4 = ERP-F0-04, 05, 09, 10). Mensajes `fix(08-02): ERP-F0-0N <resumen>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-erp-quick-wins/08-CONTEXT.md
@.planning/auditoria_erp_2026-09.md
@CLAUDE.md

<interfaces>
Inventario verificado 2026-09-22 (`grep -rn "bg-opacity-" resources/`): 32 ocurrencias en los 17 archivos de files_modified (salvo app.js, BPM-Line, programaAtadores, VerificaMaquina, ConTabla y tests). Combinaciones: `bg-black bg-opacity-50` x15, `bg-gray-900 bg-opacity-50` x9, `bg-gray-600 bg-opacity-50` x5, `bg-gray-900 bg-opacity-10` x1 (BPM-Engomado:183), `bg-gray-400 bg-opacity-10` x1 (gestion-modulos:98), y `bg-black/40 bg-opacity-60` x1 (telar-requerimiento:118: hoy renderiza /40, conservar /40). Ninguna con prefijo de variante (hover:, md:).
`resources/views/modulos/engomado/reporte-resumen-engomado.blade.php:92`: `max-md` (inválida) → `max-w-md` (la gemela de Urdido :92 ya usa max-w-md).

resources/css/app.css: `@import 'tailwindcss'` con `@source '../**/*.blade.php'` (Tailwind v4 escanea las vistas; `bg-black/50` se genera).
resources/js/app.js:2: `import '../css/app.css';` — app.js solo lo carga `components/layout-scripts.blade.php`, que solo usa `layouts/app.blade.php`, que también incluye `<x-layout-styles />` (`@vite(['resources/css/app.css'])`). Verificado: ninguna vista carga app.js sin app.css.

Engomado-BPM-Line/index.blade.php:~207 (patrón a copiar): `http.post("{{ route('eng-bpm-line.toggle', $header->Folio) }}", { actividad, valor: valorNuevo }).then(data => { if (!data.success) { notify.error(...); return; } ...actualizar UI... }).catch(err => { if (err.status === 419) { notify.error('Tu sesion expiro. Recarga la pagina para seguir capturando.'); return; } notify.error(err.message); });` con el comentario "ponytail: http (axios) manda el XSRF de la cookie...".
Urdido-BPM-Line/index.blade.php:~208: `fetch("{{ route('urd-bpm-line.toggle', $header->Folio) }}", { method:'POST', headers: {X-CSRF-TOKEN impreso} ...}).then(r=>r.json()).then(...Swal.fire error...)`.

programaAtadores/index.blade.php:~734 `setInterval(refreshStatus, 5000);` y `async function refreshStatus() { if (refrescoEnVuelo) return; ... }`. ARCHIVO CON CAMBIOS SIN COMMIT DEL USUARIO (git status M, +115/-270): editar encima, nunca `git checkout`/`restore`/`stash`.

app/Livewire/Mecanicos/VerificaMaquina/Index.php:103-117: `MecVerificaMaquinaModel::query()->when(estatus)->orderByDesc('Fecha')->orderByDesc('HoraInicio')->orderByDesc('Folio')->paginate(15, [8 columnas])`; usa `WithPagination`; mount() exige userCan('acceso', 'Estado Maquina'). Modelo: conexión `sqlsrv`, tabla `MecVerificaMaquinaTable`, PK `Folio`.
app/Support/PaginacionCompat.php: `PaginacionCompat::paginar(EloquentBuilder|QueryBuilder $query, int $porPagina = 25, ?int $pagina = null, string $nombrePagina = 'page'): LengthAwarePaginator` (TOP + recorte en PHP).
app/Livewire/Concerns/ConTabla.php:23 docblock de ejemplo usa `->paginate($this->porPagina)`; el trait ya expone `protected function paginar(Builder $query): LengthAwarePaginator` (:118).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: bg-opacity-* → sintaxis /NN y max-md → max-w-md (ERP-F0-04)</name>
  <files>los 17 archivos resources/views/... de files_modified, tests/Unit/VistasTailwindV4Test.php</files>
  <behavior>
    - Test `test_ninguna_vista_usa_bg_opacity`: recorre `resource_path('views')` (*.blade.php) y falla listando archivo:línea si alguna contiene `bg-opacity-`.
    - Test `test_ninguna_vista_usa_max_md_suelto`: falla si alguna vista contiene la clase `max-md` como token completo (regex con límites de clase, que no confunda `max-md:` variante ni `max-w-md`).
  </behavior>
  <action>
Per D de CONTEXT ERP-F0-04. Crear primero el test (RED). Luego reemplazo mecánico con perl sobre exactamente los 17 archivos: (1) `bg-<color> bg-opacity-<NN>` → `bg-<color>/<NN>` (regex `\b(bg-[a-z]+(?:-\d+)?) bg-opacity-(\d+)` → `$1/$2`); (2) quitar cualquier ` bg-opacity-<NN>` restante (solo el caso `bg-black/40 bg-opacity-60`, que hoy renderiza /40: se conserva /40, cero cambio visual ahí). (3) En reporte-resumen-engomado.blade.php:92 `max-md` → `max-w-md`. Incluir programar-urdido.blade.php y programar-engomado.blade.php aunque sean el tablero clásico huérfano (su borrado es Fase 1, fuera de alcance; el requisito pide 0 en vistas). No tocar otras clases ni reformatear.
Tras el reemplazo: `npm run build` y confirmar que el CSS generado contiene las clases nuevas.
Commit `fix(08-02): ERP-F0-04 fondos de modal con sintaxis Tailwind v4`.
  </action>
  <verify>
    <automated>php artisan test --filter=VistasTailwindV4Test && npm run build && grep -rc "bg-opacity-" resources/views | grep -v ":0$" | wc -l</automated>
  </verify>
  <done>Test en verde; el último conteo = 0; `grep -l "bg-black\\\\/50" public/build/assets/*.css` encuentra al menos un archivo; `git diff --stat` del commit toca exactamente 17 vistas + el test.</done>
</task>

<task type="auto">
  <name>Tarea 2: Tailwind una sola vez por página (ERP-F0-05)</name>
  <files>resources/js/app.js, tests/Unit/VistasTailwindV4Test.php</files>
  <action>
Per D de CONTEXT ERP-F0-05: borrar la línea `import '../css/app.css';` de resources/js/app.js:2 (nada más). Añadir a VistasTailwindV4Test `test_app_js_no_importa_app_css` que lee resources/js/app.js y afirma que no contiene `css/app.css`. `npm run build` debe quedar verde y el manifest seguir teniendo la entrada `resources/css/app.css` (la carga x-layout-styles). Anotar en el SUMMARY el tamaño del bundle JS de app antes/después (salida de vite build).
Commit `perf(08-02): ERP-F0-05 Tailwind cargado una vez por pagina`.
  </action>
  <verify>
    <automated>php artisan test --filter=VistasTailwindV4Test && npm run build && grep -c "resources/css/app.css" public/build/manifest.json</automated>
  </verify>
  <done>`grep -c "css/app.css" resources/js/app.js` = 0; build verde; manifest conserva la entrada de app.css (conteo ≥ 1).</done>
</task>

<task type="auto">
  <name>Tarea 3: Urdido-BPM-Line con http.post (ERP-F0-09)</name>
  <files>resources/views/modulos/urdido/Urdido-BPM-Line/index.blade.php</files>
  <action>
Per D de CONTEXT ERP-F0-09: en `toggleActividad` sustituir el `fetch(...route('urd-bpm-line.toggle'...)...).then(r => r.json()).then(...)` y su manejo de error por la misma forma que Engomado-BPM-Line (ver interfaces): `http.post("{{ route('urd-bpm-line.toggle', $header->Folio) }}", { actividad, valor: valorNuevo })`, `.then(data => { if (!data.success) { notify.error(data.message || 'Error al actualizar la actividad'); return; } ...misma actualización de clases e iconos que hoy... })`, `.catch(err => { 419 → notify.error('Tu sesion expiro...'); resto → notify.error(err.message) })`. Copiar el comentario ponytail de Engomado. Solo esta función; no tocar el resto de la vista (la unificación de vistas gemelas es Fase 2). El endpoint no cambia, así que no hay método de edición tocado que requiera test nuevo; la guarda es el grep.
Commit `fix(08-02): ERP-F0-09 Urdido-BPM-Line usa http.post (419)`.
  </action>
  <verify>
    <automated>grep -c "http.post(\"{{ route('urd-bpm-line.toggle'" resources/views/modulos/urdido/Urdido-BPM-Line/index.blade.php && grep -c "X-CSRF-TOKEN" resources/views/modulos/urdido/Urdido-BPM-Line/index.blade.php</automated>
  </verify>
  <done>Primer conteo = 1; el `fetch(` del toggle desaparece (`grep -c "fetch(\"{{ route('urd-bpm-line.toggle'"` = 0); segundo conteo = 0 salvo que otra función de la vista lo use (si queda, verificar que no es del toggle).</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 4: Polling de Atadores con document.hidden y VerificaMaquina con PaginacionCompat (ERP-F0-10)</name>
  <files>resources/views/modulos/atadores/programaAtadores/index.blade.php, app/Livewire/Mecanicos/VerificaMaquina/Index.php, app/Livewire/Concerns/ConTabla.php, tests/Feature/VerificaMaquinaIndexPaginacionTest.php</files>
  <behavior>
    - Con 20 filas en MecVerificaMaquinaTable (sqlite `sqlsrv`) y permisos sembrados para 'Estado Maquina': Livewire::test(Index)->call('gotoPage', 2) → `viewData('verificaciones')` tiene 5 elementos y total 20.
    - Ninguna consulta registrada con DB::listen durante el render contiene `offset` (case-insensitive).
  </behavior>
  <action>
Per D de CONTEXT ERP-F0-10:
1. programaAtadores/index.blade.php (cambios sin commit del usuario: editar encima con Edit, nunca revertir): `setInterval(refreshStatus, 5000)` → `setInterval(refreshStatus, 15000)` y primera línea de refreshStatus `if (document.hidden || refrescoEnVuelo) return;` (sustituye al `if (refrescoEnVuelo) return;`). Nada más en ese archivo. NO hacer `git add` de este archivo: su diff mezcla el trabajo pendiente del usuario; dejar el cambio en el working tree y anotarlo en el SUMMARY ("ERP-F0-10 polling: 2 líneas sin commit, viajan con el WIP del usuario en programaAtadores/index.blade.php").
2. VerificaMaquina/Index.php render(): reemplazar `->paginate(15, [cols])` por `PaginacionCompat::paginar($query->select([las mismas 8 columnas]), 15, $this->getPage())` (import `App\Support\PaginacionCompat`). Mismo orden y filtros.
3. ConTabla.php:23 docblock: cambiar el ejemplo a `$this->paginar($this->aplicarTabla(Modelo::query(), ['NomEmpl']))`.
4. Test nuevo con UsesSqlsrvSqlite (crear MecVerificaMaquinaTable con Folio, Fecha, TurnoRecibe, CveOperador, NomOperador, Estatus, HoraInicio, HoraFin) + SiembraPermisos con `[<idrol> => 'Estado Maquina']` (el componente resuelve por nombre; buscar el idrol en los comentarios `// Estado Maquina` de routes/modules/*.php y, si no aparece, usar cualquier entero: la búsqueda es por nombre). RED antes del cambio 2 (sqlite sí acepta OFFSET, así que el RED es la aserción de DB::listen).
Commit (solo Index.php, ConTabla.php y el test): `fix(08-02): ERP-F0-10 VerificaMaquina pagina sin OFFSET; polling Atadores pausado`.
  </action>
  <verify>
    <automated>php artisan test --filter=VerificaMaquinaIndexPaginacionTest && grep -c "document.hidden" resources/views/modulos/atadores/programaAtadores/index.blade.php</automated>
  </verify>
  <done>Test en verde; `grep -c "document.hidden"` = 1 y `grep -c "setInterval(refreshStatus, 15000)"` = 1 en la vista de Atadores; `grep -rn -e "->paginate(" app/Livewire` sin resultados; `git show --stat HEAD` no incluye programaAtadores/index.blade.php.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → POST urd-bpm-line.toggle | Captura de BPM con CSRF; hoy el token impreso caduca y da 419 |
| navegador → GET atadores.programa.estatus | Sondeo periódico autenticado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | Urdido-BPM-Line toggle | mitigate | http.post (axios) envía XSRF de la cookie; errores 419/HTTP visibles con notify, no JSON.parse de HTML |
| T-08-07 | Denial of Service | Polling Programa Atadores | mitigate | `document.hidden` + 15 s reduce carga en pestañas ocultas |
| T-08-08 | Denial of Service | VerificaMaquina página ≥2 en SQL Server 2008 R2 | mitigate | PaginacionCompat (TOP) en lugar de OFFSET/FETCH; test con DB::listen |
| T-08-09 | Information Disclosure | Vistas con cambio de clases CSS | accept | Solo clases de presentación; sin datos ni escapes afectados |
</threat_model>

<verification>
- `php artisan test --filter="VistasTailwindV4Test|VerificaMaquinaIndexPaginacionTest"` verde.
- `npm run build` verde.
- `grep -rn "bg-opacity-" resources/views` sin resultados; `grep -c "css/app.css" resources/js/app.js` = 0.
- programaAtadores/index.blade.php conserva todos los cambios previos del usuario (`git diff --stat` del archivo ≈ +117/-271, es decir, el WIP original más 2 líneas).
</verification>

<success_criteria>
- 0 `bg-opacity-*` en vistas; `max-w-md` en reporte-resumen-engomado (ERP-F0-04).
- Tailwind se descarga una vez por página (ERP-F0-05).
- Urdido-BPM-Line usa `http.post` (ERP-F0-09).
- Polling de Atadores pausado con `document.hidden` a 15 s; VerificaMaquina pagina con PaginacionCompat (ERP-F0-10).
</success_criteria>

<output>
Crear `.planning/phases/08-erp-quick-wins/08-02-SUMMARY.md` al terminar.
</output>
