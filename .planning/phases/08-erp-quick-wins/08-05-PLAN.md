---
phase: 08-erp-quick-wins
plan: 05
type: execute
wave: 2
depends_on: ["08-01", "08-03"]
files_modified:
  - routes/modules/urdido.php
  - routes/modules/engomado.php
  - routes/modules/configuracion.php
  - routes/public.php
  - app/Http/Controllers/ModulosController.php
  - app/Http/Controllers/SystemController.php
  - tests/Feature/PublicSensitiveRoutesAuthTest.php
  - routes/modules/planeacion.php
  - app/Http/Controllers/Planeacion/ProgramaTejido/ProgramaTejidoCatalogosController.php
  - routes/modules/tejido.php
  - app/Http/Controllers/Tejido/InventarioTrama/NuevoRequerimientoController.php
  - app/Http/Controllers/Tejido/InventarioTrama/ConsultarRequerimientoController.php
  - app/Services/Tejido/InventarioTrama/NuevoRequerimientoService.php
  - routes/modules/tejedores.php
  - app/Http/Controllers/Tejido/CortesEficiencia/CortesEficienciaController.php
  - app/Http/Controllers/Planeacion/CatCodificados/CatCodificacionController.php
  - app/Http/Controllers/Planeacion/CatalogoPlaneacion/ModelosCodificados/CodificacionController.php
  - app/Http/Controllers/Tejedores/BPMTejedores/TelBpmLineController.php
  - app/Http/Controllers/Tejido/ProduccionReenconado/ProduccionReenconadoCabezuelaController.php
autonomous: true
requirements: [ERP-F0-08]

must_haves:
  truths:
    - "Las 6 rutas de editar-ordenes-programadas que devolvían 500 ya no existen; urdido/engomado.editar.ordenes.programadas (index) siguen vivas"
    - "/modulos-sin-auth, /test-404 y los métodos de ModulosController sin consumidor ya no existen; sincronizarPermisos sigue vivo"
    - "Las 19 rutas AJAX de Inventario Trama y show/updateStatus de Consultar ya no existen; las rutas index y resumen siguen vivas"
    - "Ningún endpoint POST/PATCH sin consumidor de §2.1 fila 4 sigue registrado (guardarTabla, recalcularMarbete, buscar, bulkSave, cambiarStatus)"
    - "Telegram, /orden-produccion, el tablero clásico Urd/Eng, salon-options y calendario-lineas siguen intactos"
  artifacts:
    - path: "app/Http/Controllers/SystemController.php"
      provides: "Borrado (solo servía /test-404)"
    - path: "tests/Feature/PublicSensitiveRoutesAuthTest.php"
      provides: "Sin casos de /modulos-sin-auth"
  key_links:
    - from: "tests/Feature/RouteNameContractTest.php"
      to: "rutas borradas"
      via: "Route::has() de cada route('x') literal (creado en 08-03)"
      pattern: "RouteNameContractTest"
    - from: "routes/modules/tejido.php"
      to: "tejido.inventario.trama.nuevo.requerimiento"
      via: "ruta index conservada"
      pattern: "inventario\\.trama\\.nuevo\\.requerimiento'\\)"
---

<objective>
R1.1 (`auditoria_limpieza_plan.md`) + los 5 endpoints POST/PATCH sin consumidor de la auditoría §2.1 fila 4 (ERP-F0-08). §3 Fase 0 ítem 8.

Purpose: rutas que hoy devuelven 500 (métodos inexistentes), superficies sin autenticación de módulo (`/modulos-sin-auth`), y endpoints que nadie llama (~920 L).
Output: rutas y métodos borrados; RouteNameContractTest (de 08-03), tests filtrados y suite completa (una vez) en verde.

Regla (CONTEXT): antes de borrar, `grep` del nombre de ruta Y del fragmento de URL (y del método) en resources/ public/js app/ tests/ debe dar solo el registro/definición; si aparece un consumidor, no se borra y se anota como "vivo". Solo se borra lo listado en interfaces (R1.1 + auditoría); nada "de paso": ni otros endpoints del bloque, ni métodos de servicio que queden huérfanos, ni dependencias de constructor, ni FormRequests (si quedan sin uso, se anotan en el SUMMARY). Excluidos: TelegramController y routes/modules/telegram.php, `/orden-produccion`, tablero clásico Urd/Eng, `salon-options`, `calendario-lineas` (auditoría §2.6 lo reutiliza).

Commits: un commit por tarea con el ID del requisito: `refactor(08-05): ERP-F0-08 (N/3) ...`.
Tests: cada tarea usa `--filter`; la suite completa corre UNA vez al final de la Tarea 3.
Deploy (anotar en el SUMMARY): cache-bust de assets; una tablet con JS viejo que pegue a `/modulo-nuevo-requerimiento/guardar` recibirá 404.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-erp-quick-wins/08-CONTEXT.md
@.planning/auditoria_erp_2026-09.md
@.planning/auditoria_limpieza_plan.md
@.planning/phases/08-erp-quick-wins/08-03-SUMMARY.md

<interfaces>
Verificado 2026-09-22 (líneas pueden moverse ±5; configuracion.php y CodificacionController ya fueron editados por 08-03).

Tarea 1 — R1.1 órdenes 1, 4 y 5:
  urdido.php :72-75 (4 rutas `editar-ordenes-programadas/{actualizar,obtener-orden,actualizar-julios,actualizar-hilos-produccion}`), engomado.php :76-77 (`actualizar`, `obtener-orden`): métodos inexistentes en EditarOrdenesProgramadasController / EditarOrdenesEngomadoController (solo tienen index) → hoy 500. CONSERVAR la ruta index `editar.ordenes.programadas` de ambos (la usan EdicionOrdenes y EngomadoEdicionOrdenesTest).
  ModulosController: duplicar(:414), toggleAcceso(:438), togglePermiso(:467), getModulosPorNivel(:369), getSubmodulos(:391) — 0 consumidores de `toggle-acceso`, `toggle-permiso`, `/duplicar`, `api/modulos/nivel`, `api/modulos/submodulos` y sus nombres; rutas en configuracion.php (grupo `modulos.` y el grupo `module.permission:acceso,{$modulos}` de api/modulos). `sincronizarPermisos` VIVO (gestion-modulos/index.blade.php:525): no tocar. routes/public.php:32-39 grupo `modulos-sin-auth` (name `modulos.gestion.*`) + rama `routeIs('modulos.gestion.*')` en ModulosController::getModulosIndexRoute (:519). PublicSensitiveRoutesAuthTest: casos `/modulos-sin-auth` del data provider (:21-24) y `assertRouteHasAuth('modulos.gestion.*')` (:58-59).
  routes/public.php:19 `/test-404` → SystemController::test404 (único método; su `use` en public.php:5).

Tarea 2 — R1.1 órdenes 2 y 3:
  planeacion.php, bloques `/programa-tejido/*` (~:323-345) y `/muestras/*` (~:403-425) → ProgramaTejidoCatalogosController. Con 0 consumidores verificados (URL y concatenaciones `apiBase + '/...'`): `flogs-id-options` (getFlogsIdOptions :75), `flogs-by-tamano-clave` (getFlogsByTamanoClave :225), `ultima-fecha-final-telar` (getUltimaFechaFinalTelar :667), `eficiencia-std` (getEficienciaStd :572; no confundir con eficiencia-velocidad-std). Son 4 x 2 prefijos = 8 rutas; R1.1 hablaba de 7 x 2: anotar la diferencia en el SUMMARY, no buscar más.
  Inventario Trama (tejido.php): 19 rutas cuyo action es uno de los 11 métodos de NuevoRequerimientoController (guardarRequerimientos, getTurnoInfo, enProcesoInfo, actualizarCantidad, getCalibres, getFibras, getColores, buscarNombresColor, buscarArticulos, buscarFibras, buscarCodigosColor): :171-172, :175-180, :241-251. CONSERVAR :170 `tejido.inventario.trama.nuevo.requerimiento` y :240 `modulo.nuevo.requerimiento` (index). NuevoRequerimientoService.php:71-72: quitar las claves `actualizarCantidadUrl` y `guardarUrl` del VM (R1.1).
  ConsultarRequerimientoController: show(:32) y updateStatus(:55) + rutas tejido.php:254-255 (`modulo.consultar.requerimiento.show` / `.status`). CONSERVAR index y resumen (ConsultarRequerimiento.php:136 usa `modulo.consultar.requerimiento.resumen`).

Tarea 3 — §2.1 fila 4 (los 5 primeros; Telegram excluido). Hoy `grep -rnE "'guardarTabla'|'recalcularMarbete'|'buscar'\]|'bulkSave'|'cambiarStatus'\]" routes` = 5 líneas:
  CortesEficienciaController::guardarTabla (:1556-1668) + tejido.php:225 `cortes.eficiencia.guardar.tabla`
  CatCodificacionController::recalcularMarbete (:230-307) + planeacion.php:105 `codificacion.recalcular-marbetes`
  CodificacionController::buscar (:1161-1236) + planeacion.php:97 `codificacion.buscar`
  TelBpmLineController::bulkSave (:171-231) + tejedores.php:118 `tel-bpm-line.bulk`
  ProduccionReenconadoCabezuelaController::cambiarStatus (:410-443) + tejido.php:148 `produccion.reenconado.cambiar-status`

Tests relacionados: RouteNameContractTest, PublicSensitiveRoutesAuthTest, ConfiguracionRoutePermissionTest, RutasDestructivasPermisoTest, ModulosRutaTest, EngomadoEdicionOrdenesTest, NuevoRequerimientoLivewireTest, ConsultarRequerimientoLivewireTest, ProgramaTejidoCatalogosTest, CatCodificadosExcelImportTest, PlaneacionMutationAuthorizationTest.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: R1.1 — rutas editar-ordenes que dan 500, métodos de Modulos sin consumidor, /modulos-sin-auth y /test-404 (ERP-F0-08, 1/3)</name>
  <files>routes/modules/urdido.php, routes/modules/engomado.php, routes/modules/configuracion.php, routes/public.php, app/Http/Controllers/ModulosController.php, app/Http/Controllers/SystemController.php, tests/Feature/PublicSensitiveRoutesAuthTest.php</files>
  <action>
Per CONTEXT (ERP-F0-08, R1.1 órdenes 1, 4 y 5):
1. Borrar las 4 rutas de urdido.php y las 2 de engomado.php de interfaces (confirmar 0 usos de sus nombres). Conservar la ruta index de ambos.
2. ModulosController: para cada uno de los 5 métodos, grep de nombre de ruta y fragmento de URL; con 0, borrar método y ruta en configuracion.php (si el grupo de api/modulos queda vacío, borrarlo). Borrar el grupo `modulos-sin-auth` de routes/public.php y la rama `routeIs('modulos.gestion.*')` de getModulosIndexRoute. En el MISMO commit, quitar de PublicSensitiveRoutesAuthTest los casos `/modulos-sin-auth` y los `assertRouteHasAuth('modulos.gestion.*')`.
3. Borrar `/test-404` y su `use SystemController` de routes/public.php, y app/Http/Controllers/SystemController.php tras grep = 0 de `SystemController`.
Commit `refactor(08-05): ERP-F0-08 (1/3) rutas R1.1 que daban 500 y superficies sin consumidor`.
  </action>
  <verify>
    <automated>! grep -qE "editar-ordenes-programadas/(actualizar|obtener-orden)" routes/modules/urdido.php routes/modules/engomado.php && grep -q "editar.ordenes.programadas')" routes/modules/urdido.php && ! grep -qE "modulos-sin-auth|test-404|SystemController" routes/public.php && ! test -f app/Http/Controllers/SystemController.php && grep -q "sincronizarPermisos" app/Http/Controllers/ModulosController.php && php artisan test --filter="RouteNameContractTest|PublicSensitiveRoutesAuthTest|ConfiguracionRoutePermissionTest|RutasDestructivasPermisoTest|EngomadoEdicionOrdenesTest|ModulosRutaTest"</automated>
  </verify>
  <done>Las rutas y el archivo ya no existen, las index y sincronizarPermisos siguen; tests filtrados en verde.</done>
</task>

<task type="auto">
  <name>Tarea 2: R1.1 — 4 endpoints de catálogo PT sin consumidor e Inventario Trama AJAX (ERP-F0-08, 2/3)</name>
  <files>routes/modules/planeacion.php, app/Http/Controllers/Planeacion/ProgramaTejido/ProgramaTejidoCatalogosController.php, routes/modules/tejido.php, app/Http/Controllers/Tejido/InventarioTrama/NuevoRequerimientoController.php, app/Http/Controllers/Tejido/InventarioTrama/ConsultarRequerimientoController.php, app/Services/Tejido/InventarioTrama/NuevoRequerimientoService.php</files>
  <action>
Per CONTEXT (ERP-F0-08, R1.1 órdenes 2 y 3). Solo lo listado en interfaces:
1. Catálogo PT: re-confirmar con `grep -rnF` (resources, public/js, app, tests) 0 consumidores de los 4 fragmentos; borrar sus 8 rutas (ambos prefijos). Borrar de ProgramaTejidoCatalogosController los 4 métodos solo si no tienen llamada interna (`$this->método(`); si alguno la tiene (p. ej. getEficienciaStd desde getEficienciaVelocidadStd), se conserva el método y solo se borra la ruta. Es código de Programa Tejido: solo borrar, no refactorizar. salon-options y calendario-lineas no se tocan.
2. Inventario Trama: borrar las 19 rutas y los 11 métodos de NuevoRequerimientoController; conservar :170 y :240. En NuevoRequerimientoService borrar solo las claves `actualizarCantidadUrl` y `guardarUrl` del VM (tras confirmar 0 lecturas en resources/ y app/Livewire). No podar métodos de servicio, dependencias de constructor ni GuardarRequerimientoRequest: anotarlos en el SUMMARY si quedan sin uso.
3. ConsultarRequerimientoController: borrar show, updateStatus y sus 2 rutas; conservar index y resumen.
Commit `refactor(08-05): ERP-F0-08 (2/3) endpoints de catalogo PT e Inventario Trama sin consumidor`.
  </action>
  <verify>
    <automated>! grep -qE "flogs-id-options|flogs-by-tamano-clave|ultima-fecha-final-telar|/eficiencia-std'" routes/modules/planeacion.php && grep -q "salon-options" routes/modules/planeacion.php && grep -q "calendario-lineas" routes/modules/planeacion.php && ! grep "NuevoRequerimientoController::class, '" routes/modules/tejido.php | grep -qv "'index'" && ! grep -qE "ConsultarRequerimientoController::class, '(show|updateStatus)'" routes/modules/tejido.php && ! grep -rqE "actualizarCantidadUrl|guardarUrl" app resources && php artisan test --filter="RouteNameContractTest|NuevoRequerimientoLivewireTest|ConsultarRequerimientoLivewireTest|ProgramaTejidoCatalogosTest"</automated>
  </verify>
  <done>Solo las rutas/métodos listados desaparecen; index, resumen, salon-options y calendario-lineas siguen; tests filtrados en verde.</done>
</task>

<task type="auto">
  <name>Tarea 3: 5 endpoints POST/PATCH sin consumidor (ERP-F0-08, 3/3) + suite completa</name>
  <files>routes/modules/tejido.php, routes/modules/planeacion.php, routes/modules/tejedores.php, app/Http/Controllers/Tejido/CortesEficiencia/CortesEficienciaController.php, app/Http/Controllers/Planeacion/CatCodificados/CatCodificacionController.php, app/Http/Controllers/Planeacion/CatalogoPlaneacion/ModelosCodificados/CodificacionController.php, app/Http/Controllers/Tejedores/BPMTejedores/TelBpmLineController.php, app/Http/Controllers/Tejido/ProduccionReenconado/ProduccionReenconadoCabezuelaController.php</files>
  <action>
Per CONTEXT (ERP-F0-08, §2.1 fila 4, los 5 primeros). Para cada par: grep del nombre de ruta, del fragmento de URL (`guardar-tabla`, `recalcular-marbetes`, `codificacion-modelos/buscar`, `lineas/bulk-save`, `cambiar-status`) y del método en resources/ public/js app/ tests/; con 0, borrar la ruta y el método (el rango de líneas de la auditoría; helpers privados fuera de ese rango no se tocan). CodificacionController ya fue editado por 08-03 (:628): trabajar sobre esa versión.
Commit `refactor(08-05): ERP-F0-08 (3/3) endpoints POST sin consumidor`.
Cierre del plan: correr una vez `php artisan test` completo, `npm run build` y `npm run typecheck`; anotar resultados, total de líneas borradas (`git diff --stat` de los 3 commits) y la nota de cache-bust. Fallos ajenos se reportan sin tocarlos.
  </action>
  <verify>
    <automated>! grep -rqE "'guardarTabla'|'recalcularMarbete'|'buscar'\]|'bulkSave'|'cambiarStatus'\]" routes && php artisan test --filter="CatCodificadosExcelImportTest|RouteNameContractTest|RutasDestructivasPermisoTest|PlaneacionMutationAuthorizationTest" && php artisan test && npm run build && npm run typecheck</automated>
  </verify>
  <done>0 registros de los 5 endpoints (hoy 5); suite completa, build y typecheck en verde (o fallos ajenos documentados); `git diff --stat HEAD~3 -- routes/modules/telegram.php app/Http/Controllers/Telegram` vacío.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador/tablets → rutas HTTP | Cada ruta borrada reduce superficie; una tablet con JS viejo puede seguir llamándolas |
| usuario autenticado → routes/public.php | `/modulos-sin-auth` y `/test-404` vivían fuera del árbol de configuración |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Elevation of Privilege | /modulos-sin-auth, toggles de ModulosController | mitigate | Borrar rutas y métodos sin consumidor |
| T-08-15 | Denial of Service | Borrado de un endpoint que sí tenía consumidor | mitigate | grep previo (nombre + URL), verify con greps que exigen ausencia/presencia exacta, RouteNameContractTest, tests filtrados y suite completa al final |
| T-08-16 | Denial of Service | Tablets con JS cacheado llamando rutas borradas | accept | 404 controlado; cache-bust de assets en el deploy |
| T-08-17 | Tampering | ProgramaTejidoCatalogosController (código de PT) | mitigate | Solo los 4 endpoints listados; métodos con llamada interna se conservan |
</threat_model>

<verification>
- Filtros de cada tarea en verde; suite completa, `npm run build` y `npm run typecheck` una vez al final.
- 3 commits ERP-F0-08, ninguno con archivos ajenos.
</verification>

<success_criteria>
- R1.1 aplicado y 5 endpoints POST sin consumidor retirados (ERP-F0-08).
</success_criteria>

<output>
Crear `.planning/phases/08-erp-quick-wins/08-05-SUMMARY.md` al terminar.
</output>
