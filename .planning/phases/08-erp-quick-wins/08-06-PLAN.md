---
phase: 08-erp-quick-wins
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php
  - tests/Feature/NotificarCortadoRolloOrdenProduccionTest.php
autonomous: true
requirements: [ERP-F0-06]

must_haves:
  truths:
    - "GET /tejedores/cortadoderollo/orden-produccion ya no ejecuta SELECT @@VERSION ni devuelve la clave 'debug'"
    - "El endpoint conserva su contrato: 200 con success+orden, 404 sin orden activa, 400 sin no_telar"
  artifacts:
    - path: "tests/Feature/NotificarCortadoRolloOrdenProduccionTest.php"
      provides: "Test de que getOrdenProduccion no expone debug ni consulta la versión"
  key_links:
    - from: "routes/modules/tejedores.php"
      to: "NotificarMontRollosController::getOrdenProduccion"
      via: "notificar.cortado.rollo.orden.produccion (sin cambios)"
      pattern: "getOrdenProduccion"
---

<objective>
Quitar la fuga de información de `getOrdenProduccion` (auditoría §2.1 fila 14, Fase 0 ítem 6): hoy prueba la conexión TI con `SELECT @@VERSION` en cada llamada y devuelve la versión del servidor en la clave `debug`.

Purpose: plan pequeño separado de 08-01 porque solo toca NotificarMontRollosController; corre en paralelo en la wave 1.
Output: método sin probe ni debug, con test. Un commit: `fix(08-06): ERP-F0-06 getOrdenProduccion sin probe de version ni debug`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-erp-quick-wins/08-CONTEXT.md

<interfaces>
app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php — getOrdenProduccion(Request) (:219-265):
  lee `no_telar` (400 si falta); bloque try/catch "Probar conexión a TOW_PRO" con `DB::connection('sqlsrv_ti')->select('SELECT @@VERSION as version')` → `$conexionTowPro`; luego `DB::table('ReqProgramaTejido')->where('NoTelarId', $noTelar)->where('EnProceso', 1)->select('NoProduccion','NoTelarId','SalonTejidoId')->first()`; 404 `{error, debug:{telar_buscado, conexion_tow_pro}}`; 200 `{success, orden, debug:{conexion_tow_pro}}`.
Ruta: routes/modules/tejedores.php:52 GET `/tejedores/cortadoderollo/orden-produccion` name `notificar.cortado.rollo.orden.produccion` (solo `auth`). Ningún archivo de resources/ lee `debug` ni `conexion_tow_pro`.
tests/Concerns/UsesSqlsrvSqlite.php: useSqlsrvSqlite(), createProgramaTejidoTable() (en la conexión por defecto; trae NoTelarId, SalonTejidoId, EnProceso), createUsuario().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Quitar SELECT @@VERSION y bloque debug de getOrdenProduccion (ERP-F0-06)</name>
  <files>app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php, tests/Feature/NotificarCortadoRolloOrdenProduccionTest.php</files>
  <behavior>
    - ReqProgramaTejido con NoTelarId '201' EnProceso 1: GET route('notificar.cortado.rollo.orden.produccion', ['no_telar' => '201']) → 200, JSON con 'success' y 'orden', sin clave 'debug'.
    - Sin orden activa: 404 con 'error' y sin 'debug'.
    - Sin no_telar: 400.
    - Ninguna consulta registrada con DB::listen contiene '@@VERSION'.
  </behavior>
  <action>
Per CONTEXT ERP-F0-06: borrar el try/catch del probe (`SELECT @@VERSION` contra `sqlsrv_ti`) y `$conexionTowPro`; quitar `'debug' => [...]` de las respuestas 404 y 200. Conservar método y rutas (`/orden-produccion` puede tener clientes externos: excluido por CONTEXT). No tocar `config/database.php` ni CLAUDE.md.
Test nuevo (RED primero) con UsesSqlsrvSqlite (useSqlsrvSqlite + createProgramaTejidoTable; añadir la columna `NoProduccion` con Schema si la tabla no la trae) y `actingAs($this->createUsuario())`.
Al terminar, correr `php artisan test` completo una vez (fin del plan) y anotar el resultado.
  </action>
  <verify>
    <automated>php artisan test --filter=NotificarCortadoRolloOrdenProduccionTest && ! grep -qE "@@VERSION|conexion_tow_pro" app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php</automated>
  </verify>
  <done>Test en verde; el controlador no contiene `@@VERSION` ni `conexion_tow_pro`; commit de ERP-F0-06 creado; suite completa ejecutada una vez.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador (tablet de tejedores) → GET orden-produccion | Endpoint autenticado que respondía la versión del SQL Server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | getOrdenProduccion | mitigate | Eliminar `SELECT @@VERSION` y la clave `debug`; test que verifica su ausencia |
| T-08-18 | Denial of Service | Probe a sqlsrv_ti en cada llamada | mitigate | Se elimina la consulta extra a TI_PRO |
</threat_model>

<verification>
- `php artisan test --filter=NotificarCortadoRolloOrdenProduccionTest` verde; suite completa una vez al final.
- `! grep -q "@@VERSION" app/Http/Controllers/Tejedores/NotificarMontadoRollo/NotificarMontRollosController.php`.
</verification>

<success_criteria>
- `getOrdenProduccion` no expone `SELECT @@VERSION` ni bloque debug (ERP-F0-06).
</success_criteria>

<output>
Crear `.planning/phases/08-erp-quick-wins/08-06-SUMMARY.md` al terminar.
</output>
