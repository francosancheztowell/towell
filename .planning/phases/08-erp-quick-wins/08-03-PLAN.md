---
phase: 08-erp-quick-wins
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/modules/configuracion.php
  - resources/views/modulos/cargar-catalogos.blade.php
  - app/Http/Controllers/Planeacion/CatalogoPlaneacion/ModelosCodificados/CodificacionController.php
  - app/Http/Controllers/UsuarioController.php
  - tests/Feature/RouteNameContractTest.php
  - tests/Feature/UsuarioDestroyRedirectTest.php
  - app/Services/ProgramaUrdEng/InventarioReservasService.php
  - tests/Unit/InventarioReservasPatronesTest.php
autonomous: false
requirements: [ERP-F0-11, ERP-F0-03]

must_haves:
  truths:
    - "Ningún route('...') literal en app/ ni resources/views apunta a un nombre inexistente, y un test lo impide a futuro"
    - "La página de cargar catálogos ya no revienta con 500 (retirada, o implementada si el usuario lo decide)"
    - "Duplicar un modelo codificado inexistente y borrar un usuario inexistente redirigen a su listado en vez de lanzar RouteNotFoundException"
    - "El inventario disponible de julios (TI_PRO) se consulta con LIKE sin comodín inicial y sin LTRIM/RTRIM sobre InventLocationId"
    - "La salida del inventario disponible es idéntica antes y después (verificado contra sqlsrv_ti por el usuario)"
  artifacts:
    - path: "tests/Feature/RouteNameContractTest.php"
      provides: "Contrato route('x') del código vs Route::has()"
    - path: "tests/Unit/InventarioReservasPatronesTest.php"
      provides: "Guarda: los 3 PATTERN_* no empiezan con %"
    - path: "app/Services/ProgramaUrdEng/InventarioReservasService.php"
      provides: "Patrones sargables"
      contains: "PATTERN_RIZO = 'JU-ENG-RI%'"
  key_links:
    - from: "app/Http/Controllers/Planeacion/CatalogoPlaneacion/ModelosCodificados/CodificacionController.php"
      to: "planeacion.catalogos.codificacion-modelos"
      via: "redirect()->route"
      pattern: "route\\('planeacion\\.catalogos\\.codificacion-modelos'\\)"
    - from: "app/Http/Controllers/UsuarioController.php"
      to: "configuracion.usuarios.select"
      via: "redirect()->route en destroy()"
      pattern: "route\\('configuracion\\.usuarios\\.select'\\)"
---

<objective>
Rutas rotas (auditoría §2.7) con test de contrato, y el LIKE no sargable de InventarioReservasService (§2.6). Los dos ítems con checkpoint humano de la fase viven aquí.

Purpose: 3 `route()` a nombres inexistentes lanzan RouteNotFoundException (una de ellas hace que /configuracion/utileria/cargarcatalogos dé 500 al renderizar); el inventario disponible escanea 8.9M filas (2147 ms → 130 ms medido sin `%` inicial).
Output: decisión de cargar-catalogos aplicada, 2 nombres corregidos, RouteNameContractTest, patrones sin `%` inicial con guarda y comparación de salida aprobada por el usuario.

Commits: uno por requisito. ERP-F0-11 al cerrar la Tarea 2; ERP-F0-03 después del checkpoint de la Tarea 4 (el commit se hace SOLO si el usuario aprueba la comparación). Mensajes `fix(08-03): ERP-F0-NN <resumen>` / `perf(08-03): ERP-F0-03 ...`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-erp-quick-wins/08-CONTEXT.md
@.planning/auditoria_erp_2026-09.md
@CLAUDE.md
@tests/Concerns/SiembraPermisos.php

<interfaces>
Verificado 2026-09-22 con `route:list` y un escaneo con la misma regex que usará el test (456 literales en app/ + resources/views + routes/, exactamente 3 rotos):
  - resources/views/modulos/cargar-catalogos.blade.php:32 → `route('configuracion.utileria.cargar.catalogos.upload')` (no existe endpoint de subida; la vista entera da 500).
  - app/Http/Controllers/Planeacion/CatalogoPlaneacion/ModelosCodificados/CodificacionController.php:628 (create(), rama "duplicate no encontrado") → `route('codificacion.index')`; nombre real del listado: `planeacion.catalogos.codificacion-modelos` (GET planeacion/catalogos/codificacion-modelos).
  - app/Http/Controllers/UsuarioController.php:210 (destroy(int $id), rama "usuario no encontrado", usa `$this->usuarioRepository->findById($id)`) → `route('usuarios.select')`; nombre real `configuracion.usuarios.select`. Ruta de borrado: DELETE configuracion/usuarios/{id} name `configuracion.usuarios.destroy`, gate por idrol 59 (Usuarios, `$usuarios = 59` en configuracion.php:18).

routes/modules/configuracion.php: `$cargarCatalogos = 68;  // Cargar Catálogos` (:21), variable pasada en dos listas `use (...)` (:33 y :59); ruta :94-95 `Route::view('/cargarcatalogos', 'modulos/cargar-catalogos')->middleware("module.permission:acceso,{$cargarCatalogos}")->name('cargar-catalogos')` (nombre completo `configuracion.utileria.cargar-catalogos`). Sin otras referencias en app/, resources/, routes/ ni tests/.

Regex del contrato (probada): `/(?:(?<![\w>:$])route|redirect\(\)->route|redirectRoute|to_route)\(\s*['"]([A-Za-z0-9_.\-]+)['"]\s*[,)]/` — el lookbehind excluye `$request->route('id')` (parámetro de ruta) y `Route::`; nombres concatenados o variables no se capturan (a propósito).

app/Services/ProgramaUrdEng/InventarioReservasService.php (conexión TI `sqlsrv_ti`, DATAAREA 'PRO', ALMACENES ['A-JUL/TELA','A-JUL/URD'], LOCALIDADES_EN_MAQUINA ['MC1','MC2','MC3']):
  :42 `private const PATTERN_RIZO = '%JU-ENG-RI%';` :44 `PATTERN_PIE = '%JU-ENG-PI%'` :47 `PATTERN_URDIDO = '%JULIO-URDIDO%'`
  queryDisponibleFromTiPro(): join InventDim con `->whereIn(DB::raw('LTRIM(RTRIM(d.InventLocationId))'), self::ALMACENES)` (:694); filtro `s.ItemId like PATTERN_*` (:710-712); CASE Tipo con los mismos patrones (:728); filtros de UI por Tipo (:747-749). Los SELECT con LTRIM/RTRIM en columnas proyectadas (:714-721, :705 WMSLocationId) NO se tocan.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Tarea 1: Decidir cargar-catalogos — retirar o implementar la subida (ERP-F0-11)</name>
  <decision>/configuracion/utileria/cargarcatalogos (módulo SYSRoles idrol 68 "Cargar Catálogos") da 500 al renderizar: su formulario apunta a `configuracion.utileria.cargar.catalogos.upload`, que no existe, y no hay controlador ni Import de "catálogos" asociado. ¿Retirar la página o implementar la subida?</decision>
  <context>La página nunca funcionó en su forma actual (no hay endpoint). Implementar exige saber qué catálogos, qué hojas/columnas y qué tablas destino: ningún documento del repo lo define. Retirar deja la entrada de menú (SYSRoles 68) apuntando a un 404; desactivarla o borrarla desde Gestión de Módulos queda en manos del usuario (CONTEXT).</context>
  <options>
    <option id="retirar">
      <name>Retirar (default)</name>
      <pros>2 líneas de ruta + 1 variable + 1 vista; la página deja de dar 500; cero riesgo</pros>
      <cons>El menú sigue mostrando "Cargar Catálogos" hasta que el usuario lo desactive en Gestión de Módulos</cons>
    </option>
    <option id="implementar">
      <name>Implementar la subida</name>
      <pros>La pantalla haría algo útil</pros>
      <cons>Falta especificación (catálogos, formato, tablas destino): no cabe en Fase 0; el executor se detiene y se abre un plan nuevo con esa información</cons>
    </option>
  </options>
  <action>Presentar la decisión al usuario tal cual y esperar. Sin respuesta explícita no se asume nada; "retirar" es la opción recomendada por CONTEXT (ERP-F0-11).</action>
  <verify>El usuario respondió "retirar" o "implementar: ..." (registrar la respuesta literal en el SUMMARY).</verify>
  <done>Decisión registrada; la Tarea 2 sabe qué rama ejecutar.</done>
  <resume-signal>Escribe "retirar" o "implementar: &lt;qué catálogos, formato y tablas destino&gt;"</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Corregir las 3 rutas rotas + test de contrato de nombres (ERP-F0-11)</name>
  <files>routes/modules/configuracion.php, resources/views/modulos/cargar-catalogos.blade.php, app/Http/Controllers/Planeacion/CatalogoPlaneacion/ModelosCodificados/CodificacionController.php, app/Http/Controllers/UsuarioController.php, tests/Feature/RouteNameContractTest.php, tests/Feature/UsuarioDestroyRedirectTest.php</files>
  <behavior>
    - RouteNameContractTest: recorre app/ y resources/views (*.php), extrae nombres con la regex de interfaces y falla listando `archivo => nombre` para cada uno sin `Route::has()`. RED hoy con exactamente 3 entradas.
    - UsuarioDestroyRedirectTest: usuario autenticado con permisos sembrados para idrol 59 ('Usuarios'), UsuarioRepository mockeado (`findById` → null), DELETE route('configuracion.usuarios.destroy', 999) → redirect a route('configuracion.usuarios.select') con flash 'error'.
  </behavior>
  <action>
Per D de CONTEXT ERP-F0-11. Primero los dos tests (RED).
1. Si la decisión fue "retirar": borrar en configuracion.php las líneas :94-95 (Route::view cargarcatalogos), la línea `$cargarCatalogos = 68;` y quitar `$cargarCatalogos` de las dos listas `use (...)` (:33, :59); borrar resources/views/modulos/cargar-catalogos.blade.php. Antes de borrar, `grep -rn "cargar-catalogos\|cargarcatalogos\|\$cargarCatalogos" app resources routes tests` debe mostrar solo esas líneas. Anotar en el SUMMARY: "SYSRoles idrol 68 (Cargar Catálogos) sigue en el menú; desactivarlo en Gestión de Módulos (pendiente del usuario)". Si la decisión fue "implementar": no tocar configuracion.php ni la vista; marcar la tarea bloqueada y devolver checkpoint con la especificación recibida para que el orquestador cree un plan nuevo; en ese caso el test de contrato se entrega con esa única entrada documentada como excepción temporal en una constante `PENDIENTES = ['configuracion.utileria.cargar.catalogos.upload']` con comentario que cite ERP-F0-11.
2. CodificacionController.php:628: `redirect()->route('codificacion.index')` → `redirect()->route('planeacion.catalogos.codificacion-modelos')`.
3. UsuarioController.php:210: `route('usuarios.select')` → `route('configuracion.usuarios.select')`.
4. Correr ambos tests (GREEN). Si el contrato encuentra alguna entrada adicional no listada aquí, corregirla con el nombre real de `php artisan route:list --name=...` y anotarla.
Commit `fix(08-03): ERP-F0-11 rutas rotas y contrato de nombres de ruta`.
  </action>
  <verify>
    <automated>php artisan test --filter="RouteNameContractTest|UsuarioDestroyRedirectTest|ConfiguracionRoutePermissionTest"</automated>
  </verify>
  <done>Los tres filtros en verde; `grep -rn "codificacion.index\|'usuarios.select'\|cargar.catalogos.upload" app resources` sin resultados (con "retirar").</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Patrones LIKE sin % inicial y sin LTRIM/RTRIM en InventLocationId (ERP-F0-03)</name>
  <files>app/Services/ProgramaUrdEng/InventarioReservasService.php, tests/Unit/InventarioReservasPatronesTest.php</files>
  <behavior>
    - Para PATTERN_RIZO, PATTERN_PIE y PATTERN_URDIDO (leídos con ReflectionClassConstant): no empiezan con '%' y sí terminan con '%'.
    - El archivo del servicio no contiene `LTRIM(RTRIM(d.InventLocationId))` dentro de un whereIn.
  </behavior>
  <action>
Per D de CONTEXT ERP-F0-03. Test primero (RED). Luego: :42 `'JU-ENG-RI%'`, :44 `'JU-ENG-PI%'`, :47 `'JULIO-URDIDO%'`; :694 `->whereIn(DB::raw('LTRIM(RTRIM(d.InventLocationId))'), self::ALMACENES)` → `->whereIn('d.InventLocationId', self::ALMACENES)` (en SQL Server `=`/IN ignora espacios finales; un espacio inicial sí cambiaría el resultado: lo cubre la comparación de la Tarea 4). Actualizar el docblock de los patrones: "sin comodín inicial para que el índice de ItemId se use (auditoría §2.6: 2147 ms → 130 ms)". No tocar los LTRIM/RTRIM de columnas proyectadas ni de WMSLocationId (:705) — fuera de lo decidido.
Si la conexión `sqlsrv_ti` responde desde esta máquina, ejecutar ya las dos consultas de la Tarea 4 con `php artisan tinker --execute` (solo SELECT, con `SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED`) y dejar el resultado en el SUMMARY para el checkpoint; si no responde, no es error: el usuario las corre en la Tarea 4.
NO commitear todavía: el commit de ERP-F0-03 depende de la aprobación de la Tarea 4.
  </action>
  <verify>
    <automated>php artisan test --filter="InventarioReservasPatronesTest|ReservarConTelarTransaccionTest|AtadoDeJulioReservadoTest"</automated>
  </verify>
  <done>Tests en verde; `grep -c "'%JU-ENG\|'%JULIO-URDIDO" app/Services/ProgramaUrdEng/InventarioReservasService.php` = 0; `grep -c "LTRIM(RTRIM(d.InventLocationId))'), self::ALMACENES" app/Services/ProgramaUrdEng/InventarioReservasService.php` = 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 4: Comparar la salida del inventario disponible contra sqlsrv_ti (ERP-F0-03)</name>
  <what-built>Los 3 patrones LIKE de InventarioReservasService ya no empiezan con `%` y el join a InventDim compara `d.InventLocationId` sin LTRIM/RTRIM. Riesgo (CONTEXT): si algún ItemId tiene un prefijo antes de `JU-ENG-` / `JULIO-URDIDO`, o algún InventLocationId trae espacio inicial, esos julios dejarían de aparecer como disponibles.</what-built>
  <how-to-verify>
    1. En SSMS contra TI_PRO (o el resultado que el executor dejó en el SUMMARY si pudo conectarse), ejecutar la consulta A — ItemIds que el patrón viejo encuentra y el nuevo no:
       SELECT DISTINCT s.ItemId FROM InventSum s WITH (NOLOCK) WHERE s.DATAAREAID = 'PRO' AND (s.ItemId LIKE '%JU-ENG-RI%' OR s.ItemId LIKE '%JU-ENG-PI%' OR s.ItemId LIKE '%JULIO-URDIDO%') AND NOT (s.ItemId LIKE 'JU-ENG-RI%' OR s.ItemId LIKE 'JU-ENG-PI%' OR s.ItemId LIKE 'JULIO-URDIDO%');
       Esperado: 0 filas.
    2. Consulta B — almacenes que solo casan con LTRIM/RTRIM:
       SELECT DISTINCT d.InventLocationId FROM InventDim d WITH (NOLOCK) WHERE d.DATAAREAID = 'PRO' AND LTRIM(RTRIM(d.InventLocationId)) IN ('A-JUL/TELA', 'A-JUL/URD') AND d.InventLocationId NOT IN ('A-JUL/TELA', 'A-JUL/URD');
       Esperado: 0 filas.
    3. Opcional: abrir Programa Urd/Eng → Reservar y programar → inventario disponible, confirmar que aparecen los mismos julios que antes y que la carga es visiblemente más rápida.
  </how-to-verify>
  <action>Mostrar al usuario las consultas A y B (y el resultado si el executor pudo correrlas en la Tarea 3). Si responde "aprobado": `git add` de InventarioReservasService.php y InventarioReservasPatronesTest.php y commit `perf(08-03): ERP-F0-03 LIKE sin comodin inicial en inventario disponible`. Si pega filas: NO commitear; revertir solo esas dos ediciones propias con Edit (no con git checkout de archivos ajenos) o conservar el `%` inicial en el patrón afectado, y anotar las filas en el SUMMARY como hallazgo para decidir.</action>
  <verify>Respuesta del usuario registrada; con "aprobado", `git log -1 --oneline` muestra el commit de ERP-F0-03.</verify>
  <done>ERP-F0-03 commiteado con salida idéntica aprobada, o documentado como bloqueado con las filas divergentes.</done>
  <resume-signal>Escribe "aprobado" (A y B dan 0 filas) o pega las filas que aparecieron</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → sqlsrv_ti (TI_PRO, ERP) | Consulta de solo lectura con NOLOCK sobre InventSum (8.9M filas) |
| navegador → DELETE configuracion/usuarios/{id} | Borrado gateado por idrol 59 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Denial of Service | InventarioReservasService::queryDisponibleFromTiPro | mitigate | LIKE sargable (sin `%` inicial) y whereIn sin función sobre la columna; guarda en InventarioReservasPatronesTest |
| T-08-11 | Tampering | Resultado de inventario disponible (reservas equivocadas si cambia) | mitigate | Checkpoint Tarea 4: consultas A y B = 0 filas antes de commitear |
| T-08-12 | Denial of Service | Páginas con route() a nombres inexistentes (500) | mitigate | Corrección + RouteNameContractTest en la suite |
| T-08-13 | Information Disclosure | Stack trace de RouteNotFoundException en 500 | accept | APP_DEBUG=false en prod; la corrección elimina la excepción de todos modos |
</threat_model>

<verification>
- `php artisan test --filter="RouteNameContractTest|UsuarioDestroyRedirectTest|InventarioReservasPatronesTest"` verde.
- Usuario aprobó la comparación A/B (Tarea 4) antes del commit de ERP-F0-03.
- 2 commits (ERP-F0-11, ERP-F0-03).
</verification>

<success_criteria>
- 0 `route()` literales a nombres inexistentes y un test de contrato que lo impide (ERP-F0-11).
- Patrones LIKE sin `%` inicial con salida idéntica verificada por el usuario (ERP-F0-03).
</success_criteria>

<output>
Crear `.planning/phases/08-erp-quick-wins/08-03-SUMMARY.md` al terminar (decisión de cargar-catalogos, resultado de A/B).
</output>
