---
phase: 20-arq-sec
plan: "20-01"
wave: 2
depends_on: []
autonomous: false
requirements: [ARQ-01, ARQ-02, ARQ-03, ARQ-04]
files_modified:
  - app/Http/Controllers/Tejedores/Desarrolladores/Funciones/* -> app/Services/Tejedores/Desarrolladores/*
  - app/Http/Controllers/Tejedores/Desarrolladores/TelDesarrolladores{,Muestras}Controller.php (use)
  - app/Livewire/Desarrolladores/Captura.php (use)
  - app/Providers/AppServiceProvider.php (2 use)
  - app/Http/Controllers/Planeacion/Utilerias/{Mover,Finalizar}OrdenesController.php (use)
  - app/Http/Controllers/Planeacion/ProgramaTejido/funciones/EliminarTejido.php (use)
  - tests/Unit/*Desarrollador*, tests/Unit/NotificacionTelegram*, tests/Unit/ProcesarMuestras* (use)
  - phpstan-baseline.neon
  - app/Helpers/FolioHelper.php
  - app/Helpers/TurnoHelper.php
  - app/Services/ProgramaUrdEng/CrearOrdenesService.php (obtenerFolioUrdEng + folio CambioHilo)
  - app/Http/Controllers/ProgramaUrdEng/ReservarProgramar/CrearOrdenKarlMayerController.php (idem)
  - app/Services/Tejido/InventarioTrama/NuevoRequerimientoService.php (folio de construirVm)
  - app/Http/Controllers/Tejedores/BPMTejedores/TelBpmController.php (generarFolio)
  - app/Http/Controllers/mecanicos/OrdenesTrabajoMecaController.php (asegurarSecuenciaFolios)
  - app/Livewire/Mecanicos/VerificaMaquina/Index.php (asegurarSecuenciaFolios)
  - app/Http/Controllers/Tejido/CortesEficiencia/CortesEficienciaController.php (getTurnoInfo)
  - app/Http/Controllers/Tejido/InventarioTrama/NuevoRequerimientoController.php (getTurnoInfo)
  - tests/Unit/Helpers/{FolioHelper,TurnoHelper,RutasSinColisionDeMayusculas}Test.php
  - tests/Unit/Helpers/FoliosCaracterizacionTest.php
must_haves:
  truths:
    - "El commit de ARQ-01 es un movimiento puro: git mv + namespace + use, cero lineas de logica."
    - "Cada folio reemplazado produce el mismo texto (prefijo + padding) y deja el mismo consecutivo que antes, con el mismo bloqueo (lockForUpdate dentro de transaccion)."
    - "Los tests de caracterizacion se escriben y pasan ANTES del cambio y siguen pasando sin tocar sus expectativas despues."
    - "Las respuestas JSON de /turno-info no cambian (mismas llaves y valores)."
    - "No queda ninguna ruta de app/ que colisione con otra al ignorar mayusculas."
  artifacts:
    - "app/Services/Tejedores/Desarrolladores/ con los 8 servicios."
    - "FolioHelper como unico punto de consumo de SSYSFoliosSecuencias fuera del modelo y de Configuracion."
    - "Tests de limites de turno 6:30 / 14:30 / 22:30 en America/Mexico_City."
  key_links:
    - "SSYSFoliosSecuencia::nextFolio -> FolioHelper::obtenerSiguienteFolio"
    - "TurnoHelper::getTurnoActual -> getTurnoInfo de Cortes y Nuevo Requerimiento"
---

<objective>
Sacar los servicios de Desarrolladores de la carpeta de controllers (movimiento puro), dejar un solo camino para consumir folios de `dbo.SSYSFoliosSecuencias` y para informar el turno, y cerrar BUG-022 con una verificacion automatica. Cero cambio de comportamiento observable.
</objective>

<execution_context>
Entorno sin SQL Server: los tests usan sqlite (`Tests\Concerns\UsesSqlsrvSqlite`). `SSYSFoliosSecuencia::nextFolio()` lee `INFORMATION_SCHEMA.COLUMNS`; en sqlite se simula adjuntando una base `INFORMATION_SCHEMA` con la tabla `COLUMNS`. `lockForUpdate()` es no-op en sqlite: el bloqueo se verifica leyendo el codigo (misma consulta, misma transaccion), no con un test de concurrencia. SQL crudo nuevo: ninguno.
</execution_context>

## Hallazgos del inventario (grep `SSYSFoliosSecuencias`, `Desarrolladores\Funciones`, `UrdEngNucleos`)

| Sitio | Que hace hoy | Accion |
|---|---|---|
| `CrearOrdenesService::obtenerFolioUrdEng()` y `CrearOrdenKarlMayerController::obtenerFolioUrdEng()` | `nextFolio('URD/ENG',5)`; si lanza `\Exception`, `nextFolioById(14,5)` | Borrar ambos; `FolioHelper::obtenerSiguienteFolio('URD/ENG', 5, idRespaldo: 14)` (parametro opcional nuevo, default `null` = comportamiento actual) |
| Mismos dos archivos, folio `CambioHilo` | `SSYSFoliosSecuencia::nextFolio('CambioHilo',5)['folio']` directo | `FolioHelper::obtenerSiguienteFolio('CambioHilo', 5)` |
| `NuevoRequerimientoService::construirVm()` | Reimplementacion: lee con lock `prefijo+pad(consecutivo)` **y luego** incrementa (el consecutivo es "el siguiente a usar", igual que `obtenerFolioSugerido`); sin fila, folio aleatorio | Ver decision D1 |
| `TelBpmController::generarFolio()` | Crea la secuencia si falta (sembrada con el maximo `BT` de `TelBPM`), la alinea al maximo, `nextFolio` con respaldo `nextFolioByPrefijo`, reintenta si el folio existe | Crear/alinear via helper comun (D2) y `FolioHelper::obtenerSiguienteFolio`; se conserva la alineacion y el respaldo |
| `OrdenesTrabajoMecaController::asegurarSecuenciaFolios()` y `VerificaMaquina\Index::asegurarSecuenciaFolios()` | Mismo metodo copiado: crea la secuencia sembrada con el ultimo folio del prefijo | `FolioHelper::asegurarSecuencia($modulo, $prefijo, fn () => ultimoFolio)` (D2) |
| `CortesEficienciaController`, `NuevoRequerimientoController` (folio) | Ya usan `FolioHelper` | Nada |
| `TurnoHelper::generarFolio()` | `TRAMA-YYYYMMDD-T`; 0 usos en app/, rutas, vistas, JS y tests | Borrar (codigo muerto) |
| `SecuenciaFoliosController` | CRUD del catalogo de secuencias | Fuera de alcance (no es reimplementacion) |
| `getTurnoInfo()` x2 | Ambos arman `{turno, descripcion}` con `TurnoHelper` (Cortes: horario; Trama: "Turno X" + folio sugerido) | `TurnoHelper::info()` devuelve `turno`, `horario`, `formato`; cada controller mapea a su JSON actual |
| `app/Models/urdengomado/` (BUG-022) | Ya borrado en `160613b`; solo existe `App\Models\UrdEngomado\UrdEngNucleos` | Test que falla si dos rutas de `app/` colisionan ignorando mayusculas + `composer dump-autoload` sin ambiguas. `CLAUDE.md` aun lista `urdengomado/` -> HANDOFF (CLAUDE.md es de BASE) |
| PT (`Planeacion/ProgramaTejido/**`) | Sin consumo directo de `SSYSFoliosSecuencias` | Sin HANDOFF de folios |

## Decisiones para el owner

**Resueltas por el owner (2026-09-25): D1 = A, D2 = sí. Plan aprobado.**

- **D1 — Folio de Trama en `construirVm()`.** (A, recomendada) Agregar `FolioHelper::consumirFolioSugerido($modulo, $long)`: con lock devuelve exactamente lo que `obtenerFolioSugerido()` muestra y luego incrementa. Numeracion identica a hoy, sugerido == consumido. El folio aleatorio sin fila queda en el servicio. (B) Usar `obtenerSiguienteFolio('Trama')`: unifica semantica con `resolverFolio()` pero salta un numero una vez y el sugerido de `/turno-info` queda uno atras; sin fila la pantalla da error en vez de folio aleatorio.
- **D2 — Incluir `asegurarSecuencia` (Mecanicos x2 + BPM) en FolioHelper.** No estaba en la lista original pero es la misma copia del mismo metodo de folios. Recomendado: si.

<tasks>

<task type="auto">
<name>20-01.1 ARQ-01 movimiento puro (commit propio)</name>
<files>app/Http/Controllers/Tejedores/Desarrolladores/Funciones/*, app/Services/Tejedores/Desarrolladores/*, consumidores, phpstan-baseline.neon</files>
<action>`git mv` de los 8 archivos; namespace `App\Services\Tejedores\Desarrolladores`; `use` en Captura, 2 controllers de Desarrolladores, AppServiceProvider (solo sus 2 `use`), 3 archivos PT (solo la linea `use`) y 11 tests. En el baseline, rutas y FQCN escapados. `use` entre servicios del mismo namespace que sobren se quitan solo si Pint/phpstan lo exigen.</action>
<verify>`git diff -M --stat` muestra renames ~100 %; `grep -r "Desarrolladores\\Funciones" app tests phpstan-baseline.neon` vacio; `php artisan test`, `phpstan`, `composer dump-autoload` limpios.</verify>
<done>Commit "arquitectura: mover servicios de Desarrolladores a app/Services" sin cambios de logica.</done>
</task>

<task type="auto">
<name>20-01.2 Caracterizacion de folios y turnos (antes de cambiar nada)</name>
<files>tests/Unit/Helpers/FoliosCaracterizacionTest.php, tests/Unit/Helpers/TurnoHelperTest.php</files>
<action>Escenarios por sitio: URD/ENG con fila (URD00042 -> URD00043, consecutivo 43), sin fila de modulo pero con Id 14 (respaldo), CambioHilo; Trama en construirVm (TR00007 creado, consecutivo 8; sin fila -> folio TR aleatorio y consecutivo intacto); BPM (sin fila: crea `BPMTEjido`/`BT` sembrada con el maximo de TelBPM; con fila atrasada: alinea; folio ocupado: avanza); Mecanicos (crea sembrada con el ultimo folio del prefijo; con fila no toca). Metodos privados por reflexion para no montar requests completos. Turnos: 06:29/06:30/14:29/14:30/22:29/22:30/00:00 en America/Mexico_City (fijando `Carbon::setTestNow` en UTC), y JSON actual de ambos `getTurnoInfo()`.</action>
<verify>Todos verdes sobre el codigo actual; commit "arquitectura: caracterizar folios y turnos antes de deduplicar".</verify>
<done>Las expectativas quedan congeladas; despues solo cambia el "como se invoca" cuando el metodo privado desaparece.</done>
</task>

<task type="auto">
<name>20-01.3 ARQ-02 folios -> FolioHelper</name>
<files>app/Helpers/FolioHelper.php y los sitios de la tabla</files>
<action>`obtenerSiguienteFolio(..., ?int $idRespaldo = null)`, `consumirFolioSugerido()` (si D1=A), `asegurarSecuencia()` (si D2). Reemplazar cada sitio sin cambiar transaccion/bloqueo: el helper corre dentro de la transaccion del llamador igual que hoy. Borrar `TurnoHelper::generarFolio()`. Quitar `use SSYSFoliosSecuencia` que queden sin uso.</action>
<verify>Tests de 20-01.2 sin cambios en expectativas; grep `SSYSFoliosSecuencias` fuera de FolioHelper/modelo/SecuenciaFoliosController vacio.</verify>
<done>Un solo camino de folios.</done>
</task>

<task type="auto">
<name>20-01.4 ARQ-03 getTurnoInfo -> TurnoHelper::info()</name>
<files>app/Helpers/TurnoHelper.php, CortesEficienciaController, NuevoRequerimientoController</files>
<action>`TurnoHelper::info(): array{turno, horario, formato}`; los dos controllers arman su JSON actual desde ahi.</action>
<verify>Tests de JSON de 20-01.2 verdes sin cambios.</verify>
<done>La logica de turno vive solo en TurnoHelper.</done>
</task>

<task type="auto">
<name>20-01.5 ARQ-04 BUG-022</name>
<files>tests/Unit/Helpers/RutasSinColisionDeMayusculasTest.php</files>
<action>Test que recorre `app/` y falla si dos rutas son iguales en minusculas; verificar `class_exists(App\Models\UrdEngomado\UrdEngNucleos::class)`. HANDOFF para la linea de CLAUDE.md.</action>
<verify>`composer dump-autoload` sin "Ambiguous class resolution".</verify>
<done>BUG-022 cerrado con guardia automatica.</done>
</task>

<task type="auto">
<name>20-01.6 Cierre</name>
<action>php artisan test; phpstan (quitar del baseline lo que se arregle); dump-autoload; npm run build && npm run ratchet; pint --test en tocados; code-review y security-review (concurrencia de folios); 20-01-SUMMARY.md y HANDOFF.md.</action>
<done>Push a claude/20-01-arq, sin PR.</done>
</task>

</tasks>
