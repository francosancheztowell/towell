---
phase: 20-arq-sec
plan: "20-03"
wave: 2
depends_on: ["20-02"]
autonomous: false
requirements: [SEC-05]
files_modified:
  - app/Http/Middleware/EnsureModulePermission.php (tercer parámetro `auditar`)
  - routes/modules/{atadores,crudo,engomado,mantenimiento,mecanicos,tejedores,tejido,telegram,urdido}.php (solo agregar middleware)
  - tests/Feature/Seguridad/ModuloPermisoAuditarTest.php (nuevo)
  - tests/Feature/Seguridad/EscrituraFueraDePlaneacionTest.php (nuevo, inventario)
  - .planning/phases/20-arq-sec/20-03-MAPA-AUTHZ.md
must_haves:
  truths:
    - "`module.permission:<accion>,<modulo>` sin tercer parámetro se comporta exactamente igual que hoy (403 JSON/HTML)."
    - "Con `auditar`: con permiso no se registra nada; sin permiso se registra SYSMonAcceso Tipo=authz_denegaria (UsuarioId, NumeroEmpleado, Motivo = acción, módulo, método y ruta) y la request sigue."
    - "Como máximo una fila por usuario+ruta+acción por hora (Cache::add con TTL 3600)."
    - "Si falla la caché o el registro, la request pasa igual (auditar nunca bloquea)."
    - "Toda ruta de escritura fuera de Planeación (POST/PUT/PATCH/DELETE, acción de App\\ o closure de routes/modules) tiene module.permission (enforce o auditar) o está justificada en la lista del test; alta de paros sin nada (excepción del owner)."
    - "No se mueven ni renombran rutas; el snapshot de PT no cambia; planeacion.php no se toca."
  artifacts:
    - "20-03-MAPA-AUTHZ.md con ruta ↔ acción ↔ módulo/idrol ↔ fuente."
  key_links:
    - "EnsureModulePermission -> userCan() (sin cambios) -> AccesoService::registrar('authz_denegaria', ...)"
---

<objective>
SEC-05: medir, sin bloquear a nadie, quién usa hoy las escrituras sin permiso de módulo, para que cada 19-xx pase a enforce (SEC-06) con dos semanas de datos de `/admin/accesos`.
</objective>

<execution_context>
- Inventario con `php artisan route:list --json` menos las rutas del snapshot de PT: 87 rutas de escritura sin `module.permission` (redirects 301, Livewire, debugbar/boost, `storage/{path}`, login/logout y telemetría aparte). El 20-CONTEXT estimaba ~106 porque es anterior a los gates de fases anteriores.
- `userCan()` acepta idrol o nombre de `SYSRoles.modulo`. No hay BD: los idrol salen de los comentarios de `module.permission` ya existentes en el mismo archivo de rutas y de las constantes de los controllers (48 Desarrolladores, 189 Desarrolladores Muestras). Donde no hay idrol probado se usa el nombre exacto que ya usa el controller o la vista con `userCan` (Programa Urdido, Programa Engomado, Andon, Solicitudes, Reporte Estado de Maquina, OT Diarias).
- 27 de las 87 ya se validan dentro del controller: el auditar de router repite ese mismo chequeo y no cambia nada; lo que aporta es el dato de cuántos intentos se niegan.
- Motivo (nvarchar 200) = `crear · 45 · POST atadores.save` (ruta por nombre o, si no tiene, por URI).
</execution_context>

## Resumen del mapa (detalle en 20-03-MAPA-AUTHZ.md)

| Archivo | Rutas | Módulo (idrol) | Acciones |
|---|---|---|---|
| urdido.php | 18 | Producción Urdido (154) ×8 · Programa Urdido (nombre) ×6 · BPM Urd (35) ×4 | modificar; registrar en actualizar-calidad; crear en store BPM |
| engomado.php | 21 | Producción Engomado (43) ×11 · Programa Engomado (nombre) ×4 · BPM Eng (41) ×4 · Captura de Formula (168) ×2 | crear/modificar |
| tejido.php | 19 | Reenconado (27) ×4 · Marcas Finales (177) ×7 · Cortes de Eficiencia (105) ×8 | crear/modificar/registrar; exportes y Telegram con `acceso` |
| tejedores.php | 12 | BPM Tejedores (47) ×5 · Inv Telas (21) ×2 · Desarrolladores (48/189) ×2 · 3 sin módulo (D3) | crear/modificar/acceso |
| mecanicos.php | 10 | Ordenes de Trabajo (193) ×4 · OT Diarias / Reporte Estado de Maquina (nombre) ×6 | crear/modificar; `acceso` en calificar línea y reportes |
| atadores.php | 2 | Programa Atadores (45) · reporte OEE (D3) | modificar |
| crudo.php | 2 | Andon (nombre) | registrar |
| mantenimiento.php | 2 | alta de paros: **excepción, sin nada** · finalizar paro: Solicitudes (nombre), modificar | — |
| telegram.php | 1 | Mensajes (181) | acceso |

## Decisiones para el owner

- **D3 — 4 rutas sin módulo identificable** (`tejedores/atadodejulio/notificar`, `tejedores/cortadoderollo/{notificar,insertar}`, `atadores/reportes-atadores/oee/despachar`): no hay nombre ni idrol en código ni en docs. (A, recomendada) Van en la lista "pendiente de idrol" del test y en el mapa, con una consulta `SELECT idrol, modulo, Ruta FROM SYSRoles WHERE Ruta LIKE ...` para que el owner la corra en Laragon; cuando llegue el idrol se agrega el auditar. (B) Auditarlas con un nombre supuesto (por ejemplo "Atado de Julio"): si el nombre no existe en SYSRoles, cada usuario genera una fila por hora (falsos positivos).
- **D4 — Rutas que el controller ya valida (27):** (A, recomendada) Agregarles el auditar con la misma acción y módulo, así el inventario queda uniforme y el test no necesita excepciones. (B) Justificarlas en el test sin middleware.
- **D5 — Rutas abiertas a propósito hoy** (BPM crear/editar de Urdido, Engomado y Tejedores; Desarrolladores con `acceso`; calificar línea de OT por tejedores; finalizar paro): se auditan con la acción que pide la vista (crear/modificar/acceso). Como auditar no bloquea, lo que se decida sobre ellas queda para el enforce de cada 19-xx. No hay nada que elegir; queda anotado para que lo revises.
- **Huecos que el inventario hizo visibles (fuera de alcance, van en el SUMMARY para las 19-xx):**
  - `atadores/save` con `action=supervisor` autoriza sin revisar permisos.
  - `engomado/.../actualizar-campo-orden` no tiene ningún chequeo.
  - `programar-engomado/actualizar-prioridades` está "habilitado para todos".
  - `telegram/send` es un relay sin llamadores (propuesta: borrarlo en 19-xx).
  - `PUT modulo-cortes-de-eficiencia/{id}` es un stub.
  - `produccion/reenconado-cabezuela` es un duplicado legacy.

<tasks>

<task type="auto">
<name>20-03.1 Middleware en modo auditar (tests primero)</name>
<files>app/Http/Middleware/EnsureModulePermission.php, tests/Feature/Seguridad/ModuloPermisoAuditarTest.php</files>
<action>
Firma `handle($request, $next, string $action, string $module, ?string $modo = null)`. `$modo === 'auditar'` → si no `userCan`: `Cache::add('authz_auditar:'.sha1(usuario|ruta|acción), 1, 3600)` y, si entra, `AccesoService::registrar('authz_denegaria', [...])`; siempre `$next`. Cualquier otro valor de `$modo` → comportamiento actual (fail closed).
Tests: enforce sin cambio (JSON 403, HTML 403, con permiso pasa); auditar con permiso → 0 filas; sin permiso → pasa + 1 fila con Motivo/UsuarioId; repetir 3 veces → 1 fila; otra ruta u otra acción → otra fila; al pasar la hora (travel) → nueva fila; kill switch de monitoreo → pasa sin fila; caché que lanza → pasa.
</action>
</task>

<task type="auto">
<name>20-03.2 Aplicar a routes/modules/*.php + test de inventario</name>
<files>routes/modules/{...}.php, tests/Feature/Seguridad/EscrituraFueraDePlaneacionTest.php</files>
<action>
Solo `->middleware('module.permission:<acción>,<idrol|nombre>,auditar')` o `middlewareFor` en resources, con comentario `// <módulo>` como el resto del archivo. Test: recorre `Route::getRoutes()`, toma escrituras que no están en el snapshot de PT, cuya acción es de `App\` o es un closure definido en `routes/modules`, y exige `module.permission:` salvo en la lista justificada (alta de paros, telemetría, login/logout, pendientes D3) → `assertSame` como en PlaneacionEscrituraAutorizacionTest. Además comprueba que los parámetros son válidos (acción ∈ 5, modo ∈ {vacío, auditar}).
</action>
<verify>php artisan test; snapshot PT sin cambios (tests de Planeación verdes)</verify>
</task>

<task type="auto">
<name>20-03.3 Mapa + SUMMARY</name>
<files>20-03-MAPA-AUTHZ.md, 20-03-SUMMARY.md</files>
<action>Mapa ruta por ruta con fuente. SUMMARY con despliegue (sin migración: SYSMonAcceso ya acepta authz_denegaria), consulta para revisar en /admin/accesos y pendientes D3/huecos.</action>
</task>

</tasks>
