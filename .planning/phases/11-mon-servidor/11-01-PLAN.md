---
phase: 11-mon-servidor
plan: "01"
wave: 0
depends_on: []
autonomous: true
requirements: [MON-01, MON-02, MON-03, MON-04, MON-09, MON-12, MON-14]
branch: claude/11-mon-servidor
contract: .planning/phases/11-mon-servidor/11-CONTRACT.md
files_modified:
  - database/migrations/2026_09_24_000001_create_sysmon_tables.php
  - database/sql/sysmon_tablas.sql
  - app/Models/Sistema/Monitoreo/*.php
  - app/Providers/MonitoreoServiceProvider.php
  - bootstrap/providers.php
  - bootstrap/app.php
  - config/monitoreo.php
  - app/Http/Middleware/Monitoreo/IdentificarDispositivo.php
  - app/Http/Middleware/Monitoreo/AplicarCierreRemoto.php
  - app/Listeners/Monitoreo/*.php
  - app/Services/Monitoreo/*.php
  - app/Http/Controllers/AuthController.php
  - routes/modules/admin.php
  - routes/web.php
  - resources/views/modulos/admin/index.blade.php (placeholder)
  - tests/Feature/Monitoreo/*.php
must_haves:
  truths:
    - "Un fallo del monitoreo nunca rompe la request del usuario."
    - "Cerrar sesión en una tablet no saca a las demás tablets del mismo usuario."
    - "Solo usuarios del área Sistemas pueden abrir /admin."
    - "Con MONITOREO_ENABLED=false no se escribe nada."
  artifacts:
    - "Migración + .sql espejo de las 6 tablas SYSMon*."
    - "Accesos (login, fallido, bloqueo, recordarme, logout) registrados."
    - "Cierre remoto por dispositivo operativo de extremo a extremo (servidor)."
---

<objective>
Esquema de monitoreo, identidad estable de dispositivo, sesiones y accesos (incluidos fallidos y bloqueos), rate limit de login, logout por dispositivo, cierre remoto y Gate `admin` por área. Es la base sobre la que trabajan las fases 12, 13 y 14.
</objective>

<execution_context>
- Leer primero `.planning/PROTOCOLO-SESIONES.md` (propiedad MON-A) y **`11-CONTRACT.md`** (fuente de verdad de nombres, columnas, rutas y config).
- Ponytail: reusar `app/Helpers/device_helpers.php` (`getDeviceInfo()`, `detectDeviceModel()`, `getClientIpv4()`), `app/Services/Mantenimiento/ParoTelegramNotifier.php` (patrón), `app/Http/Middleware/EnsureModulePermission.php` (estilo de middleware), `tests/Concerns/UsesSqlsrvSqlite.php` (tests), migración `2026_08_24_000001_create_catalogo_calibres_module.php` (estilo de migraciones sobre `sqlsrv` con guardas `hasTable`).
- `AuthController` hoy valida la contraseña a mano (no usa `Auth::attempt`), por eso `Illuminate\Auth\Events\Failed` nunca se dispara: el fallo se registra explícitamente. `Auth::login($empleado, true)` (remember siempre) **se conserva**.
- `SessionGuard` dispara `Login` también al restaurar por cookie remember → distinguir por ruta.
- Entorno sin SQL Server: migración con Schema builder portable; índices filtrados y `INCLUDE` solo en el `.sql` espejo. Tests con sqlite.
- Archivos calientes de esta ola que te pertenecen: `bootstrap/app.php`, `bootstrap/providers.php`, `routes/web.php` (solo `require`). No tocar `resources/js/**`, `package.json`, `vite.config.js`, `phpunit.xml`, `.github/**` (son de otras sesiones).
- Skills: `laravel-specialist`, `php-pro`, `security-review`, `code-review`.
</execution_context>

<tasks>

<task type="auto">
<name>11-01.1 Config, provider y Gate admin (MON-14)</name>
<files>config/monitoreo.php, app/Providers/MonitoreoServiceProvider.php, bootstrap/providers.php</files>
<action>`config/monitoreo.php` exactamente con las llaves del contrato §8. `MonitoreoServiceProvider`: registra en runtime la conexión `sqlsrv_monitoreo` clonando `config('database.connections.sqlsrv')` (no tocar `config/database.php`, gitignored/skip-worktree); `Gate::define('admin', fn ($u) => ...)` normalizando `area` (trim, sin acentos con `Str::ascii`, minúsculas) contra `areas_admin` normalizadas; `RateLimiter::for('login')` (10/min por `numero_empleado`+IP, 30/min por IP) y `RateLimiter::for('telemetria')` (120/min por dispositivo). Listeners registrados con `Event::listen`.</action>
<verify>Test: usuario `area='Sistemas'`, `' sistemas '` y `'SISTEMAS'` pasan el Gate; `'Planeación'` no.</verify>
<done>Provider registrado en `bootstrap/providers.php`.</done>
</task>

<task type="auto">
<name>11-01.2 Migración, .sql espejo y modelos (MON-01, MON-12)</name>
<files>database/migrations/2026_09_24_000001_create_sysmon_tables.php, database/sql/sysmon_tablas.sql, app/Models/Sistema/Monitoreo/{MonDispositivo,MonSesion,MonVista,MonError,MonErrorEvento,MonAcceso}.php</files>
<action>Tablas y columnas del contrato §2 (nombres exactos). Migración idempotente (`hasTable`). `.sql` espejo con tipos SQL Server, índices filtrados/INCLUDE y comentarios para el DBA. Modelos con `$table`, `$primaryKey='Id'`, `$timestamps=false`, `$fillable`, `$casts`; `MassPrunable` con `prunable()` según `config('monitoreo.retencion')`. Programar `model:prune` para estos modelos diario 02:00 en `routes/console.php` (**solo agregar** la línea de schedule; si `routes/console.php` tiene otras entradas, no tocarlas).</action>
<verify>`php artisan migrate --pretend` legible; test de poda con sqlite (fechas viejas se borran, recientes no).</verify>
<done>Tablas + modelos + poda.</done>
</task>

<task type="auto">
<name>11-01.3 Identidad de dispositivo y touch (MON-02)</name>
<files>app/Http/Middleware/Monitoreo/IdentificarDispositivo.php, app/Services/Monitoreo/DispositivoService.php, bootstrap/app.php</files>
<action>Middleware en el grupo `web` (append, antes de los de negocio en orden lógico): si falta la cookie `towell_disp` genera UUID v4 y la agrega a la respuesta (5 años, HttpOnly, Lax, Secure si HTTPS). Resuelve/crea `SYSMonDispositivo` por Uuid (datos de `getDeviceInfo()`/`detectDeviceModel()`; `UaHash`). Si hay usuario autenticado: "touch" (`UltimaActividad`, `UltimaIp`, `UltimoUsuarioId`, `UltimaRuta` = nombre de ruta) como máximo cada `touch_cache_seg` por dispositivo, usando `Cache::add("mon:touch:{uuid}", 1, 30)` como candado; UPDATE con query builder. También actualiza `SYSMonSesion.UltimaActividad`. Todo en try/catch → `Log::warning`. Respetar kill switch. No aplicar a rutas `telemetria.*` el touch pesado (el latido ya actualiza).</action>
<verify>Tests: primera visita pone cookie; segunda no duplica dispositivo; 10 requests en < 30 s = 1 UPDATE; excepción simulada en el service no rompe la request (200).</verify>
<done>Dispositivo estable aunque cambie la IP.</done>
</task>

<task type="auto">
<name>11-01.4 Sesiones y accesos (MON-03, MON-04)</name>
<files>app/Listeners/Monitoreo/{RegistrarLogin,RegistrarLogout}.php, app/Services/Monitoreo/AccesoService.php, app/Http/Controllers/AuthController.php</files>
<action>Listener `Login`: crea `SYSMonSesion` (Origen `login` si `request()->routeIs('login')`/POST login, si no `recordarme`), cierra la sesión abierta previa del mismo dispositivo (`reemplazada`), guarda `mon_sesion_id` en sesión, `SYSMonAcceso` tipo `login`/`recordarme`. Listeners `Logout` y `CurrentDeviceLogout`: cierra sesión (`logout`) + acceso. En `AuthController::login`: aplicar `RateLimiter` `login` **antes** de validar la contraseña (si excede → acceso `bloqueo` + mensaje "Demasiados intentos, espera N segundos" con el mismo estilo de error actual); al fallar credenciales → acceso `login_fallido` con `NumeroEmpleado` tecleado y `Motivo` (`usuario_inexistente`\|`contrasena`), `RateLimiter::hit`; al éxito `RateLimiter::clear`. `logout()` → `Auth::logoutCurrentDevice()` (decisión D-F), luego invalidate/regenerateToken como hoy. **No** cambiar la lógica de contraseñas legacy ni el remember siempre activo.</action>
<verify>Feature tests: login OK crea sesión+acceso; login fallido registra y no crea sesión; 11º intento en 1 min → bloqueo; logout cierra sesión; logout no cambia `remember_token` del usuario (a diferencia de `Auth::logout()`); restauración por remember (simular cookie) registra `recordarme`. Tests existentes de login siguen verdes.</verify>
<done>Accesos completos en `SYSMonAcceso`.</done>
</task>

<task type="auto">
<name>11-01.5 Cierre remoto (MON-09)</name>
<files>app/Http/Middleware/Monitoreo/AplicarCierreRemoto.php, app/Services/Monitoreo/CierreRemotoService.php, bootstrap/app.php</files>
<action>Según contrato §5. `CierreRemotoService::solicitar(MonDispositivo, Usuario $admin)` (lo usará el panel de fase 13) y `aplicarSiCorresponde(Request)`. Middleware en grupo web después de la autenticación de sesión: HTML → redirect a `login` con flash; `expectsJson()`/`X-Livewire` → 401 JSON `{message, code: 'cierre_remoto'}`. Registrar `logout_remoto` y `admin_accion` (con `ActorId`).</action>
<verify>Feature test con dos "dispositivos" (dos cookies distintas) del mismo usuario: se solicita cierre para A → la siguiente request de A queda deslogueada; B sigue autenticado.</verify>
<done>Cierre remoto por dispositivo operativo.</done>
</task>

<task type="auto">
<name>11-01.6 Rutas admin placeholder (MON-21 parcial)</name>
<files>routes/modules/admin.php, routes/web.php, resources/views/modulos/admin/index.blade.php</files>
<action>`routes/modules/admin.php` con prefijo `/admin`, nombre `admin.`, middleware `can:admin`, ruta `GET /admin` → vista placeholder que extiende `layouts.app` ("Panel de administración — en construcción (fase 13)"). `require` en `routes/web.php` dentro del grupo `auth`.</action>
<verify>Feature test: Sistemas → 200; otra área → 403; invitado → redirect login.</verify>
<done>Superficie `/admin` protegida lista para la fase 13.</done>
</task>

</tasks>

<verification>
php artisan test --filter=Monitoreo · php artisan test (suite completa sin regresiones) · npm run build (no debería cambiar) · security-review + code-review sobre el diff.
</verification>

<deliverables>
`11-01-SUMMARY.md`: tablas creadas, cómo desplegar (migración o `.sql` por DBA, `MONITOREO_AREAS_ADMIN`, valor real de `area` a verificar con `SELECT DISTINCT area FROM SYSUsuario`), pruebas, decisiones.
</deliverables>
