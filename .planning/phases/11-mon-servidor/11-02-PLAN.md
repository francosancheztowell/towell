---
phase: 11-mon-servidor
plan: "02"
wave: 0
depends_on: ["11-01"]
autonomous: true
requirements: [MON-05, MON-06, MON-07, MON-08, MON-10, MON-11, MON-13]
branch: claude/11-mon-servidor
contract: .planning/phases/11-mon-servidor/11-CONTRACT.md
files_modified:
  - app/Http/Middleware/Monitoreo/ServerTiming.php
  - app/Services/Monitoreo/ErrorRecorder.php
  - app/Services/Monitoreo/ErrorTelegramNotifier.php
  - app/Http/Middleware/Monitoreo/CapturarRespuesta5xx.php
  - app/Http/Controllers/Monitoreo/TelemetriaController.php
  - app/Http/Requests/Monitoreo/*.php
  - routes/modules/telemetria.php
  - routes/web.php
  - bootstrap/app.php
  - app/Support/Http/Concerns/HandlesApiErrors.php (solo agregar report())
  - app/Models/Sistema/SYSMensaje.php
  - app/Http/Controllers/Configuracion/MensajesController.php (o donde viva)
  - resources/views/**/mensajes.blade.php
  - database/migrations/2026_09_24_000002_add_errores_sistema_to_sysmensajes.php
  - resources/views/errors/500.blade.php
  - resources/views/components/layout-head.blade.php (solo 3 <meta>)
  - tests/Feature/Monitoreo/*.php
must_haves:
  truths:
    - "El recorder de errores nunca lanza ni entra en recursión."
    - "Un error dentro de una transacción de negocio queda registrado aunque la transacción se revierta."
    - "Los endpoints de telemetría nunca devuelven 500."
    - "No se guardan contraseñas, payloads ni query strings."
  artifacts:
    - "Errores PHP y 5xx agrupados por huella con eventos."
    - "Endpoints de telemetría del contrato."
    - "Alerta Telegram en error nuevo/regresión."
---

<objective>
Captura de rendimiento del servidor y de errores (excepciones reportadas y respuestas 5xx que hoy se tragan los `catch`), endpoints de telemetría que usará el cliente (fase 12), alertas Telegram configurables y página 500 con código de referencia.
</objective>

<execution_context>
- Mismo contexto que `11-01-PLAN.md`; contrato §1, §4, §9.
- H5: ~401 `catch` devuelven `$e->getMessage()` con 500 y **no pasan por el handler** → hace falta el middleware `CapturarRespuesta5xx` además de `report()`.
- H6: si el INSERT del error va por la conexión `sqlsrv` dentro de una transacción abierta, se revierte con ella → usar `sqlsrv_monitoreo` (registrada en 11-01.1).
- `HandlesApiErrors` (`app/Support/Http/Concerns/HandlesApiErrors.php`) ya genera `trace_id`; solo agregar `report($e)` en su camino de error (sin cambiar la respuesta).
- `ParoTelegramNotifier` es el patrón a copiar para Telegram.
- `window.fetch` ya está parchado por `programa-tejido/index.js` y `crudo/dashboard.ts`: el servidor no depende de ningún parche cliente.
</execution_context>

<tasks>

<task type="auto">
<name>11-02.1 ErrorRecorder (MON-06)</name>
<files>app/Services/Monitoreo/ErrorRecorder.php, bootstrap/app.php</files>
<action>`$exceptions->report(fn (Throwable $e) => app(ErrorRecorder::class)->capturar($e))` sin impedir el log normal (no `->stop()`). Ignorar `ValidationException`, `AuthenticationException`, `AuthorizationException`, `TokenMismatchException`, `NotFoundHttpException`, `ModelNotFoundException`, `ThrottleRequestsException` (configurable). Huella según contrato §2. Upsert en `SYSMonError` (Ocurrencias++, UltimaVez; `resuelto`→`nuevo` = regresión) + `SYSMonErrorEvento` (respetando tope diario) por la conexión `sqlsrv_monitoreo`. Guard estático anti-recursión; `try/catch (Throwable)` total → `Log::warning`. Saneado: `QueryException` → solo SQL con placeholders; mensaje ≤ 1000; traza ≤ 8 KB con nombres de llaves de input (nunca valores). Devuelve el Id del evento (para la página 500 y SEC-04).</action>
<verify>Tests: dos excepciones iguales = 1 error con Ocurrencias=2; excepción dentro de `DB::transaction` que se revierte sigue registrada; si la tabla no existe, la request devuelve su error normal y no hay recursión; contraseña en input no aparece en ninguna columna.</verify>
<done>Errores PHP agrupados.</done>
</task>

<task type="auto">
<name>11-02.2 5xx sin handler y HandlesApiErrors (MON-07)</name>
<files>app/Http/Middleware/Monitoreo/CapturarRespuesta5xx.php, app/Support/Http/Concerns/HandlesApiErrors.php, bootstrap/app.php</files>
<action>Middleware web: si la respuesta es ≥ 500 y en esta request no se reportó nada (flag en el container que pone el recorder), registrar un error `origen=http5xx` con huella por `ruta|status|mensaje JSON normalizado` (leer `message`/`error` del JSON si existe, truncado). `HandlesApiErrors`: agregar `report($e)` donde construye la respuesta de error.</action>
<verify>Test con ruta de prueba (solo en test) que hace `catch → response()->json(['message'=>$e->getMessage()],500)` → queda fila `http5xx`.</verify>
<done>Los errores tragados también se ven.</done>
</task>

<task type="auto">
<name>11-02.3 ServerTiming (MON-05)</name>
<files>app/Http/Middleware/Monitoreo/ServerTiming.php, bootstrap/app.php</files>
<action>Solo GET con respuesta HTML: medir duración total (desde `LARAVEL_START`) y consultas (`DB::listen` acumulando conteo y ms solo durante esta request). Agregar header `Server-Timing: app;dur=X, db;dur=Y;desc="N q"`. Costo despreciable; kill switch.</action>
<verify>Test: GET HTML trae el header; JSON no.</verify>
<done>El cliente puede leer `serverTiming` por Navigation Timing.</done>
</task>

<task type="auto">
<name>11-02.4 Endpoints de telemetría (MON-08) y señales HTML</name>
<files>app/Http/Controllers/Monitoreo/TelemetriaController.php, app/Http/Requests/Monitoreo/*.php, routes/modules/telemetria.php, routes/web.php, resources/views/components/layout-head.blade.php</files>
<action>Rutas y cuerpos exactamente como contrato §4 (nombres `telemetria.*`), `withoutMiddleware([SetSqlContextInfo::class, ProgramaTejidoContext::class])`, `throttle:telemetria`. `latido`: actualiza dispositivo/sesión (Visible, InactivoSeg, VersionFront, Pantalla, UltimaRuta) y responde `{cerrar, intervalo}` (cerrar = llave `mon:cerrar:{uuid}`). `vista`: INSERT `SYSMonVista` (idempotente por Uuid). `vista/{uuid}/fin`: acepta JSON o FormData (beacon con `_token`). `error`: pasa por `ErrorRecorder::capturarCliente()` (misma huella/tope; origen js/livewire/red). `dispositivo/nombre`: guarda `Nombre`. Truncar en vez de rechazar; nunca 500 (try/catch → 204). En `layout-head`: `<meta name="towell-ruta">`, `<meta name="towell-version">` (hash del manifest de Vite si existe), `<meta name="towell-telemetria" content="1">` solo si enabled + auth.</action>
<verify>Feature tests por endpoint (auth requerida, CSRF, truncado, idempotencia de vista, beacon FormData, cerrar=true tras solicitar cierre, kill switch → 204 sin escribir).</verify>
<done>Contrato servidor completo para la fase 12.</done>
</task>

<task type="auto">
<name>11-02.5 Telegram de errores (MON-10, MON-11)</name>
<files>database/migrations/2026_09_24_000002_add_errores_sistema_to_sysmensajes.php, database/sql/sysmon_sysmensajes.sql, app/Models/Sistema/SYSMensaje.php, MensajesController, mensajes.blade.php, app/Services/Monitoreo/ErrorTelegramNotifier.php</files>
<action>Columna bit `ErroresSistema` default 0 en `SYSMensajes` (migración idempotente + `.sql`). Agregarla a `columnasModuloPermitidas()`, `$fillable`, validación/guardado en `MensajesController` y checkbox/columna en `mensajes.blade.php` siguiendo exactamente cómo están las demás columnas. `ErrorTelegramNotifier` copiando `ParoTelegramNotifier` (5 s timeout, nunca lanza, texto plano, log de resultado); invocado con `dispatch(fn () => ...)->afterResponse()` solo en huella nueva o regresión y con tope global `telegram_max_hora` (contador en cache). Link a `url('/admin/errores/'.$id)`.</action>
<verify>Tests con `Http::fake()`: error nuevo → 1 envío por chat; repetido → 0; tope/h respetado; fallo de Telegram no afecta la request.</verify>
<done>Sistemas recibe alertas de errores nuevos.</done>
</task>

<task type="auto">
<name>11-02.6 Página 500 con referencia (MON-13)</name>
<files>resources/views/errors/500.blade.php</files>
<action>Quitar la frase falsa "Nuestro equipo técnico ha sido notificado" y mostrar "Código de referencia: #<id del evento>" cuando exista (el recorder deja el Id en el container). No rediseñar (el estilo lo hace UX-09).</action>
<verify>Test: error forzado en ruta de prueba → HTML 500 contiene el código.</verify>
<done>El usuario puede dar un código que Sistemas busca en `/admin/errores`.</done>
</task>

</tasks>

<verification>
php artisan test --filter=Monitoreo · php artisan test completo · security-review (endpoints, saneado de datos) · code-review.
</verification>

<deliverables>
`11-02-SUMMARY.md` + actualizar `11-CONTRACT.md` **solo** si hubo que cambiar algo (y avisar en SUMMARY para que el integrador lo propague a las fases 12/13).
</deliverables>
