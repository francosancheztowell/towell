---
phase: 12-mon-cliente
plan: "01"
wave: 1
depends_on: [11-mon-servidor]
autonomous: true
requirements: [MON-15, MON-16, MON-17, MON-18, MON-19, MON-20, MON-21 (solo enlace "Admin")]
files_modified:
  - resources/js/monitoreo/telemetria.ts
  - resources/js/monitoreo/cliente.ts
  - resources/js/monitoreo/dispositivo.ts
  - resources/js/app.js (1 línea de import)
  - resources/views/components/navbar/sections/user-modal.blade.php
  - tests/Js/monitoreo-cliente.test.mjs
  - tests/Js/monitoreo-dispositivo.test.mjs
  - tests/Js/monitoreo-presupuesto.test.mjs
must_haves:
  truths:
    - "Sin <meta name=\"towell-telemetria\" content=\"1\"> el cliente no hace ninguna request (kill switch)."
    - "El cliente nunca rompe la página: todo va en try/catch y los fallos de telemetría no se reportan a sí mismos."
    - "No se parchea window.fetch; los fallos se escuchan con towell:http-error y Livewire.hook('request')."
    - "El ratchet no sube: sin fetch( ni X-CSRF-TOKEN nuevos (se usa window.axios, que ya lleva el CSRF de bootstrap.js) y el <script> inline de user-modal se va a TS (baja 1)."
    - "Bundle del cliente ≤ 5 KB gzip, verificado por test."
  artifacts:
    - "Cliente TS con latido, vistas (carga/suave), errores, conexión y nombre de dispositivo."
    - "Tests node --test con axios/sendBeacon/DOM falsos."
    - "Enlace 'Admin' en el modal de usuario solo con @can('admin')."
  key_links:
    - "11-CONTRACT.md §3–§6 -> endpoints /telemetria/*, metas, eventos towell:http-error / towell:conexion"
    - "app/Http/Controllers/Monitoreo/TelemetriaController.php -> nombres de campos reales (incl. `version` en error)"
    - "resources/js/crudo/dashboard.ts:271 -> patrón Livewire.hook('request', ({ fail }) => …)"
---

<objective>
Script cliente liviano que reporta latido, vistas con tiempos de carga, errores del navegador y nombre de dispositivo contra los endpoints `/telemetria/*` ya integrados (fase 11), sin afectar la experiencia ni el rendimiento.
</objective>

<execution_context>
TS con sintaxis borrable (los tests node importan `.ts`). Transporte inyectado: el núcleo (`cliente.ts`) recibe `post(url, body)`; la entrada (`telemetria.ts`) lo cablea a `window.axios` (ya en el bundle, ya con `X-CSRF-TOKEN`), así no se toca `utils/**` (sesión 15-01 en paralelo) ni se agregan `fetch(`. `sendBeacon` para el cierre de vista (`FormData{_token, visibleMs}`). Sin SQL Server: verificación real en sqlite local con la skill `run`.
</execution_context>

<tasks>

<task type="auto">
<name>12.1 Núcleo: arranque, latido y conexión (MON-15, MON-18)</name>
<files>resources/js/monitoreo/cliente.ts, resources/js/monitoreo/telemetria.ts, resources/js/app.js</files>
<action>`telemetria.ts` se importa con una línea desde `app.js`; si falta el meta `towell-telemetria=1` sale sin registrar nada (guard contra doble arranque con wire:navigate). Latido `POST /telemetria/latido {vista, ruta, visible, inactivoSeg, version, pantalla}`: primero tras registrar la vista, luego cada `intervalo` que devuelva el servidor (default 60 s, acotado 15–900 s); en `visibilitychange` se envía y se reprograma. Inactividad = segundos desde el último `pointerdown/keydown/touchstart/scroll` (listeners pasivos). `cerrar:true` → `location.reload()`. Conexión: fallo sin respuesta HTTP → `towell:conexion {online:false}`; primer latido OK después → `{online:true}`; también `online`/`offline` del navegador (solo se emite en cambio de estado; `online` fuerza un latido).</action>
<verify>Tests con reloj y transporte falsos: intervalo del servidor respetado, visibilitychange reprograma, `cerrar` recarga, eventos de conexión solo en transición.</verify>
<done>El dispositivo aparece en línea en `SYSMonDispositivo` con `Visible`/`InactivoSeg`/`VersionFront`/`Pantalla`.</done>
</task>

<task type="auto">
<name>12.2 Vistas con tiempos (MON-16)</name>
<files>resources/js/monitoreo/cliente.ts</files>
<action>Tras `load` (o de inmediato si ya ocurrió, en el siguiente tick para que exista `loadEventEnd`): `POST /telemetria/vista {uuid, tipo:'carga', ruta (meta towell-ruta), url (solo pathname), nav:{ttfb, dom, carga, kb}, st:{app, db, q}}` desde Navigation Timing L2 + `serverTiming` (`q` sale de `desc="<n> q"`). UUID: `crypto.randomUUID()` → fallback `getRandomValues` (v4) → fallback `Math.random` (la LAN no es contexto seguro). Tiempo visible acumulado solo con la pestaña visible; `pagehide` → `sendBeacon('/telemetria/vista/{uuid}/fin', FormData{_token, visibleMs})`. `livewire:navigated` (ignorando el disparo inicial) cierra la vista actual y abre una `tipo:'suave'` sin tiempos de navegación.</action>
<verify>Tests: payload exacto de carga con serverTiming, uuid v4 con y sin randomUUID, beacon en pagehide con visibleMs que excluye tiempo oculto, navegación suave cierra y abre.</verify>
<done>Filas en `SYSMonVista` con `TtfbMs/DomMs/CargaMs/Kb/ServidorMs/ConsultasN` y `Fin/VisibleMs` al salir.</done>
</task>

<task type="auto">
<name>12.3 Errores del navegador (MON-17)</name>
<files>resources/js/monitoreo/cliente.ts</files>
<action>`window` `error` (ignora `Script error.`, fuentes `chrome-extension:`/`moz-extension:`/`safari-extension:` y `ResizeObserver loop`), `unhandledrejection`, `towell:http-error {status,url,method}` y `Livewire.hook('request', ({ fail }) => …)` (instalado en `livewire:init` o de inmediato si `window.Livewire` ya existe). Decisión: de HTTP/Livewire solo se reportan status 0 (red, y solo si `navigator.onLine`) y ≥ 500; 4xx (422/419/403) es flujo normal. Nunca se reportan URLs `/telemetria/*` (evita bucles). Dedupe por mensaje+fuente y máximo 10 por página. Payload: `{origen, mensaje, fuente, linea, col, stack (≤ 8 KB), url, vista, status, metodo, version}`.</action>
<verify>Tests: filtros, dedupe, tope de 10, origen correcto por canal, sin bucle por fallos de telemetría.</verify>
<done>Un `throw` en consola crea/incrementa una fila en `SYSMonError` (origen `js`).</done>
</task>

<task type="auto">
<name>12.4 Nombre de dispositivo y enlace Admin (MON-19, MON-21)</name>
<files>resources/js/monitoreo/dispositivo.ts, resources/views/components/navbar/sections/user-modal.blade.php</files>
<action>El Blade lee el `Nombre` guardado en `SYSMonDispositivo` por la cookie `towell_disp` (una lectura por índice único, dentro de `Monitoreo::seguro`, solo con monitoreo activo) y lo pasa en `data-*`. El `<script>` inline se mueve a `dispositivo.ts` (edición del nombre + IP por WebRTC tal cual): al guardar → `POST /telemetria/dispositivo/nombre {nombre ≤ 80}`; migración única: si el servidor no tiene nombre y existe `localStorage['device_name_<hash>']`, se envía y, con éxito, se borra la llave. Con el kill switch apagado el nombre solo se muestra (sin editar ni requests). Enlace "Admin" (`route('admin.index')`) dentro de `@can('admin')`.</action>
<verify>Tests de migración/guardado con DOM y storage falsos; `php artisan test` sin regresiones en vistas.</verify>
<done>El nombre sobrevive a limpiar el navegador (vive en servidor) y el panel de fase 13 lo muestra.</done>
</task>

<task type="auto">
<name>12.5 Presupuesto, checks y verificación real (MON-20)</name>
<files>tests/Js/monitoreo-presupuesto.test.mjs, .planning/phases/12-mon-cliente/12-01-SUMMARY.md</files>
<action>Test que empaqueta `telemetria.ts` con esbuild (minify, axios externo) y exige ≤ 5 KB gzip. Checks: `php artisan test`, `npm run typecheck && npm run test:js && npm run build`, `npm run ratchet` (fijar la baja con `--update`), `phpstan`, Pint sobre archivos tocados (ninguno PHP), skill `code-review`. Skill `run`: login en sqlite local → navegar → filas en `SYSMonVista`; `throw` en consola → `SYSMonError`; meta ausente → 0 requests a `/telemetria`.</action>
<verify>Salidas de los checks y consultas sqlite pegadas en el SUMMARY.</verify>
<done>Rama `claude/12-mon-cliente` pusheada con SUMMARY (y HANDOFF solo si hizo falta algo ajeno). Sin PR.</done>
</task>

</tasks>
