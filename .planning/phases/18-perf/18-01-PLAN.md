---
phase: 18-perf
plan: "01"
wave: 2
depends_on: [11-mon-servidor, 12-mon-cliente, 14-mon-pulse]
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06, PERF-07, HANDOFF-12-§1]
files_modified:
  - config/cache.php
  - .env.example
  - docs/cerebro-towell/Runbooks/deploy.md
  - docs/cerebro-towell/Runbooks/00-Indice-runbooks.md
  - app/Helpers/permission-helpers.php
  - app/Providers/AppServiceProvider.php
  - app/Http/Middleware/SetSqlContextInfo.php
  - app/Services/Monitoreo/EstadoRequest.php
  - app/Services/Monitoreo/ErrorRecorder.php
  - app/Http/Controllers/Monitoreo/TelemetriaController.php
  - resources/js/monitoreo/cliente.ts
  - tests/Feature/Perf/*
  - tests/Feature/Monitoreo/ErroresTest.php (o TelemetriaTest.php)
  - tests/Js/monitoreo-cliente.test.mjs
must_haves:
  truths:
    - "Producción usa `file` para cache y sesión; defaults del código y .env.example dicen lo mismo."
    - "moduleNameForRoute no consulta SYSRoles más de una vez por ruta y hora, y se invalida al escribir SYSRoles."
    - "Lazy loading y requests con > 500 ms de consultas quedan en el log, nunca lanzan."
    - "SetSqlContextInfo sigue sellando el contexto en cada request web; solo se mide."
    - "SYSMonError.Ruta de un error de navegador es la ruta de la página, nunca `telemetria.error`."
  artifacts:
    - "Runbook de despliegue con OPcache, realpath_cache, optimize/rollback y el acumulado de fases 10–15 + PT."
    - "Test que cuenta queries de moduleNameForRoute antes/después."
    - "18-01-SUMMARY.md con números y la propuesta de PERF-07."
  key_links:
    - "permission-helpers.php -> ModuloService (prefijo modulos_v3_<env>, TTL 3600)"
    - "SetSqlContextInfo -> Server-Timing `ctx;dur=` + Pulse"
    - "cliente.ts reportar() -> meta towell-ruta -> TelemetriaController::error -> ErrorRecorder"
---

<objective>
Infraestructura de rendimiento sin cambiar comportamiento de negocio: decidir y documentar drivers, dejar el
runbook de despliegue (OPcache, optimize, rollback), quitar consultas repetidas de `moduleNameForRoute`,
hacer visibles lazy loading y requests con mucho tiempo en BD, medir el costo de `SetSqlContextInfo`, y
arreglar la Ruta de los errores de cliente en el monitoreo (HANDOFF 12 §1).
</objective>

<execution_context>
Sin SQL Server en la nube: todo se prueba en sqlite (`phpunit.xml`); lo que requiere números de producción
(OPcache, costo real de `sp_SetAppContext`) queda como runbook para el owner en Laragon. Propiedad de
archivos según `SESIONES-OLA-2.md` fila 18-01. `AppServiceProvider`: solo agregar en `boot`; las 2 `use` de
Desarrolladores son de 20-01.
</execution_context>

<tasks>

<task type="auto">
<name>1. PERF-01 drivers (decisión del owner: `file`)</name>
<files>config/cache.php, .env.example, docs/cerebro-towell/Runbooks/deploy.md</files>
<action>`config/cache.php` default `database` → `file` (no hay migración de tabla `cache`; hoy un `.env` sin
`CACHE_STORE` rompería). `config/session.php` ya es `file`. `.env.example` ya dice `file` para ambos: se
agrega comentario de la decisión (un servidor Windows, sin Redis ni proxy) y cuándo revisarla (segundo
servidor web). `QUEUE_CONNECTION` fuera de alcance: se documenta lo que hay.</action>
<verify>Test: sin `CACHE_STORE` el default es `file`.</verify>
<done>Decisión escrita en el runbook y en el SUMMARY.</done>
</task>

<task type="auto">
<name>2. PERF-02/03 runbook de despliegue</name>
<files>docs/cerebro-towell/Runbooks/deploy.md, 00-Indice-runbooks.md</files>
<action>Checklist `php.ini` de Laragon: `opcache.enable=1`, `memory_consumption=256`,
`interned_strings_buffer=16`, `max_accelerated_files=20000`, `validate_timestamps` (1 + `revalidate_freq=2` si
se hace `git pull` en caliente; 0 solo con reinicio de Apache/php-cgi en cada deploy), `realpath_cache_size=4096K`,
`realpath_cache_ttl=600`; cómo verificarlo (`php -i`, `opcache_get_status()`). Deploy: `php artisan optimize`
(en Laravel 12 ya incluye config, **event**, route y view cache: se verifica con la salida del comando aquí) y
`optimize:clear` como primer paso del rollback. `npm run update` ya hace `optimize:clear && optimize`: no se
toca `package.json` (sin HANDOFF). Sección "despliegue acumulado" con los pasos de STATE.md Pending Todos
(fases 10–15, PT 01.1/02) + lo de esta fase.</action>
<verify>`php artisan optimize` y `optimize:clear` corren limpios en local (route:cache con el alias de Livewire del provider).</verify>
<done>Runbook enlazado desde el índice.</done>
</task>

<task type="auto">
<name>3. PERF-04 memoizar moduleNameForRoute</name>
<files>app/Helpers/permission-helpers.php, app/Providers/AppServiceProvider.php, tests/Feature/Perf/ModuleNameForRouteTest.php</files>
<action>Mismas 3 consultas (misma semántica SQL: collation, `LIKE`, `LEN`), envueltas en: (a) memo por
request en el contenedor (patrón de `userPermissions`); (b) `Cache::remember` con llave
`modulos_v3_<env>_modulo_ruta_<ver>_<sha1(ruta)>`, TTL 3600 (los de `ModuloService`), guardando también el
"no encontrado". Invalidación: `SYSRoles::saved/deleted` (ModulosController escribe con Eloquent) rota
`<ver>`, registrado en `AppServiceProvider::boot`. No se toca `ModuloService` (solo lectura).</action>
<verify>Test que cuenta queries: antes (sin caché) 1–3 por llamada; después 0 en la 2.ª llamada del mismo request
y 0 en un request nuevo; tras `SYSRoles::create` vuelve a consultar y resuelve el módulo nuevo. Mismos
resultados que antes para exacta, prefijo, última parte y null.</verify>
<done>Número antes/después en el SUMMARY (queries y ms en sqlite).</done>
</task>

<task type="auto">
<name>4. PERF-05/06 lazy loading y tiempo de BD por request → log</name>
<files>app/Providers/AppServiceProvider.php, tests/Feature/Perf/ObservabilidadConsultasTest.php</files>
<action>`Model::preventLazyLoading(! isProduction())` + `handleLazyLoadingViolationUsing` → `Log::warning`
una vez por modelo+relación por request (sin throw). `whenQueryingForLongerThan(500)` por conexión, registrado
en el listener existente de `ConnectionEstablished` (cubre sqlsrv, TI_PRO, TOW_PRO), que loguea ruta, conexión,
ms totales y n.º de consultas. Es el **acumulado por request**, que Pulse no tiene (su SlowQueries es por
consulta individual): no se duplica.</action>
<verify>Tests: acceso lazy en sqlite escribe 1 warning y no lanza; en producción no se activa; umbral superado
escribe 1 warning por conexión.</verify>
<done>Suite completa verde (ningún test existente pasa a lanzar).</done>
</task>

<task type="auto">
<name>5. PERF-07 medir SetSqlContextInfo</name>
<files>app/Http/Middleware/SetSqlContextInfo.php, app/Services/Monitoreo/EstadoRequest.php, tests/Feature/Perf/SetSqlContextInfoTest.php</files>
<action>Sin cambiar semántica (se sigue sellando siempre): cronometrar el `EXEC` (incluye abrir la conexión
física si es la primera consulta) y agregar `ctx;dur=<ms>` al header `Server-Timing` de la respuesta
(se anexa al que pone `ServerTiming`; visible en DevTools también para polls de Livewire) y
`Pulse::record('contexto_sql', método, ms)->avg()->max()->count()` para agregados por hora sin tocar Pulse
UI. Runbook: consulta a `pulse_aggregates` para sacar promedio/máximo y comparar con `app;dur`.</action>
<verify>Test con conexión que reporta driver `sqlsrv` simulada: statement llamado igual que antes y header con `ctx`.
En sqlite nada cambia.</verify>
<done>Propuesta con números (overhead de la medición en local + criterios para decidir con números de prod) en el SUMMARY.</done>
</task>

<task type="auto">
<name>6. Fix MON: Ruta de errores de cliente</name>
<files>TelemetriaController.php, ErrorRecorder.php, resources/js/monitoreo/cliente.ts, tests</files>
<action>Servidor: `error()` lee `ruta` (`$request->ruta()`) y `version`; si falta `ruta`, la `Ruta` de la
`SYSMonVista` del uuid `vista` (del mismo usuario); si no, `'desconocida'`. `capturarCliente` recibe `ruta` y
`registrar` la usa en lugar del nombre de la request (`telemetria.error`). Filas ya existentes con
`Ruta = 'telemetria.error'` se corrigen en su siguiente ocurrencia. **Huella: Ruta NO entra** (el mismo defecto
de un bundle compartido en N páginas es un solo error; las huellas existentes no se parten tras el deploy; la
página de cada ocurrencia queda en `SYSMonErrorEvento.Url`). Cliente: `ruta: meta('towell-ruta') || undefined`
en `reportar()`, leído en el momento del error (Livewire `navigate` reemplaza los `<meta>` del head antes de
`livewire:navigated`, verificado en `livewire.esm.js::mergeNewHead`).</action>
<verify>PHP: ruta del cuerpo; fallback por vista; `desconocida`; nunca `telemetria.error`; huella igual con distinta
ruta. JS: `ruta` en el cuerpo y actualizada tras cambiar el meta + `livewire:navigated`; presupuesto ≤ 5 KB gzip.
Con la skill run: `throw` en consola en una página → `SYSMonError.Ruta` = ruta de esa página (sqlite local).</verify>
<done>HANDOFF 12 §1 cerrado.</done>
</task>

<task type="auto">
<name>7. Checks y entrega</name>
<action>SessionStart hook; `php artisan test`; `phpstan`; `pint --test` en PHP tocado; `npm run typecheck && npm run test:js
&& npm run build`; `npm run ratchet`; skill code-review; 18-01-SUMMARY.md; commits "rendimiento: …" / "monitoreo: …";
push a `claude/18-01-perf-infra`. Sin PR.</action>
</task>

</tasks>
