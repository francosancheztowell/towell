---
phase: 19-modulos
plan: "03"
wave: 3
depends_on: [15-02, 16-01, 20-03]
autonomous: false
requirements: [MIG-ATA-01, MIG-ATA-02, MIG-ATA-03, MIG-ATA-04, PERF-08, SEC-07, UX-18]
files_modified:
  - routes/modules/atadores.php
  - app/Http/Controllers/Atadores/** (AtadoresController, AtaDevolucionesController, catálogos, ReportesAtadoresController)
  - app/Services/Atadores/** (ProgramaAtadoresListado, siembra del checklist)
  - app/Models/Atadores/AtaComentariosModel.php
  - resources/views/modulos/atadores/** (7 vistas con JS inline)
  - resources/views/modulos/catalogos-atadores/** (llave de Comentarios)
  - resources/js/modulos/atadores/** (nuevo) y resources/js/modulos/catalogos-atadores/index.ts
  - database/sql/atadores_comentarios_id.sql (nuevo, lo corre el owner)
  - tests/Feature/Atadores*, tests/Js/atadores-*
  - scripts/ratchet-baseline.json (solo bajar), phpstan-baseline.neon (solo quitar)
must_haves:
  truths:
    - "Ninguna vista de Atadores lleva más de 50 líneas de JS inline; el JS vive en resources/js/modulos/atadores/<pantalla>/index.ts."
    - "Mismo diseño: el HTML (clases, textos, orden) no cambia salvo onclick → data-accion y el bloque <script>."
    - "El tablero del programa no pide nada con la pestaña oculta y nunca vuelve a bajar el HTML para refrescar estatus."
    - "Iniciar y autorizar un atado hacen un número fijo de consultas, sin importar cuántas máquinas/actividades tenga el catálogo."
    - "Ningún JSON del módulo devuelve getMessage() de una excepción al usuario."
    - "action=supervisor en atadores/save audita registrar,45 (modo auditar: no bloquea)."
    - "Un comentario con '/' se edita y se elimina; las URLs /atadores/catalogos/comentarios/{nota1} siguen funcionando."
  artifacts:
    - "Números antes/después: consultas por iniciar/autorizar, bytes/min y peticiones/min del tablero (visible y oculto)."
    - "Capturas antes/después a 768×1024 (tablet) de programa y calificar (Jacquard y Karl Mayer)."
    - "19-03-SUMMARY.md y HANDOFF.md."
  key_links:
    - "19-CONTEXT.md (receta; 19-00-RECETA.md no existe en la base)"
    - "20-03-MAPA-AUTHZ.md §Huecos y §Pendientes"
    - "16-componentes/HANDOFF.md C3"
    - "docs/cerebro-towell/Arquitectura/livewire-cuando-si-cuando-no.md (Atadores: no Livewire)"
---

<objective>
Sacar las 2 751 líneas de JS inline de Atadores (7 vistas) a módulos TS con `http`/`notify`/`dom`/`format`, cerrar el N+1 de la siembra del checklist, quitar `getMessage()` de las respuestas del módulo, auditar la autorización del supervisor y dar a Comentarios una llave que aguante cualquier texto. Sin cambios de diseño.
</objective>

<context>
Situación medida en la base (`98e6e5e`):

| Vista | JS inline | fetch | Swal.fire | onclick= | X-CSRF |
|---|---:|---:|---:|---:|---:|
| calificar-atadores/index | 1 620 | 15 | 52 | 4 (+11 on*) | 12 |
| programaAtadores/index | 647 | 1 | 5 | 9 | 0 |
| calificar-atadores/_proceso-km | 148 | | | | |
| reportes/atadores | 149 | | | | |
| reportes/km | 83 | | | | |
| reportes/programa | 59 | | | | |
| calificar-atadores/proceso-km | 45 | | | | |

- **Polling del programa.** El "HTML completo cada 5 s aunque la pestaña esté oculta" es el estado de `d6bffcb` (22-09). En `9cf2e50` (24-09) ya pasó a `GET programaaatadores/estatus` (JSON) cada 15 s y se salta con `document.hidden`, pero sin medición y todavía inline. Aquí se mide antes (`d6bffcb`) contra después, se lleva al TS y se refresca al volver a la pestaña. `estatus` hoy corre la misma consulta de 23 columnas que el tablero para devolver `id` + `status`: se proyecta.
- **Livewire:** la guía dice "Atadores: no migrar ahora" (OEE file-heavy, ciclo Activo→Autorizado). Se queda en JSON + `http`.
- **N+1:** `sembrarMaquinasYActividades()` hace `all()` ×2 + un `exists()` y un `create()` por máquina y por actividad; `save(action=supervisor)` repite `all()` ×2 con `create()` por faltante; `calificarAtadores()` crea faltantes uno por uno.
- **SEC-07:** 14 `getMessage()` hacia el usuario (AtadoresController ×3, AtaDevolucionesController ×2, catálogos ×9) + `['error' => $e->getMessage()]` y la ruta física del Excel en ReportesAtadoresController. Los catálogos además atrapan la `ValidationException` y la vuelven 500.
- **AuthZ:** `POST atadores/save` audita `modificar,45` para todas las acciones; `supervisor` debe auditar `registrar,45`. `oee/despachar` sigue sin idrol (espera al owner).
- **Comentarios:** `AtaComentarios` tiene `Nota1` como PK de texto libre; `catalog-base.ts` pone la llave en la ruta con `encodeURIComponent` → `%2F` → 404 (Apache con `AllowEncodedSlashes Off` ni siquiera llega a PHP). No hay evidencia en el repo de una columna `Id`.
</context>

<tasks>

<task type="auto">
<name>19-03.1 (p1, backend) N+1 de la siembra del checklist</name>
<files>app/Services/Atadores/ChecklistAtado.php (nuevo), AtadoresController.php, tests/Feature/AtadoresChecklistConsultasTest.php</files>
<action>Un servicio `ChecklistAtado::sembrar($noJulio, $noProduccion, $turno)`: lee el catálogo con `pluck` (2 consultas), lee lo ya sembrado del folio (2 consultas) e inserta los faltantes en un `insert()` por tabla (en bloques bajo 2 100 parámetros). Lo usan `iniciarAtado`, `save(supervisor)` y los faltantes de `calificarAtadores`. Mismas columnas y valores que hoy (Estado 0, Porcentaje del catálogo, Turno).</action>
<verify>Test con 10 máquinas + 12 actividades cuenta consultas con `DB::listen` antes/después; idempotencia (segunda llamada no inserta).</verify>
<done>Consultas de la siembra: antes 2 + 2·(M+A) (46 con 10+12), después ≤ 6 fijas.</done>
</task>

<task type="auto">
<name>19-03.2 (p1) SEC-07 del módulo</name>
<files>AtadoresController, AtaDevolucionesController, Ata{Actividades,Comentarios,Maquinas}Controller, ReportesAtadoresController</files>
<action>`use HandlesApiErrors` y `apiErrorResponse()` (mensaje fijo + `trace_id`, excepción al log y a `/admin/errores`) en cada catch que hoy concatena `getMessage()`. En catálogos: `validate()` fuera del try (vuelve a ser 422 con errores) y `firstOrFail` fuera del try (404). El mensaje controlado de `fechaKm()` pasa a constante. Autorizar supervisor pasa de 200 `ok:false` a 500 con `trace_id` (el TS lo muestra).</action>
<verify>Tests: excepción forzada → cuerpo sin el texto de la excepción y con `trace_id`; alta inválida en catálogo → 422. Ratchet `getMessage() en response()->json` baja 14.</verify>
<done>0 `getMessage()` hacia el usuario en `app/Http/Controllers/Atadores/**`.</done>
</task>

<task type="auto">
<name>19-03.3 (p1) AuthZ del supervisor en modo auditar</name>
<files>AtadoresController::save, tests/Feature/AtadoresAutorizarSupervisorAuditaTest.php</files>
<action>Con `action=supervisor`, antes de validar nada, pasar por `EnsureModulePermission` con `registrar,45,auditar` (se reutiliza el middleware, no se toca). No bloquea. `oee/despachar` queda igual y en SIN_PERMISO_DE_MODULO hasta que el owner mande el idrol (si llega durante la sesión, se agrega `registrar,<idrol>,auditar`).</action>
<verify>Usuario con `modificar,45` sin `registrar` → 1 fila `authz_denegaria` con `registrar · 45`; la autorización sigue pasando. Con `registrar` → 0 filas.</verify>
<done>Hueco de 20-03 cerrado en modo auditar.</done>
</task>

<task type="checkpoint:decision">
<name>19-03.4 (p1) Llave de Comentarios</name>
<files>AtaComentariosController, AtaComentariosModel, CatalogosAtadoresVista, routes/modules/atadores.php, resources/js/modulos/catalogos-atadores/index.ts, database/sql/atadores_comentarios_id.sql</files>
<action>
- `.sql` idempotente 2008 R2: `IF COL_LENGTH('dbo.AtaComentarios','Id') IS NULL ALTER TABLE dbo.AtaComentarios ADD Id INT IDENTITY(1,1) NOT NULL` + índice único. Lo corre el owner; no se ejecuta aquí.
- Rutas nuevas `…/comentarios/id/{id}` (`whereNumber`) para show/update/destroy con los mismos permisos (151). Las `{nota1}` se quedan tal cual (URLs existentes no se rompen).
- Mientras la columna no exista (detectado una vez y cacheado), la vista sigue con `llave: Nota1` y el comportamiento de hoy; con la columna, `llave: Id` y endpoint `…/comentarios/id`. Store/update devuelven `data` para que la fila nueva tenga su Id (subclase mínima en `catalogos-atadores/index.ts`; `catalog-base.ts` no se toca).
</action>
<verify>Tests con y sin columna Id: nota con "/" se muestra, edita y elimina por Id; `{nota1}` sigue respondiendo.</verify>
<done>HANDOFF 16 C3 cerrado, condicionado a correr el .sql.</done>
</task>

<task type="auto">
<name>19-03.5 (p1) Estatus del programa proyectado + medición del polling</name>
<files>ProgramaAtadoresListado (`estatus()`), AtadoresController::estatus, tests</files>
<action>`estatus()` selecciona solo `id` y el CASE de estatus con los mismos joins y filtros por rol (mismo contrato JSON). Medir con datos sembrados en sqlite (N filas realistas): bytes de `index` (HTML) vs `estatus` (JSON), peticiones/min visible y oculta, tiempo de respuesta.</action>
<verify>Test de contrato: `estatus` devuelve los mismos `{id,status}` que las filas del tablero para cada rol/filtro.</verify>
<done>Tabla antes (`d6bffcb`: HTML/5 s, también oculta) / base (JSON/15 s) / después en el SUMMARY.</done>
</task>

<task type="auto">
<name>19-03.6 (p2) calificar-atadores → TS</name>
<files>resources/js/modulos/atadores/calificar/index.ts (+ módulos: guardado, devolucion, devolucion-km, checklist, acciones), calificar-atadores/index.blade.php</files>
<action>Receta 19-CONTEXT: datos por `<script type="application/json" id="datos-pagina">` (rutas, estatus, folio); `onclick/onchange/oninput/onblur/onsubmit` → `data-accion` + `delegate()`; `fetch` + X-CSRF → `http`; `Swal.fire` → `notify` (confirm/alert/validation/loading) salvo los formularios de calificar/autorizar que necesitan inputs (se quedan en Swal, importado del paquete); `innerHTML` con datos → `textContent`/`escapeHtml`. Lógica pura (validaciones de merma, formateo de fechas, armado de payloads de devolución) en `logica.ts` con tests node. Primero se mueve tal cual y luego se tipa.</action>
<verify>typecheck, test:js, build; con la skill run: Jacquard (checklist, merma, observaciones, devolución, terminar/calificar/autorizar) y Karl Mayer (montado/enhebrado, devolución KM) con 0 errores de consola.</verify>
<done>Vista con 0 líneas de JS inline salvo el JSON de datos.</done>
</task>

<task type="auto">
<name>19-03.7 (p3) programa atadores, proceso-km y reportes → TS</name>
<files>resources/js/modulos/atadores/{programa,proceso-km,reportes-atadores,reportes-km,reportes-programa}/index.ts, las 5 vistas</files>
<action>Programa: filtros/orden/menú contextual/selección a TS; `refreshStatus` con `http`, sin petición con la pestaña oculta y con refresco inmediato en `visibilitychange`; `Swal` → `notify`; los 9 `onclick` → `data-accion`. Filtro/orden puros con tests node. `_proceso-km` y `proceso-km` comparten un solo módulo (hoy duplican el guardado). Reportes: mismo patrón.</action>
<verify>run: tablero en tablet, filtros, orden, selección + Iniciar; pestaña oculta 60 s = 0 peticiones (Network), al volver 1 petición.</verify>
<done>Las 7 vistas < 50 líneas inline; ratchet baja (fetch −16, Swal −57, onclick −13, X-CSRF −12, innerHTML −6, `<script>` inline −7).</done>
</task>

<task type="auto">
<name>19-03.8 Cierre</name>
<action>Checks del protocolo (test, phpstan, typecheck, test:js, build, ratchet `--update` solo con bajas, pint en tocados), skill code-review, capturas antes/después a 768×1024, checklist UX-18 (17-02-CHECKLIST.md si ya existe; si no, la de 19-CONTEXT: foco, 12 px, 44 px de toque, sin clic derecho obligatorio), 19-03-SUMMARY.md y HANDOFF.md.</action>
</task>

</tasks>

<out_of_scope>
- SEC-06 (enforce): sigue en auditar.
- `OeeAtadoresFileService` (3 097 LOC) y `ActualizarOeeAtadoresJob` (app/Jobs no es del módulo; su `getMessage()` al archivo de estado va a HANDOFF).
- Menú contextual del programa sin clic derecho (UX-06 depende del helper global de 17-02): se deja el menú igual y se anota en HANDOFF para adoptarlo cuando exista.
- `userCan('crear','Programa Atadores')` por nombre en `ProgramaAtadoresListado`: se cambia a idrol 45 (mismo módulo, regla de 20-03).
</out_of_scope>

<preguntas_owner>
1. ¿`dbo.AtaComentarios` ya tiene una columna `Id` (identity)? Si sí, no hace falta el .sql y la llave por Id queda activa al desplegar.
2. idrol de "Reportes Atadores" para auditar `oee/despachar` (consulta en 20-03-MAPA-AUTHZ §Pendientes).
</preguntas_owner>
