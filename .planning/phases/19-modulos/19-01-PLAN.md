---
phase: 19-modulos
plan: "19-01"
wave: 3
depends_on: [15-01, 15-02, 16-01, 20-02, 20-03]
autonomous: true
requirements: [MIG-URD-01, MIG-URD-02, MIG-URD-03, MIG-URD-04, MIG-ENG-01, MIG-ENG-02, MIG-ENG-03, MIG-ENG-04, PERF-08, PERF-09, PERF-10, PERF-11, SEC-07, UX-18]
files_modified:
  - resources/views/modulos/engomado/**   # salvo programar-*
  - resources/views/modulos/urdido/**     # salvo programar-*
  - resources/views/catalogosurdido/**
  - resources/js/modulos/{urdido,engomado}/**
  - app/Http/Controllers/{Urdido,Engomado,UrdEngomado}/**   # salvo ProgramaUrdido/ProgramaEngomado
  - tests/Js/urdeng-*.test.mjs, tests/Feature/UrdEng/**, tests/Unit/ProduccionUrdidoScriptsBladeTest.php
  - scripts/ratchet-baseline.json (solo bajas), phpstan-baseline.neon (solo quitar)
must_haves:
  truths:
    - "Ninguna vista del módulo emite <script> inline propio ni onclick= (test guardián)."
    - "Ningún endpoint del módulo devuelve $e->getMessage() al usuario."
    - "Cada optimización de backend tiene número de queries antes/después en un test."
    - "El diseño en tablet no cambia salvo los bugs anotados."
    - "AuthZ sigue en modo auditar (sin enforce)."
  artifacts:
    - "19-00-RECETA.md para todas las 19-xx."
    - "Bundles resources/js/modulos/{urdido,engomado}/<pantalla>/index.ts + logica.ts con tests node."
    - "19-01-SUMMARY.md con capturas, números y puentes; HANDOFF.md."
  key_links:
    - "resources/js/urd-eng/edicion-ordenes.ts (19-05) -> window.abrirModalCalificarJuliosEng (puente)"
    - "routes/modules/engomado.php actualizar-campo-orden -> validación + auditar"
---

<objective>
Sacar el JS inline de Urdido y Engomado (~11 500 líneas en 25 vistas) a bundles Vite tipados siguiendo 19-00-RECETA, deduplicar los pares Urd/Eng (BPM, BPM-Line, actividades BPM, calificar-julios, reportes), y cerrar SEC-07, los N+1 medibles y el hueco de actualizar-campo-orden del backend del módulo, sin cambiar el diseño.
</objective>

<execution_context>
Sin SQL Server: tests en sqlite y pantallas con el arnés `19-01-arnes/` (sqlite en archivo + Playwright). Rama `claude/19-01-urdido-engomado` sobre `claude/friendly-hopper-506bg9`. Propiedad: fila 19-01 de `SESIONES-OLA-3.md`. Fuera: `programar-*`, `app/Livewire/UrdEng/**`, `app/Services/Programas/**`, `resources/js/urd-eng/**` (19-05), `tel-bpm` (19-04), utils/componentes/layouts (17-02), `package.json`, `vite.config.js`.
</execution_context>

<tasks>

<task type="auto">
<name>0. Receta, plan, arnés y capturas "antes"</name>
<files>.planning/phases/19-modulos/19-00-RECETA.md, 19-01-PLAN.md, 19-01-arnes/**</files>
<action>Escribir la receta común y este plan. Arnés sqlite + Playwright; capturas 768×1024 de 29 pantallas (0 errores de consola en el "antes").</action>
<done>Docs commiteados; capturas en el scratchpad de la sesión (referenciadas en el SUMMARY).</done>
</task>

<task type="auto">
<name>p1.1 Calificar julios en un modal parametrizado</name>
<files>resources/views/modulos/engomado/partials/modal-calificar-julios{,-eng}.blade.php, resources/views/modulos/urdido/comun/calificar-julios.blade.php, resources/js/modulos/urdido/comun/calificar-julios/*.ts, app/Http/Controllers/Engomado/Produccion/CalificarJuliosController.php</files>
<action>Una vista y un JS con `variante` (urdido: julios de UrdProduccionUrdido desde Producción Engomado; engomado: EngProduccionEngomado desde Reimpresión). `fetch` → `http`. Puente `window.abrirModalCalificarJuliosEng` para `urd-eng/edicion-ordenes.ts`. Backend: métodos duplicados → privado por modelo; SEC-07.</action>
<verify>Feature test de las 4 rutas (get/calificar × variante); test node de la lógica de defectos.</verify>
</task>

<task type="auto">
<name>p1.2 BPM, BPM-Line y actividades BPM compartidos</name>
<files>resources/views/modulos/{urdido,engomado}/{BPM-*,*-BPM-Line,*-actividades-bpm}/index.blade.php, resources/views/modulos/urdido/comun/{bpm,bpm-line,actividades-bpm}.blade.php, resources/js/modulos/urdido/comun/{bpm,bpm-line,actividades-bpm}/*.ts</files>
<action>Vista + JS parametrizados (rutas, etiquetas, columna autoriza, colores de estado, Máquina solo Urdido, paginación solo Engomado). Corrige: fecha vacía en Editar de Engomado BPM; doble disparo de botones en actividades Urdido. SEC-07 de UrdBpm/EngBpm.</action>
<verify>Tests node de filtros BPM; guardianes; run: crear/editar/checklist/terminar en ambas variantes.</verify>
</task>

<task type="auto">
<name>p1.3 Reportes con rango de fechas</name>
<files>resources/views/modulos/{urdido,engomado}/report*.blade.php, resources/js/modulos/urdido/comun/reporte-rango/*.ts</files>
<action>`Swal` con inputs → `x-ui.modal-base` compartido; validación de rango en logica.ts; resumen Urd/Eng a una vista y sus charts al bundle.</action>
<verify>Test node del rango; run de cada reporte (abre el modal si faltan fechas, navega con query).</verify>
</task>

<task type="auto">
<name>p1.4 Catálogos (ubicaciones, núcleos, julios, máquinas)</name>
<action>Misma receta; SEC-07 en CatUbicaciones, UrdEngNucleos, CatalogosUrdido.</action>
</task>

<task type="auto">
<name>p1.5 Captura de fórmula</name>
<files>resources/views/modulos/engomado/captura-formula/index.blade.php, resources/js/modulos/engomado/captura-formula/**, EngProduccionFormulacionController.php</files>
<action>2 800 líneas a módulos por responsabilidad (estado/folio, modales, componentes BOM, filtros de columna, calidad). Backend: SEC-07 (12), `create()` de líneas en loop → `insert()` por bloques, `LTRIM(RTRIM(Folio)) = ''` → `Folio = ''` (equivalente en SQL Server por padding ANSI), con número antes/después.</action>
</task>

<task type="auto">
<name>p1.6 Producción Engomado</name>
<files>resources/views/modulos/engomado/modulo-produccion-engomado.blade.php, resources/js/modulos/engomado/produccion/**, resources/js/modulos/urdido/comun/produccion/logica.ts, ModuloProduccionEngomadoController.php</files>
<action>2 560 líneas a TS; lógica pura (neto, rangos, oficiales duplicados, fila completa) compartida con Urdido. Backend: SEC-07 (6, incluida la QueryException con SQL), creación de filas en loop → insert por bloques, `actualizarCampoOrden` con lista blanca de campos y valor numérico (sigue en auditar).</action>
</task>

<task type="auto">
<name>p2.1 Producción Urdido</name>
<files>resources/views/modulos/urdido/produccion/*.blade.php, resources/js/modulos/urdido/produccion/**, ModuloProduccionUrdidoController.php, tests/Unit/ProduccionUrdidoScriptsBladeTest.php</files>
<action>`_scripts` a TS reutilizando la lógica común; el test de Blade pasa a verificar `data-orden-id`. Backend: SEC-07 (3), SELECT por grupo de Hilos → un whereIn, con número.</action>
</task>

<task type="auto">
<name>p2.2 Reportes, reimpresión y SEC-07 restante de Urdido</name>
</task>

<task type="auto">
<name>Cierre</name>
<action>Guardianes por vista, ratchet --update (solo bajas), phpstan-baseline (solo quitar), checks completos, code-review, run con capturas "después", SUMMARY + HANDOFF, push.</action>
<verify>php artisan test · phpstan · typecheck/test:js/build · ratchet · pint --test en tocados.</verify>
</task>

</tasks>
