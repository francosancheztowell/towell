---
phase: 14-mon-pulse
plan: "01"
wave: 1
depends_on: [11-01, 11-02, 11-03]
autonomous: true
requirements: [MON-29, MON-30, MON-31, MON-32, SEC-02]
files_modified:
  - composer.json
  - composer.lock
  - config/pulse.php
  - database/migrations/2023_06_07_000001_create_pulse_tables.php
  - resources/views/vendor/pulse/dashboard.blade.php
  - app/Providers/MonitoreoServiceProvider.php
  - app/Services/Monitoreo/Legado.php
  - app/Services/Monitoreo/{AccesoAdmin,ErrorRecorder,Monitoreo,AccesoService,DispositivoService}.php
  - app/Listeners/Monitoreo/{RegistrarLogout,RegistrarLogin}.php
  - app/Http/Controllers/Monitoreo/TelemetriaController.php
  - app/Models/Sistema/Monitoreo/MonDispositivo.php
  - bootstrap/app.php
  - phpstan-baseline.neon
  - tests/Feature/Monitoreo/{PulseTest,IpRealTest}.php
must_haves:
  truths:
    - "Pulse nunca toca SQL Server: guarda en la conexión `pulse` (SQLite, storage/pulse/pulse.sqlite)."
    - "/admin/pulse responde 200 a Sistemas, 403 a otra área y manda al login al invitado."
    - "Con PULSE_ENABLED=false no se graba nada, la migración se salta y /admin/pulse explica cómo encenderlo."
    - "Los errores siguen teniendo una sola fuente (SYSMonError): el recorder Exceptions está apagado."
    - "La IP que se registra es REMOTE_ADDR: sin trustProxies ni lectura cruda de X-Forwarded-For."
  artifacts:
    - "config/pulse.php con recorders y exclusiones del contexto."
    - "Tests de acceso, kill switch, migración y IP real."
  key_links:
    - "14-CONTEXT.md -> recorders, gate, ruta"
    - "11-CONTRACT.md §7 -> /admin/pulse con gate viewPulse = admin"
    - "10-base/HANDOFF.md §2 y §6 -> deuda phpstan y SEC-02"
---

<objective>
Pagar la deuda que dejó MON-A (phpstan y trustProxies) y agregar Laravel Pulse como métrica agregada de servidor en /admin/pulse, sobre una conexión SQLite propia, sin tocar config/database.php ni SQL Server.
</objective>

<execution_context>
Sin SQL Server: todo se prueba en sqlite (phpunit.xml ya trae PULSE_ENABLED=false). La conexión `pulse` se registra en runtime en MonitoreoServiceProvider::register(). Pulse 1.8 soporta sqlite; con sqlsrv lanza "Unsupported database driver".
</execution_context>

<tasks>

<task type="auto">
<name>14.0a Deuda phpstan de MON-A</name>
<files>app/Models/Sistema/Monitoreo/MonDispositivo.php, app/Listeners/Monitoreo/RegistrarLogout.php, app/Services/Monitoreo/ErrorRecorder.php, app/Services/Monitoreo/AccesoAdmin.php, phpstan-baseline.neon</files>
<action>
- `@property` de todas las columnas en MonDispositivo (las usa también el panel).
- RegistrarLogout: `$event->user` puede llegar null en logout aunque el tipo diga Authenticatable → `instanceof Authenticatable` en vez de `=== null`.
- ErrorRecorder: `$frame['function']` siempre existe → quitar el `?? ''`.
- AccesoAdmin: el Gate recibe `mixed`; leer `area` con `is_object()` en vez de `?->`.
- Quitar esas 5 entradas del baseline. Las de MensajesController no son de este track (quedan).
</action>
<verify>`vendor/bin/phpstan analyse` en verde sin esas entradas; tests de Monitoreo verdes.</verify>
<done>Baseline más corto, sin cambio de comportamiento.</done>
</task>

<task type="auto">
<name>14.0b SEC-02: IP real</name>
<files>bootstrap/app.php, app/Services/Monitoreo/Monitoreo.php, AccesoService, DispositivoService, RegistrarLogin, TelemetriaController, tests/Feature/Monitoreo/IpRealTest.php</files>
<action>
- Quitar `trustProxies(at: '*')` (el owner confirmó que no hay proxy delante de Laragon). Laravel deja de confiar en X-Forwarded-*.
- `getClientIpv4()` (helper ajeno, solo lectura) cae a X-Forwarded-For crudo cuando REMOTE_ADDR no es IPv4: el monitoreo deja de usarlo y usa `Monitoreo::ip()` = `request()->ip()` recortado a 45.
- Test: request con REMOTE_ADDR 10.1.2.3 y `X-Forwarded-For: 6.6.6.6` → `request()->ip()`, SYSMonAcceso.Ip, SYSMonSesion.Ip y SYSMonDispositivo.UltimaIp = 10.1.2.3; el rate limit de login usa esa IP (rotar XFF no evita el bloqueo).
</action>
<verify>IpRealTest verde; suite de Monitoreo verde.</verify>
<done>Ni el registro ni el rate limit dependen de un header que manda el cliente.</done>
</task>

<task type="auto">
<name>14.1 Instalar Pulse sobre conexión SQLite dedicada</name>
<files>composer.json, composer.lock, config/pulse.php, app/Providers/MonitoreoServiceProvider.php, database/migrations/2023_06_07_000001_create_pulse_tables.php</files>
<action>
- `composer require laravel/pulse` (solo trae laravel/pulse, laravel/sentinel y doctrine/sql-formatter).
- `register()`: si no existe `database.connections.pulse`, registrarla: driver sqlite, `database` = `PULSE_DB_DATABASE` o `storage_path('pulse/pulse.sqlite')` (`:memory:` en tests), `journal_mode=wal`, `busy_timeout=5000`, `synchronous=normal`, `foreign_key_constraints=false`.
- `config/pulse.php`: `path` = admin/pulse; `storage.database.connection` = `PULSE_DB_CONNECTION` (default `pulse`); `ingest.trim.keep` y `storage.trim.keep` 7 días.
- Migración publicada con `shouldRun()` = false si `pulse.enabled` es false (si prod no tiene pdo_sqlite, `migrate` no truena; una migración saltada no se registra y corre cuando se encienda). Crea el directorio y el archivo si faltan.
</action>
<verify>Test: con Pulse encendido la migración crea `pulse_*` en la conexión `pulse` y nada en `sqlsrv`; con Pulse apagado se salta.</verify>
<done>Pulse instalado sin tocar config/database.php.</done>
</task>

<task type="auto">
<name>14.2 Acceso, usuario y recorders</name>
<files>config/pulse.php, app/Providers/MonitoreoServiceProvider.php, resources/views/vendor/pulse/dashboard.blade.php, app/Services/Monitoreo/Legado.php, tests/Feature/Monitoreo/PulseTest.php</files>
<action>
- Middleware de Pulse: `web`, `auth`, `Authorize`. Gate `viewPulse` → `admin` (definido con `callAfterResolving(Gate)` para ganarle al default de Pulse, que solo abre en `local`).
- `Pulse::user()` → `name` = nombre, `extra` = `#numero_empleado`, `avatar` = `getFotoUsuarioUrl(foto)`.
- Recorders encendidos: SlowRequests (1000 ms), SlowQueries (500 ms, con ubicación), SlowJobs, SlowOutgoingRequests, UserRequests. Apagados: Exceptions (fuente única SYSMonError), Servers (requiere daemon `pulse:check`), CacheInteractions, Queues, UserJobs. Ignorar `#^/telemetria/#` y `#^/admin/pulse#`; `Pulse::filter` descarta 9 de cada 10 `user_request` de `livewire*/update` (muestreo 0.1).
- Vista publicada: tarjetas de uso, requests/queries/jobs/salientes lentas; si `pulse.enabled` es false, muestra un aviso con el fallback en vez de las tarjetas (así no consulta tablas que no existen).
- `Legado::registrar($uri)`: `Pulse::record('legado', $uri)->count()` dentro de `Monitoreo::seguro` (ADOP-01 lo usa en Ola 2).
</action>
<verify>PulseTest: Sistemas 200 (con Pulse encendido y apagado), Tejido 403, invitado → /login, SlowRequests/Exceptions según config, Legado no lanza con Pulse apagado.</verify>
<done>/admin/pulse listo y protegido.</done>
</task>

<task type="auto">
<name>14.3 Fallback y medición</name>
<files>.planning/phases/14-mon-pulse/14-01-SUMMARY.md</files>
<action>Documentar despliegue (pdo_sqlite en Laragon con `php -m | findstr sqlite`, `storage/pulse`, `migrate`, `optimize`), el fallback `PULSE_ENABLED=false` y un runbook para medir el overhead p95 contra 10-BASELINE.md. Medición local en sqlite con Pulse on/off como referencia.</action>
<verify>SUMMARY con evidencia de tests, phpstan, build y ratchet.</verify>
<done>El owner puede desplegar o apagar Pulse sin leer código.</done>
</task>

</tasks>
