---
phase: 15-fe-fundacion
plan: "01"
wave: 1
depends_on: [10-base, 11-mon-servidor]
autonomous: true
requirements: [FE-01, FE-02, FE-03, FE-04, FE-05, FE-06]
files_modified:
  - resources/js/utils/http.ts
  - resources/js/utils/notifications.ts
  - resources/js/utils/format.ts
  - resources/js/utils/dom.ts
  - resources/js/types/global.d.ts
  - resources/js/bootstrap.js
  - resources/js/tejido/inventario-telas.ts
  - resources/js/trazabilidad/*.ts (solo sintaxis/tipos, autorizado por el owner)
  - tsconfig.json
  - tests/Js/utils-*.test.mjs
must_haves:
  truths:
    - "window.http, window.notify, window.showToast, window.Swal, window.toastr, window.$ y window.axios siguen existiendo con la misma API."
    - "Toda petición de utils/http manda Accept: application/json y el CSRF fresco de la meta."
    - "Todo fallo de utils/http emite towell:http-error en window con detail {status, url, method} (contrato 11 §4)."
    - "Un 419 avisa y recarga una sola vez por página."
    - "Los toasts son nativos (contenedor aria-live) y nunca cierran un modal de SweetAlert2 abierto."
    - "tsc strict con erasableSyntaxOnly y verbatimModuleSyntax pasa sobre todo resources/js/**/*.ts, sin exclude."
    - "Ninguna vista Blade cambia."
  artifacts:
    - "utils/{http,notifications,format,dom}.ts + types/global.d.ts"
    - "tests/Js/utils-{http,notifications,format,dom}.test.mjs"
    - "15-01-SUMMARY.md y HANDOFF.md"
  key_links:
    - "11-CONTRACT.md §4 -> evento towell:http-error"
    - "bootstrap.js -> globals consumidas por ~130 vistas"
---

<objective>
Una sola capa TypeScript para HTTP, notificaciones, formato y DOM, tipada y probada en node, que reemplaza a utils/http.js y utils/notifications.js sin cambiar ninguna global ni ningún call-site. La migración de vistas es de fases posteriores (ADOP / 19-xx); el retiro de Toastr/jQuery es 15-02.
</objective>

<execution_context>
Node 22 con type-stripping: los tests importan `.ts` directamente, por eso solo sintaxis borrable y los imports relativos llevan extensión `.ts` (`allowImportingTsExtensions`, válido con `noEmit`). No hay jsdom: los tests de DOM usan un DOM mínimo falso (`tests/Js/utils-fake-dom.mjs`). axios se mantiene (ponytail); el adapter se inyecta por `config.adapter` en los tests.
Decisión del owner (2026-09-24): se autoriza tocar `resources/js/trazabilidad/**` solo en sintaxis/tipos (parameter properties y su `declare global`).
</execution_context>

<tasks>

<task type="auto">
<name>15-01.1 tsconfig estricto y trazabilidad borrable</name>
<files>tsconfig.json, resources/js/trazabilidad/*.ts</files>
<action>Activar erasableSyntaxOnly, verbatimModuleSyntax y allowImportingTsExtensions. Convertir las 16 parameter properties de trazabilidad en campos declarados con asignación en el constructor. Quitar el `declare global` de trazabilidad/types.ts (lo reemplaza types/global.d.ts).</action>
<verify>npm run typecheck sin errores fuera de tejido/ (aún excluido).</verify>
<done>Solo sintaxis borrable en todo resources/js/**/*.ts.</done>
</task>

<task type="auto">
<name>15-01.2 utils/format.ts y utils/dom.ts (FE-03, FE-04)</name>
<files>resources/js/utils/format.ts, resources/js/utils/dom.ts, tests/Js/utils-format.test.mjs, tests/Js/utils-dom.test.mjs</files>
<action>escapeHtml por string (sin DOM), debounce con cancel, formatNumber/formatDate/formatDateTime es-MX en America/Mexico_City (nulo/NaN → ''). qs, qsa (array), delegate (devuelve el unsubscribe), onReady.</action>
<verify>node --test de ambos archivos.</verify>
<done>Una sola implementación reutilizable de cada helper.</done>
</task>

<task type="auto">
<name>15-01.3 utils/http.ts (FE-01)</name>
<files>resources/js/utils/http.ts, tests/Js/utils-http.test.mjs</files>
<action>Misma API que http.js (get/post/put/patch/delete(url, config)/upload/csrfToken; default + named). Accept JSON, X-Requested-With y CSRF en cada llamada. HttpError normalizado (status, data, errors, original). towell:http-error en window en todo fallo (url sin query, method en mayúsculas, status 0 = red). 419 una sola vez: notify.alert + location.reload(); el error igual se lanza.</action>
<verify>Tests con adapter axios falso: data directa, headers, 422, evento, 419 único.</verify>
<done>Contrato 11 §4 cumplido del lado del emisor.</done>
</task>

<task type="auto">
<name>15-01.4 utils/notifications.ts (FE-02)</name>
<files>resources/js/utils/notifications.ts, tests/Js/utils-notifications.test.mjs, tests/Js/utils-fake-dom.mjs</files>
<action>Toasts nativos: contenedor aria-live polite, pila máx 4, 2.5/3/5/6 s, textContent, botón cerrar accesible, estilos inyectados una vez con prefers-reduced-motion. alert/validation/confirm/loading/close con SweetAlert2. showToast(message, type) con fallback a info.</action>
<verify>Tests con DOM falso.</verify>
<done>notify ya no depende de Toastr ni del singleton de Swal para toasts.</done>
</task>

<task type="auto">
<name>15-01.5 types/global.d.ts + bootstrap.js (FE-05)</name>
<files>resources/js/types/global.d.ts, resources/js/bootstrap.js</files>
<action>Declarar en Window: http, notify, showToast, Swal, axios, toastr, $/jQuery, Livewire. bootstrap.js importa los .ts y re-expone exactamente las mismas globals; se borran http.js y notifications.js.</action>
<verify>npm run build; en navegador las globals existen.</verify>
<done>Sin cambios en vistas.</done>
</task>

<task type="auto">
<name>15-01.6 Tipar tejido/inventario-telas.ts (FE-06)</name>
<files>resources/js/tejido/inventario-telas.ts, tsconfig.json</files>
<action>Tipos de dominio, casts de elementos del DOM, parámetros tipados y globals del módulo en un declare global local. Sin cambios de comportamiento. Quitar el exclude.</action>
<verify>npm run typecheck sin exclude; npm run build.</verify>
<done>Todo resources/js/**/*.ts bajo strict.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
<name>15-01.7 Verificación y entrega</name>
<action>typecheck, test:js, build, ratchet, php artisan test, code-review, skill run en 3 pantallas con http/notify (0 errores de consola). 15-01-SUMMARY.md + HANDOFF.md. Push a claude/15-01-utils-ts sin PR.</action>
</task>

</tasks>
