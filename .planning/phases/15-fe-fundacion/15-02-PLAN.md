---
phase: 15-fe-fundacion
plan: "02"
wave: 2
depends_on: [15-01]
autonomous: false
requirements: [FE-07, FE-08, FE-09, FE-10, FE-11, FE-12]
files_modified:
  - package.json
  - package-lock.json
  - vite.config.js
  - resources/js/bootstrap.js
  - resources/js/app.js
  - resources/js/app-core.js
  - resources/js/types/global.d.ts
  - resources/js/utils/combobox.ts
  - resources/js/utils/combobox.css
  - resources/js/utils/combobox-opciones.ts
  - resources/js/utils/librerias.ts
  - resources/js/lmat-lista/index.js
  - resources/js/catcodificacion/lmat-modal.js
  - resources/js/trazabilidad/filter-selects.ts
  - resources/js/trazabilidad/scroll-manager.ts
  - resources/css/fontawesome-display.css
  - public/js/catalog-core.js
  - tests/Js/combobox-opciones.test.mjs
  - tests/Js/utils-librerias.test.mjs
  - "vistas: solo líneas select2 / toastr / bg-opacity-* / <script> CDN (lista en cada tarea)"
must_haves:
  truths:
    - "Ningún archivo del repo importa ni usa jquery, select2 ni toastr; package.json ya no los declara."
    - "El layout (129 páginas) no carga ningún chunk vendor global: Tom Select, html2canvas y pdf.js solo bajan en la página/acción que los usa."
    - "Los 5 combos migrados (lmat-lista, color y artículo del modal LMat, 3 filtros de Trazabilidad, tarea de Redbooth) buscan, limpian, cargan remoto y disparan el mismo flujo que antes."
    - "Las 23 llamadas toastr fuera de PT pasan a notify; window.toastr sigue existiendo como adaptador a notify para PT (balancear/repaso) hasta la fase 21."
    - "Ratchet: toastr. baja; bg-opacity- baja; ninguna métrica sube."
  artifacts:
    - "resources/js/utils/combobox.ts (wrapper de Tom Select con su CSS)."
    - "resources/js/utils/librerias.ts (import() dinámico de html2canvas y pdfjs-dist, expuesto como window.librerias)."
    - "Tabla de JS inicial por página antes/después (manifest + gzip -9) en 15-02-SUMMARY.md."
  key_links:
    - "bootstrap.js -> window.combobox (carga diferida del wrapper) -> redbooth.blade.php"
    - "bootstrap.js -> window.librerias -> mecanicos/reportes, cortes-eficiencia"
    - "vite.config.js input glob resources/js/modulos/**/index.ts -> Ola 3"
---

<objective>
Retirar jQuery, Select2 (RC 2021) y Toastr (2018) del bundle global, reemplazándolos por Tom Select detrás de un wrapper propio y por `notify`; quitar el chunk `vendor` que cargan todas las páginas; traer html2canvas y pdf.js por npm con carga diferida; y corregir las regresiones de Tailwind v4 / Font Awesome 7 que son de FE.
</objective>

<execution_context>
Propiedad de archivos: `SESIONES-OLA-2.md` §15-02 (manda sobre el protocolo). En vistas solo se tocan las líneas de select2/toastr/`bg-opacity-*`/CDN; lo demás que haga falta va a `HANDOFF.md`. Sin SQL Server: la verificación visual se hace con la skill `run` sobre sqlite (como en 15-01). Línea base de JS inicial medida antes de tocar nada con el mismo script del SUMMARY (layout = `app.js` + `app-core.js` + imports estáticos).
</execution_context>

<tasks>

<task type="auto">
<name>15-02.1 Wrapper combobox (FE-07)</name>
<files>package.json, resources/js/utils/combobox.ts, resources/js/utils/combobox.css, resources/js/utils/combobox-opciones.ts, resources/js/types/global.d.ts, resources/js/bootstrap.js, tests/Js/combobox-opciones.test.mjs</files>
<action>`npm i tom-select`. `combobox(el, opciones)` crea/reusa la instancia de Tom Select (idempotente: si ya existe la devuelve), con opciones en español: `placeholder`, `permitirVacio` (botón limpiar), `multiple`, `dropdownParent` (elemento: modales de Swal / Redbooth), `remoto: { url, params(), procesar() }` que usa `http.get` (formato `{results:[{id,text}]}` de los endpoints actuales, con debounce), `render` para opciones y textos (sin resultados / buscando / error). `destruir(el)` restaura el `<select>`. La lógica pura (mapeo de resultados, construcción de parámetros, textos) vive en `combobox-opciones.ts` para probarla en node sin DOM. El CSS de Tom Select + un tema que imita los inputs Tailwind actuales se importa desde el wrapper (no desde app.css). `bootstrap.js` expone `window.combobox(el, op)` como carga diferida (`import()`), para scripts inline de Blade; los módulos Vite importan el wrapper directo. Para Livewire: `wire:ignore` en el contenedor o destruir antes del morph y recrear después (patrón que ya usa Trazabilidad).</action>
<verify>`tests/Js/combobox-opciones.test.mjs` (mapeo, parámetros, escape de HTML en templates, textos); `npm run typecheck`.</verify>
<done>Wrapper tipado, probado y sin estilos en app.css.</done>
</task>

<task type="auto">
<name>15-02.2 Migrar los usos de Select2 (FE-07)</name>
<files>resources/js/lmat-lista/index.js, resources/views/catalagos/lmat-lista.blade.php (bloque CSS select2), resources/js/catcodificacion/lmat-modal.js, resources/js/trazabilidad/filter-selects.ts, resources/js/trazabilidad/scroll-manager.ts, resources/views/modulos/programa-tejido/modal/redbooth.blade.php (solo bloque select2/jQuery)</files>
<action>lmat-lista: 3 filtros con `combobox`, `change` → `aplicarFiltros`; el CSS `.select2-*` de la vista se retira (lo cubre el tema del wrapper). lmat-modal: color y artículo dentro del popup de Swal (`dropdownParent: Swal.getPopup()`), recrear al rellenar opciones (`sync()`/destruir+crear), `asignarValorSelectLMat` sincroniza la instancia. Trazabilidad: filtros con carga remota de Flog (mismos parámetros artículo/tamaño/mes), destruir antes del dispatch a Livewire y recrear en `trazabilidad-filtros-actualizados`; `scroll-manager` cierra instancias abiertas y desenfoca sus inputs. Redbooth: `window.combobox` con remoto a `programa-tejido.redbooth.proyectos`, `dropdownParent` = modal, `selectValue` vía API de la instancia, `guardar` lee `proyecto.value`; z-index del dropdown en la misma línea de estilo. Sin cambio visual en Redbooth.</action>
<verify>grep: 0 `select2` en resources/ (salvo la CSS de trazabilidad, que es de solo lectura → HANDOFF). Skill `run`: los 4 lugares abren, buscan, limpian y filtran; 0 errores de consola.</verify>
<done>Ningún uso de Select2 ni de `window.jQuery`.</done>
</task>

<task type="auto">
<name>15-02.3 toastr → notify (FE-08)</name>
<files>modulos/produccion-reenconado-cabezuela.blade.php, modulos/urdido/produccion/_scripts.blade.php, modulos/engomado/partials/modal-calificar-julios{,-eng}.blade.php, modulos/engomado/modulo-produccion-engomado.blade.php (solo líneas toastr), resources/js/app-core.js, resources/js/bootstrap.js</files>
<action>Cada `toastr.x(msg)` → `notify.x(msg)` (sin el `typeof toastr` guard). `app-core.js` pierde `initToastr`. `bootstrap.js` deja `window.toastr` como adaptador `{success,error,warning,info,clear,options}` → `notify`, marcado para retirarse en la fase 21 (lo usan PT balancear/repaso hasta su sesión).</action>
<verify>`npm run ratchet`: `toastr.` 25 → 5 (solo PT); fijar con `--update`.</verify>
<done>Fuera de PT no queda toastr.</done>
</task>

<task type="auto">
<name>15-02.4 Quitar jQuery/Select2/Toastr (FE-09)</name>
<files>package.json, package-lock.json, resources/js/bootstrap.js, resources/js/app.js, resources/js/types/global.d.ts</files>
<action>Confirmar con grep que cada `$(`/`$$(` restante tiene `$` local y que no hay `$.fn`, `jQuery(`, `$.ajax`. `npm uninstall jquery select2 toastr`; quitar el shim de jQuery 4, los CSS de select2/toastr en app.js, los tipos `JQueryBridge`/`Select2Bridge`/`$`/`jQuery` (el tipo de `toastr` queda como adaptador). `app.js` deja de reasignar `window.Swal` (ya lo hace bootstrap).</action>
<verify>`npm run typecheck`, `npm run build`, grep vacío.</verify>
<done>package.json sin las 3 dependencias.</done>
</task>

<task type="auto">
<name>15-02.5 Vite sin vendor + inputs por glob (FE-10)</name>
<files>vite.config.js</files>
<action>Quitar `manualChunks`. `input` = entradas actuales + `globSync('resources/js/modulos/**/index.ts')` (de `node:fs`, Node 22; conservar la línea ancla de 16 si llega). Medir JS inicial por página (manifest + imports estáticos, gzip -9) antes/después; comparar con 10-BASELINE.</action>
<verify>`npm run build`; manifest sin chunk `vendor`; tabla antes/después.</verify>
<done>El layout baja sus KB; los entry points de `resources/js/modulos/**/index.ts` se detectan solos.</done>
</task>

<task type="auto">
<name>15-02.6 html2canvas y pdf.js por npm (FE-11)</name>
<files>package.json, resources/js/utils/librerias.ts, resources/js/bootstrap.js, modulos/mecanicos/reportes/{estado-maquina,ot-diarias}.blade.php, modulos/cortes-eficiencia/{cortes-eficiencia,visualizar-cortes-eficiencia}.blade.php (solo el `<script>` CDN y la llamada)</files>
<action>`npm i html2canvas pdfjs-dist`. `librerias.ts`: `html2canvas()` y `pdfjs()` con `import()` dinámico y singleton; pdf.js usa la build `legacy` (tablets viejas) y su worker como asset de Vite (`?url`). `window.librerias` en bootstrap. Vistas: se borra el `<script src=cdn>`; `const html2canvas = await window.librerias.html2canvas()` en la función de descarga; `cargarPdfJs()` pasa a `window.librerias.pdfjs()`.</action>
<verify>`tests/Js/utils-librerias.test.mjs` (singleton, error reintentable); build genera chunks aparte; skill `run`: la descarga de imagen funciona sin red a cdnjs.</verify>
<done>0 `<script src=https://cdnjs…>` en esas 4 vistas.</done>
</task>

<task type="auto">
<name>15-02.7 Regresiones Tailwind v4 / FA7 (FE-12)</name>
<files>vistas con bg-opacity-* fuera de PT y de catalogos-atadores (solo esa clase), resources/css/fontawesome-display.css, public/js/catalog-core.js</files>
<action>`bg-black bg-opacity-50` → `bg-black/50`, `bg-gray-900 bg-opacity-50` → `bg-gray-900/50`, etc. (en v4 `bg-opacity-*` no existe y el overlay sale sólido); `bg-black/40 bg-opacity-60` → `bg-black/40` (lo que se ve hoy). `fontawesome-display.css`: familia `'Font Awesome 7 Free'` (hoy declara la de FA6, así que el `font-display: swap` no aplica). `catalog-core.js`: ya no reasigna `window.showToast`; su `CatalogCore.showToast` delega en `notify` (sus llamadas internas siguen iguales).</action>
<verify>grep `bg-opacity-` solo queda en catalogos-atadores (16); ratchet baja; captura de un modal con overlay.</verify>
<done>Overlays semitransparentes y un solo `showToast` global.</done>
</task>

<task type="checkpoint:verify" gate="blocking">
<name>15-02.8 Verificación y entrega</name>
<action>typecheck, test:js, build, ratchet, `php artisan test`, phpstan, skill `code-review`, skill `run` (lmat-lista, trazabilidad, redbooth en PT, cortes de eficiencia, reporte de mecánicos, reenconado con toastr migrado): capturas antes/después y 0 errores de consola. `15-02-SUMMARY.md` con KB antes/después y `HANDOFF.md` (CSS select2 muerta en `resources/css/trazabilidad/index.css`, comentarios obsoletos).</action>
<done>Rama `claude/15-02-librerias` pusheada, sin PR.</done>
</task>

</tasks>

<verification>
- `npm run typecheck && npm run test:js && npm run build && npm run ratchet`
- `php artisan test`; `vendor/bin/phpstan analyse --memory-limit=2G`
- `grep -rn "select2\|jquery\|toastr" resources/js package.json` solo muestra el adaptador `window.toastr`.
- Capturas y consola limpia de las 6 pantallas.
</verification>

<success_criteria>
G2 (parte FE): sin jQuery/Select2/Toastr y sin vendor global; JS inicial del layout medido y menor que la línea base; ratchet de `toastr.` limitado a PT (5) y `bg-opacity-` limitado al piloto de 16.
</success_criteria>

<rollback>
Solo frontend: revertir los commits de la rama y `npm ci && npm run build`. Sin migraciones ni `.env`.
</rollback>
