---
phase: 13-mon-panel
plan: "01"
wave: 1
depends_on: [11-01, 11-02, 11-03, 14-01]
autonomous: true
requirements: [MON-21, MON-22, MON-23, MON-24, MON-25, MON-26, MON-27, MON-28]
files_modified:
  - routes/modules/admin.php
  - app/Livewire/Admin/Concerns/SoloAdmin.php
  - app/Livewire/Admin/{EnLinea,Sesiones,Navegacion,Rendimiento,Errores,ErrorDetalle,Accesos}.php
  - app/Services/Monitoreo/{PanelConsultas,AuditoriaAdmin}.php
  - resources/views/modulos/admin/{index,panel,_nav}.blade.php
  - resources/views/livewire/admin/*.blade.php
  - tests/Feature/Monitoreo/Panel*Test.php
must_haves:
  truths:
    - "Toda ruta /admin/* y toda llamada Livewire del panel exigen el Gate admin (403 a otra área)."
    - "En línea se calcula solo con SYSMonDispositivo (+ la sesión abierta por Id), nunca escanea SYSMonVista."
    - "El cierre remoto usa CierreRemotoService::solicitar(); renombrar y cambiar estado de error dejan SYSMonAcceso admin_accion con ActorId."
    - "p50/p95 salen del mismo SQL en SQL Server 2008 R2 y en sqlite (ROW_NUMBER, rango más cercano); PERCENTILE_CONT no existe en 2008."
    - "Sin <script> inline, sin fetch, sin onclick: Livewire + Alpine + notify."
  artifacts:
    - "7 componentes Livewire con tests de render, filtros, acciones y autorización."
  key_links:
    - "11-CONTRACT.md §5 cierre remoto, §6 definiciones, §7 rutas"
    - "app/Livewire/Concerns/ConTabla.php + components/tabla.blade.php"
---

<objective>
Llenar /admin (hoy placeholder) con el panel de monitoreo para el área Sistemas: dispositivos en línea con cierre remoto y renombrar, sesiones, navegación, rendimiento, errores con flujo de estado y accesos.
</objective>

<execution_context>
Tablas MON-A solo lectura, salvo las escrituras que el contrato asigna al panel: `CierreRemotoService::solicitar()`, `SYSMonDispositivo.Nombre`, y `SYSMonError.Estado/Nota/ResueltoPor/ResueltoEn`. Producción es SQL Server 2008 R2: la paginación va por `ConTabla::paginar()` (PaginacionCompat). Una vista host (`modulos/admin/panel.blade.php`) monta el componente de cada ruta; el menú de pestañas es un partial.
</execution_context>

<tasks>

<task type="auto">
<name>13.1 Rutas, vista host y protección</name>
<files>routes/modules/admin.php, resources/views/modulos/admin/*, app/Livewire/Admin/Concerns/SoloAdmin.php</files>
<action>
- Rutas del contrato §7 (`admin.index`, `admin.sesiones`, `admin.navegacion`, `admin.rendimiento`, `admin.errores`, `admin.errores.show` con `{id}` numérico, `admin.accesos`) con `can:admin`; `/admin/pulse` lo pone Pulse.
- Trait `SoloAdmin` con `bootSoloAdmin()` → `Gate::authorize('admin')` en cada request Livewire (mount y updates), no solo en la ruta.
- Pestañas con enlace a Pulse (solo si `pulse.enabled`).
</action>
<verify>Test por ruta: Sistemas 200, Tejido 403, invitado → /login; Livewire::test con Tejido → 403.</verify>
<done>Superficie /admin cerrada.</done>
</task>

<task type="auto">
<name>13.2 En línea (MON-22) + cierre remoto y renombrar (MON-28)</name>
<files>app/Livewire/Admin/EnLinea.php, resources/views/livewire/admin/en-linea.blade.php, app/Services/Monitoreo/AuditoriaAdmin.php</files>
<action>
- Tarjetas con conteo en línea / inactivo / desconectado (24 h) y tabla ConTabla + x-tabla sobre SYSMonDispositivo ⟕ SYSUsuario ⟕ SYSMonSesion (por UltimaSesionId): estado, dispositivo, usuario, área, IP, tipo/modelo, SO/navegador, página, tiempo en sesión, última actividad, front desactualizado. Filtro de estado (default: en línea + inactivos).
- `wire:poll.visible.{poll}s` con `max(10, monitoreo.poll_seconds)`; se pausa mientras el modal de renombrar está abierto.
- Acciones en el navbar (fila seleccionada): Cerrar sesión (`notify.confirm` en Alpine → `$wire.cerrarSesion(id)` → `CierreRemotoService::solicitar`) y Renombrar (modal, ≤ 80, auditado).
</action>
<verify>Tests: estados según contrato §6, poll presente, cerrar sesión deja llave de cache + admin_accion con ActorId, renombrar valida y audita.</verify>
<done>Sistemas ve su tablet en ≤ 15 s y la puede sacar.</done>
</task>

<task type="auto">
<name>13.3 Sesiones (MON-23), navegación (MON-24) y accesos (MON-27)</name>
<files>app/Livewire/Admin/{Sesiones,Navegacion,Accesos}.php + vistas</files>
<action>
- Sesiones: filtros usuario (nombre/número), dispositivo, desde/hasta; duración, origen, IP, motivo de fin.
- Navegación: requiere dispositivo o usuario + día (usa IX(DispositivoId, Inicio); por usuario se resuelven sus dispositivos desde SYSMonSesion); línea de tiempo y total visible por página.
- Accesos: filtros tipo, desde/hasta, buscar número/nombre; muestra actor en admin_accion.
</action>
<verify>Tests de filtros y render.</verify>
<done>Historial consultable.</done>
</task>

<task type="auto">
<name>13.4 Rendimiento (MON-26)</name>
<files>app/Livewire/Admin/Rendimiento.php, app/Services/Monitoreo/PanelConsultas.php</files>
<action>p50/p95 de ServidorMs y CargaMs por ruta, últimos 7 días vs 7 previos, con n. Percentil de rango más cercano con `ROW_NUMBER() OVER (PARTITION BY Ruta ORDER BY ms)` y `COUNT(*) OVER` (válido en SQL Server 2005+ y sqlite 3.25+). Cache 5 min. Lentas resaltadas con `monitoreo.umbrales`. Enlace a Pulse.</action>
<verify>Test con datos sembrados: p50/p95 exactos, comparación semanal, resaltado.</verify>
<done>Rutas lentas visibles.</done>
</task>

<task type="auto">
<name>13.5 Errores (MON-25)</name>
<files>app/Livewire/Admin/{Errores,ErrorDetalle}.php + vistas</files>
<action>
- Lista agrupada por huella con filtros estado/origen y búsqueda; enlace al detalle.
- Detalle: datos del error, últimos 50 eventos (usuario, dispositivo, URL, método, status, versión) y traza plegable; cambiar estado (nuevo/visto/resuelto/ignorado) con nota ≤ 500. `ResueltoPor/En` en resuelto o ignorado. Auditado.
</action>
<verify>Tests: filtros, detalle, cambio de estado + auditoría, estado inválido rechazado, 404 si no existe.</verify>
<done>Flujo de estado completo.</done>
</task>

<task type="auto">
<name>13.6 Verificación</name>
<files>.planning/phases/13-mon-panel/13-01-SUMMARY.md</files>
<action>Suite, phpstan, build, ratchet, pint en archivos tocados; skills security-review y code-review; capturas con la skill run (Sistemas 200, otra área 403).</action>
<verify>Todo en verde; capturas en el SUMMARY.</verify>
<done>Rama lista para integrar.</done>
</task>

</tasks>

<out_of_scope>
- Enlace "Admin" en user-modal: lo pone la sesión 12 (dueña de user-modal).
- Gráficas Chart.js: requieren tocar resources/js (prohibido en esta ola); se usan barras CSS.
- Resumen diario por Telegram (opcional): queda para después; las alertas son por correo (11-03).
</out_of_scope>
