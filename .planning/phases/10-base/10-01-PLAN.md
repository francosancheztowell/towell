---
phase: 10-base
plan: "01"
wave: 0
depends_on: []
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03, BASE-04, BASE-05, BASE-06, BASE-07, BASE-08, BASE-09, BASE-10, BASE-11, BASE-12, SEC-01, SEC-02]
branch: claude/10-base
files_modified:
  - .github/workflows/frontend-checks.yml
  - .gitignore
  - phpunit.xml
  - phpstan.neon
  - phpstan-baseline.neon
  - scripts/ratchet.mjs
  - scripts/ratchet-baseline.json
  - .planning/phases/10-base/10-BASELINE.md
  - tests/** (solo los 41 que fallan en sqlite, fuera de tests/*/Planeacion/**)
  - routes/public.php
  - routes/modules/tejedores.php (solo la ruta log-debug)
  - resources/views/modulos/usuarios/qr.blade.php
  - config/queue.php
  - config/logging.php (solo defaults)
  - .env.example
  - tsconfig.json
  - CLAUDE.md
  - .claude/settings.json
must_haves:
  truths:
    - "Ningún test se salta, se marca incompleto ni se borra para llegar a verde."
    - "CI corre PHP y JS en cada push/PR."
    - "Toda optimización posterior tiene contra qué compararse."
  artifacts:
    - "Job PHP en frontend-checks.yml verde."
    - "scripts/ratchet.mjs + baseline commiteados."
    - "10-BASELINE.md con runbook para Laragon."
---

<objective>
Poner las barandillas que protegen a todas las sesiones paralelas: CI con PHP, análisis estático, ratchet de deuda que no puede subir, línea base de rendimiento repetible e higiene rápida (código muerto, rutas debug, archivos expuestos, configuración de colas/log).
</objective>

<execution_context>
- Protocolo: `.planning/PROTOCOLO-SESIONES.md` (propiedad BASE). No tocar lógica de controllers de negocio ni nada del track PT (`tests/*/Planeacion/**`, `resources/js/programa-tejido/**`, etc.).
- Entorno nube: sin SQL Server. Tests en sqlite (`phpunit.xml`), trait `Tests\Concerns\UsesSqlsrvSqlite` (métodos `useSqlsrvSqlite()`, `createTablaDesdeModelo()`, `createProgramaTejidoTable()`).
- Lo que necesite datos reales (baseline de tiempos, `.env` de prod) se entrega como runbook/script para el owner.
- Skills útiles: `session-start-hook` (BASE-12), `laravel-specialist`/`php-pro` (`.agents/skills`), `security-review` (SEC-01), `code-review`.
</execution_context>

<tasks>

<task type="auto">
<name>10.1 SessionStart hook (BASE-12)</name>
<files>.claude/settings.json, scripts/ (si hace falta un script de hook)</files>
<action>Usar la skill `session-start-hook` para que las sesiones web ejecuten `composer install --no-interaction` y `npm ci` al arrancar (idempotente, rápido si ya está instalado). Conservar `enabledPlugins` existente.</action>
<verify>Una sesión nueva tiene `vendor/` y `node_modules/` sin pasos manuales.</verify>
<done>Hook commiteado y probado en esta sesión.</done>
</task>

<task type="auto">
<name>10.2 Arreglar los tests que fallan en sqlite (BASE-01)</name>
<files>tests/** fuera de Planeacion</files>
<action>`composer install`, `php artisan test` y listar los fallos. Para cada uno: si falta tabla, usar `UsesSqlsrvSqlite` / `createTablaDesdeModelo()`; si depende de semántica real de SQL Server (triggers, `sp_SetAppContext`, `sys.*`), moverlo a un grupo `@group sqlserver` excluido en `phpunit.xml` y documentar en `tests/README-sqlserver.md` cómo correrlo en Laragon. **Prohibido** `markTestSkipped`, `markTestIncomplete`, borrar asserts o tests. Los fallos dentro de `tests/*/Planeacion/**` se listan en HANDOFF para el track PT, sin tocarlos.</action>
<verify>`php artisan test` en verde (salvo el grupo `sqlserver` documentado y los de PT listados en HANDOFF).</verify>
<done>Conteo antes/después en el SUMMARY.</done>
</task>

<task type="auto">
<name>10.3 CI PHP (BASE-01, BASE-02)</name>
<files>.github/workflows/frontend-checks.yml, phpstan.neon, phpstan-baseline.neon</files>
<action>Agregar un job `php` al mismo workflow (el `.gitignore` solo permite `frontend-checks.yml`): `shivammathur/setup-php` 8.4 con `pdo_sqlite`, cache de composer, `composer install`, `php artisan test`, `vendor/bin/phpstan analyse` (larastan nivel 5, `--generate-baseline` inicial), y Pint `--test` solo sobre archivos PHP cambiados contra la base del PR. Actualizar el comentario del workflow que explica por qué no corría PHP.</action>
<verify>El workflow corre verde en la rama.</verify>
<done>CI PHP + JS verde; recomendar al owner marcarlo como required.</done>
</task>

<task type="auto">
<name>10.4 Ratchet de deuda (BASE-03)</name>
<files>scripts/ratchet.mjs, scripts/ratchet-baseline.json, package.json (script `ratchet`)</files>
<action>Script Node sin dependencias que cuenta en `resources/views/**` y `resources/js/**` + `public/js/**`: `fetch(`, `Swal.fire`, `toastr.`, `onclick=`, `innerHTML =`, `X-CSRF-TOKEN`, bloques `<script>` sin `src` en blades, `getMessage()` dentro de `response()->json` en `app/**`, `bg-opacity-`. Modo `--update` reescribe el baseline; modo normal falla si algún conteo sube e imprime qué bajó. Agregar al job JS del CI. **Nota:** `package.json` es archivo caliente: solo agregar el script `ratchet` y la dependencia de 10.7.</action>
<verify>`node scripts/ratchet.mjs` pasa; subir artificialmente un conteo lo hace fallar.</verify>
<done>Baseline commiteado con los números actuales.</done>
</task>

<task type="auto">
<name>10.5 Línea base de rendimiento (BASE-04)</name>
<files>.planning/phases/10-base/10-BASELINE.md</files>
<action>Documento con: (a) tamaños de chunks de `npm run build` (medidos aquí); (b) KB de JS inline por vista top-15 (medido aquí con script); (c) **runbook para Laragon** (el owner lo corre) que mide TTFB y número de queries por pantalla con `php artisan db:profile` (`app/Console/Commands/DbProfileCommand.php`) y el método de `.planning/phases/04-ux-grid/04-PERF-MEDIDO.md`, con tabla vacía para llenar. Pantallas: programa-tejido, liberar-ordenes, balancear, codificación, captura-fórmula engomado, producción engomado, producción urdido, calificar-atadores, programa atadores, cortes-eficiencia, inventario-telas, trazabilidad, crudo dashboard, paros, home `/produccionProceso`.</action>
<verify>Los números medidos aquí son reproducibles con los comandos del documento.</verify>
<done>Baseline listo para comparar en fases 11, 15, 18, PT.</done>
</task>

<task type="auto">
<name>10.6 Higiene (BASE-05, BASE-11)</name>
<files>resources/views/modulos/urdido/programar-urdido.blade.php, resources/views/modulos/engomado/programar-engomado.blade.php, app/Services/ProgramaUrdEng/OrdenKarlMayerService.php, public/audio/click-sound.js, public/pwa-check.mjs, public/towell-ca.crt, public/towell.csr, routes/public.php, routes/modules/tejedores.php, CLAUDE.md</files>
<action>Para cada archivo: `grep -rn` de su nombre/clase/vista en `app/ resources/ routes/ tests/ public/` y borrar solo si no hay referencias vivas (los tests que lean esas vistas como texto se ajustan o se documentan). Quitar la ruta `/test-404` (y su método si queda huérfano) y `tel-bpm/log-debug` (y `logDebug` si queda huérfano). Sacar certificados del web root (documentar en SUMMARY que el owner debe rotarlos si eran privados). En `CLAUDE.md`: cache prefix `modulos_v3`, `routes/ai.php` es local/gitignored, documentar CI PHP y ratchet.</action>
<verify>`php artisan route:list` sin esas rutas; tests verdes; `npm run build` verde.</verify>
<done>Lista de borrados con evidencia de grep en el SUMMARY.</done>
</task>

<task type="auto">
<name>10.7 Página QR (BASE-06)</name>
<files>resources/views/modulos/usuarios/qr.blade.php, package.json, vite.config.js (solo si hace falta un input), resources/js/usuarios/qr.ts</files>
<action>Hoy el `<script>` de qrcodejs está comentado y la línea ~103 llama `new QRCode(...)` → la página truena. Usar el paquete npm `qrcode` (render a canvas/SVG) en un `resources/js/usuarios/qr.ts` cargado con `@vite`. Mantener el contenido codificado (número de empleado) y el diseño.</action>
<verify>`npm run build` verde; test de markup o test JS que confirme que no queda `new QRCode` sin librería.</verify>
<done>Página QR funcional.</done>
</task>

<task type="auto">
<name>10.8 Colas, logs, cache, Ventas, tsconfig (BASE-07..10)</name>
<files>config/queue.php, config/logging.php, .env.example, tsconfig.json, config/database.php (solo si procede, ver nota)</files>
<action>
- BASE-08: default de `queue.failed.driver` a `database-uuids` (tabla `failed_jobs` ya migrada); `LOG_STACK=daily`, `LOG_DAILY_DAYS=30` en `.env.example`. Quitar el bloque MAIL duplicado de `.env.example`.
- BASE-09: **no** crear tabla `cache` sin saber el driver real; dejar en SUMMARY la pregunta al owner y la migración lista como opción (`php artisan make:cache-table` sugerido) solo si confirma `CACHE_STORE=database`.
- BASE-07: los modelos `app/Models/Ventas/*` usan `sqlsrv_Reportes_Towell`, que se quitó de `config/database.php` en el commit 11740c7 (antes agregado en f8d4a80). Leer `scripts/db-config.ps1` y ese historial; restaurar la conexión **leyendo todo de env** (sin secretos) y agregar las llaves a `.env.example`. Si el flujo skip-worktree hace que editar `config/database.php` en git sea contraproducente, documentar en SUMMARY los pasos exactos para el owner en vez de editarlo.
- BASE-10: `tsconfig.json` → `include: ["resources/js/**/*.ts"]` + `exclude: ["resources/js/tejido/**"]` (con comentario: lo tipa FE-06). `npm run typecheck` debe quedar verde; si otro archivo no tipa, excluirlo nominalmente y listarlo en HANDOFF para FE.
</action>
<verify>`php artisan config:show queue.failed`; `npm run typecheck` verde.</verify>
<done>Config y .env.example actualizados; decisiones pendientes del owner en SUMMARY.</done>
</task>

<task type="auto">
<name>10.9 SQL interpolado y proxies (SEC-01, SEC-02)</name>
<files>app/Http/Controllers/Tejido/InventarioTelas/TelaresController.php, app/Services/ProgramaUrdEng/InventarioReservasService.php, y los otros 3 casos que encuentres; bootstrap/app.php NO (es de MON-A en Ola 0)</files>
<action>Buscar SQL crudo con variables PHP interpoladas (`DB::select("...$var...")`, `whereRaw("... {$x}")`, `selectRaw`/`orderByRaw` con input). Distinguir expresiones construidas desde constantes/whitelist (ej. el CASE `{$barra}` de TelaresController, que puede ser falso positivo) de input real; parametrizar solo lo que venga de request/BD con bindings **sin cambiar el resultado**; test que lo cubra si es posible en sqlite. Si alguno cae en archivos del track PT, dejarlo en HANDOFF. SEC-02: no editar `bootstrap/app.php`; documentar en SUMMARY la recomendación de `trustProxies` (solo IPs del proxy real, o ninguno) para que MON-A la aplique con la respuesta del owner.</action>
<verify>`security-review` sobre el diff; tests verdes.</verify>
<done>0 interpolaciones de input en SQL fuera de PT.</done>
</task>

</tasks>

<verification>
php artisan test · vendor/bin/phpstan analyse · npm run typecheck && npm run test:js && npm run build · node scripts/ratchet.mjs · CI verde en la rama.
</verification>

<deliverables>
`10-01-SUMMARY.md` (conteos antes/después, borrados con evidencia, decisiones pendientes del owner: CACHE_STORE/SESSION_DRIVER reales, Ventas `.env`, rotación de certificados, trustProxies) y `HANDOFF.md` si hay fallos en PT o archivos ajenos.
</deliverables>
