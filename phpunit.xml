<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <!-- Grupo sqlserver: prueban triggers/pantallas que solo existen en SQL Server real.
         En sqlite no ejercitan nada; se corren a mano en Laragon (tests/README-sqlserver.md). -->
    <groups>
        <exclude>
            <group>sqlserver</group>
        </exclude>
    </groups>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- NO descomentar hacia atras: sin esto la suite corre contra la BD real
             de .env (ProdTowel) y ningun test usa RefreshDatabase ni
             DatabaseTransactions, asi que escribe sin rollback.
             Los tests que necesiten esquema de SQL Server usan el trait
             Tests\Concerns\UsesSqlsrvSqlite, que reapunta la conexion 'sqlsrv'
             a sqlite y crea sus tablas. -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
